// WP8 — flat textured plane + Rapier collider (Phase 1). Swappable for heightmap in Phase 3.
// Interface defined here so later WPs edit, not create.
//
// interface Terrain {
//   getHeight(x: number, z: number): number;
//   getMesh(): THREE.Mesh;
//   getCollider(): unknown; // Rapier.Collider — typed when WP8 lands
// }
export {};
