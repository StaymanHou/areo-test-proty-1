# Web Flight Sim

## Project Overview

Browser-based flight simulator for casual gamers. A player opens a URL and is flying within 30 seconds — no install, no tutorial, no account. Physics are plausible (6DOF rigid body with lift/drag/thrust/stall on per-surface aero model) but tuned for accessibility rather than study-level accuracy. v1 ships four mission types: free flight, waypoint navigation, takeoff/landing, and combat.

**Not goals for v1:** multiplayer, accounts, persistence, mobile, monetization, mission editor, multiple aircraft.

See `docs/product/vision.md` for the full vision.

## Tech Stack

- **TypeScript** (strict) — catches math bugs in physics code
- **Three.js** (r170+) — rendering. Chosen over Babylon.js for ecosystem depth, AI-assist friendliness, and smaller bundle.
- **Rapier3D** (`@dimforge/rapier3d-compat`, optionally `rapier3d-simd` for perf) — physics. 2–5× faster than alternatives in 2026; cannon-es and Ammo.js are unmaintained.
- **Vite** — build + dev server with HMR
- **lil-gui** — in-dev tuning UI behind `?debug=true`
- **stats.js** — FPS counter, on from day one
- **No backend. No database.** Static deploy to Vercel / Netlify / Cloudflare Pages.

## Project Structure

```
docs/product/          # vision, roadmap, research, arch, wbs, context — durable reference
workflow/              # transient workflow state (wip/, backlog.md, archive/)
src/
  main.ts              # entry
  engine/              # game loop, input, assets, debug UI
  world/               # scene, terrain, camera
  aircraft/            # rigidbody, aerosurface, flightmodel, controls
  mission/             # Phase 2 (WP11): loader.ts, runner.ts, parse.ts, select.ts, hooks/
  hud/                 # Phase 2 (WP12): HUD interface + dom-hud.ts implementation
public/
  models/              # GLTF aircraft, textures
  missions/            # Phase 2 (WP11): declarative-JSON mission definitions
  config/
    aircraft.json      # tunable flight model constants
tests/e2e/             # Playwright smoke (WP9.6) + Phase 2 mission-level probes
CLAUDE.md              # this file
CONVENTIONS.md         # coord conventions, module rules, β1/β4/β5 sign conventions
```

## Getting Started

### Prerequisites
- Node.js 18+
- Modern browser (Chrome / Safari / Firefox latest)

### Setup
```
npm install
npm run dev
```

Open the printed URL. Append `?debug=true` for the lil-gui tuning panel and Stats.js FPS counter.

### Build & Deploy
```
npm run build    # outputs static files to dist/
```
Deploy `dist/` to any static host.

No Docker. No backend.

## Development Conventions

- **TypeScript strict mode** — `strict: true` in `tsconfig.json`. No `any` without a comment explaining why.
- **Fixed-timestep physics, variable-timestep render.** Non-negotiable. See arch D1. Rapier produces wrong results at variable dt.
- **Right-handed Y-up coordinates** for both Three.js and Rapier — they align, no transform at the sync boundary. Document any exception.
- **Aerosurface as first-class primitive.** Every lift-producing part is an `AeroSurface`. Don't add ad-hoc lift/drag formulas elsewhere — extend the aerosurface model.
- **Flight-model constants live in `public/config/aircraft.json`**, not in code. Tune via lil-gui, export back to JSON.
- **Debug UI gated on `?debug=true`.** Never ship debug panels to end users.
- **No framework (React/R3F).** Vanilla Three.js. Revisit if mission-select / HUD grows beyond basic DOM overlays.
- **Phase discipline.** Phase 1 closed 2026-05-12 (post-WP10 arch revision). Phase 2 populates `mission/` and `hud/` per D11/D12. Phase 2 work does not pre-implement Phase 3 polish (audio, visual replacement, onboarding) — those are WP18+ deliverables. AI architecture (behavior tree vs FSM) is a WP16-internal decision, not an arch decision.
- **Write code for a casual-gamer audience.** "Feels right" beats "is accurate." When tuning a constant, the test is whether a non-pilot player says "yeah, that's how a plane should behave."

### Physics-mechanism discipline

Lessons codified from SURFACE-2026-05-10-01 (β-AoA sign convention bug) and SURFACE-2026-05-12-03 (β5 `clAlphaDot` mechanism diverges at every tuning value). Both shipped because pure-math tests passed for physically wrong behavior. Both rules below are MUST-follow for any new physics mechanism added under `src/aircraft/` (future β-coefficients, new aero terms, alternate force/moment routes).

1. **No sign-convention unit tests before live-system observation.** New physics mechanisms — any field added to `AeroSurfaceConfig` / `AircraftSurfaceConfig` that affects force/moment computation — require a Playwright probe at a non-zero coefficient before any sign-convention unit test (`"positive X produces +Y in regime Z"`) is written. Internal math consistency ≠ physical correctness; pure-math tests will pass for the destabilizing direction. The probe must observe the mechanism in at least two operating regimes (e.g. low- and high-throttle, or rising- and falling-α) for a ≥10s window. Only after live observation confirms the sign, write the unit test that codifies it. **Origin:** `aerosurface.test.ts:1135` codified the destabilizing direction of β5 as correct (the "rising α → +lift" assertion); WP14.5 disposed of that mechanism as untunable after 3 attempts.

2. **Schema-landing close requires a non-default verify-self, not just default-parity.** A WP that adds a new physics-mechanism schema field cannot close on the strength of `default value preserves prior behavior` parity tests alone. That's back-compat validation, not arch-decision validation. The close gate is a verify-self at a non-default value against the SURFACE the schema was introduced to address — i.e., the mechanism must demonstrably do its claimed job before the schema-land WP is declared done. Without this gate, a defective mechanism ships invisibly under `default=0` and surfaces only at the downstream tuning WP. **Origin:** WP10.5 closed β5 schema-landing on 256/256 Vitest parity; the mechanism was never observed at non-zero values until WP14.5, where it diverged in every regime tested. **Interpretation clarification (added 2026-05-17 post-WP14.9b):** "non-default coefficient" means "any non-default value that activates the mechanism observably and stays finite." It does NOT mean "the textbook-range value" or "the spec-quoted high-stress value" specifically. If the empirical stable region under the current `aircraft.json` parameters is narrower than the textbook reference suggested, Rule #2 is satisfied by ANY non-default value within the empirical stable region that (a) stays finite over the SURFACE-claimed regime, AND (b) is observably different from the control (clQ=0 or analog) trajectory. **Why:** Conflating "textbook-range non-default fails to stay finite" with "implementation is defective" routes the WP into a wrong escalation lane — the failure may be a tuning-bounds question (handled by the tuning WP, e.g. WP14.11) rather than a mechanism-implementation question (which would require arch revision). **Clarification origin:** WP14.9b verify-self — clQ=12 NaN'd at tick 687 but clQ=1 stayed finite 1800 ticks and observably differed from clQ=0; the empirical stable region `[0..~1.5]` was narrower than arch.md D17's "textbook 1–10" claim (SURFACE-2026-05-17-03). Reading Rule #2 as "clQ=12 must work" would have failed WP14.9b incorrectly; reading it as "clQ=1 satisfies, and the narrow stable region is a tuning concern" closed correctly.

3. **Physics-mechanism tuning runs through the harness, not hand-guessing.** Any WP that adjusts `aircraft.json` physics constants (mass, thrust, areas, β-coefficients, inertia) MUST run the harness optimizer (`npm run tune`) over an explicit parameter space and commit the optimizer's output artifact alongside the JSON change. Hand-guessing physics values is reserved for (a) initial schema-land WPs where defaults must be 0 anyway, and (b) the operator-as-architect explicitly choosing a value for non-physical reasons (e.g., gameplay feel override). **Origin:** WP14.5 exhausted the 3-attempt budget on 3 sparse points in a continuous parameter space; the mechanism may or may not have been tunable — the search density was too low to know.

4. **Physics-mechanism verify-self requires a control regime alongside baseline + non-default.** The Rule #2 non-default verify-self proves "the mechanism does its claimed job at a non-default coefficient." It does NOT prove "the mechanism is the *driver* of any observed behavior" — for that, a control regime (mechanism disabled entirely, e.g. `clQ=0`, `clAlphaDot=0`) must run alongside in the same fixture. The control disambiguates "is this defect caused by my mechanism, or by something else interacting with it?" Without it, a verify-self failure can be misread as a different mechanism's bug, sending the WP into the wrong escalation lane. **Verify-self contract for physics-mechanism WPs:** one harness run at baseline (current aircraft.json), one at non-default per Rule #2, and one with the new mechanism's coefficient(s) set to 0. Compare; the control should be visibly different from both other runs (proves your mechanism is doing *something*); the baseline and non-default should differ from each other in the SURFACE-claimed direction (proves your mechanism is doing the *intended* thing). **Origin:** WP14.9 verify-self (2026-05-17) — the clQ=0 control was added ad-hoc during diagnostic interpretation when both baseline and clQ=12 NaN'd; it ruled out "second instability source" hypotheses cleanly and isolated β4 as the sole driver. Without it, the misframing of attempt-1's wrong-layer implementation could have been mistaken for a different mechanism's bug, routing into Option 2 (Form B re-evaluation) instead of Option 3 (full V-scaling reframe). **Plan-time addendum (added 2026-05-23 post-WP14.10):** When choosing the verify-self regimes for a new physics-mechanism WP, do not inherit the WBS description's regime list verbatim. Read the most recent session-pause note and `aircraft.json` baseline to identify which regimes currently NaN under the existing setup, and substitute clean regimes for any polluted ones. Baseline-NaN regimes pollute the mechanism-under-test's signal — verify-self at a regime where baseline NaN's cannot disambiguate "my mechanism failed" from "the orthogonal mechanism failed." arch.md verify-self contracts that read "≥2 regimes (low-V + high-V OR rising-α + falling-α)" give explicit license to swap regimes as long as the structural intent (two operating envelopes) is satisfied. Document the substitution + rationale in the WIP file's verify-self leaf. **Origin:** WP14.10 plan listed throttle-low + throttle-high per WBS WP14.10 description, but baseline aircraft.json's β4 setup (clQ=3,3,8,0) NaN's at throttle-high (per WP14.9b session-pause 2026-05-17 18:32). Substituted throttle-low + throttle-mid at verify-self execution; arch.md D16's two-regime requirement satisfied; deviation documented in WIP retrospect.

5. **Plan-time physics derivation precedes spec-text reading for new physics mechanisms.** Before reading the binding spec text in arch.md or wbs.md for any new physics-mechanism WP, do a 30-second derivation pass: (a) right-hand-rule any cross products in the formula, (b) dimensional-analyze the result (does ΔF scale as V? V²? V³? — compare to the destabilizing-moment scaling), (c) trace one numerical example through with concrete unit-quantity values. THEN read the spec text and compare. The aim is to catch arch-spec errata (sign-inverted cross products, misnamed factors, transcription bugs) at plan time instead of at first-failing-test time. **Origin:** The pattern fired 3× in 24 hours during the D14/D17 cascade: WP14.9 attempt-1 (D15 implementation-layer ambiguity — caught at verify-self, costing the WP); D17 arch.md authoring (cubic-V³ damping growth via dimensional analysis — caught at WP14.9 escalation, drove the reframe); WP14.9b (arch.md D17 `(position × normal)` cross-product order sign-inverted vs textbook damping convention — caught at first Vitest run, SURFACE-2026-05-17-02). Operator-as-architect makes the operator both spec author and implementer; this rule keeps the architect side honest by forcing derivation before implementation reads the architect's own spec text.

6. **Two-close-gates split for mechanism-impl + tune-deploy WP pairs.** When a new physics-mechanism schema field is added (β-coefficient or analog), split the work into TWO WPs: (a) a **mechanism-impl WP** that ships the code change with default-inactive gates + the Rule #1/#2/#4 triple gate at a non-default coefficient, demonstrating "the mechanism does its claimed job"; (b) a **tune-deploy WP** that commits `aircraft.json` values + a 1800-tick gate + browser-walkthrough verify-self. The mechanism-impl WP closes DONE on *implementation soundness* (default-zero parity + non-default mechanism activation) without modifying `aircraft.json`; the tune-deploy WP closes the SURFACE on *deployment + envelope*. **Why:** the 1800-tick gate at fully-tuned values requires joint tuning of the new mechanism alongside any pre-existing destabilizers (β4 in the D18 case); attempting both in one WP risks the impl WP being stuck behind tuning attempts that should run through the harness optimizer per Rule #3. The split also makes the "default-inactive ships" claim verifiable in isolation. **Origin:** the pattern has fired twice — D17 cascade (WP14.9b ships β4 non-dim mechanism; WP14.11 was the joint tune — escalated, then WP14.12 takes over), and D18 cascade (WP14.11.5 ships the drag polar; WP14.12 will joint-tune (clQ, clAlphaDot, inducedDragK, fuselageDrag.*)). Future βN extensions should follow the same split unless the mechanism is intentionally default-active (rare).

7. **Per-tick energy-budget sanity check on the integrator.** When a physics-tuning WP shows trajectory anomalies (NaN cascades; AS / vZ / vY growth far exceeding what F=ma predicts from thrust + gravity + drag), run a one-tick sanity check BEFORE adding mechanism layers: with all augmented mechanisms zeroed and thrust = 0, gravity-only trajectory should give `vY = -g·t` exactly; with thrust > 0 and aero zeroed, `Δv per tick` should equal `thrust / mass / Hz` at every tick. If `Δv` grows with tick number (compounds), suspect the integrator's force accumulator: Rapier requires `body.resetForces(true) + resetTorques(true)` before each `applyForces` call. The cascade fix at `src/aircraft/physics-core/flightmodel.ts:applyForces` enforces this at the API boundary. **Origin:** SURFACE-2026-05-24-09 / `fix-resetforces-bug` task (2026-05-24). The D14→D23 cascade spent ~13 days adding β4 / β5 / drag-polar mechanisms to dissipate energy that a missing `resetForces` call was injecting at (n+1)× per tick. Adding more aerodynamic damping cannot fix an integrator that lacks reset semantics; the per-tick budget check cheaply separates "the mechanism is wrong" from "the integrator is wrong."

8. **Initial-condition-equilibrium-consistency for level-flight score-function fixtures.** When the score function evaluates "level flight" (criterion 0: altitude drop ≤ threshold over a 1-second window) at a parity-fixture spawn condition, the fixture's spawn AS MUST be within ~20% of the airframe's **L=W trim AS** — the AS at which lift equals weight at the airframe's incidence-induced cruise α. For a fixed-α airframe (like this project's symmetric-flat-plate wings at fixed `incidenceRad`), the L=W trim AS is **throttle-independent**: V_trim = √(2·W / (ρ·S·CL_at_trim_α)). For the production WP14.10 aircraft.json (W=9810, S=12, ρ=1.225, CL≈0.22 at incidenceRad=0.0349), V_trim ≈ **78 m/s**. **All fixtures spawn at V_trim regardless of throttle**; throttle determines whether the airframe accelerates (climbs), holds (cruises), or decelerates (descends) FROM the common trim AS. If `aircraft.json` mass / wing area / CL_at_trim_α changes, V_trim MUST be re-derived alongside (note: thrust no longer enters this derivation — it only determines T-vs-D balance AT V_trim, not V_trim itself). The derivation belongs in `docs/product/arch.md`; the constants live in `tests/parity-fixtures.ts`. **Origin:** D24 architect cycle (arch.md Revision 2026-05-25) FIRST formulation; **D25 amended (arch.md Revision 2026-05-25 afternoon)** to remove the erroneous "at the fixture's throttle" qualifier — D24 conflated T=D balance with L=W balance. D24's qualifier produced per-regime spawn AS {45, 78, 128} which made high regime rocket-climb to alt=636m (lift=2.7×W at spawn) and low regime crash within 5s (lift=0.33×W at spawn). D25 corrects: uniform spawn AS=78 for all regimes; throttle alone differentiates climb/cruise/descent. The entire D14→D23 cascade's residual criterion-0 failure was driven by spawn AS=30 being below V_trim=78 at every throttle; D24 fixed mid; D25 fixed low and high.

### Testing
- Unit tests for pure physics math (aerosurface lift/drag at known α, stall behavior). Test framework: Vitest (default with Vite, TBD at WP1). Run via `npm run test`.
- End-to-end browser tests: `@playwright/test` (adopted WP9.6). Run via `npm run test:e2e`. Lives under `tests/e2e/`. Currently a single load-bearing smoke (`casual-flight.spec.ts`) — the WP9.5 collider-fix regression anchor; loads `?debug=true`, waits 5s, asserts via `window.__aircraft.getState()` that altitude/airspeed are finite, aircraft moved from spawn, no NaN/Infinity in console. Chromium-only at this phase (cross-browser is WP21). Keep this suite tiny per the "Playwright tests are flaky" trap noted in SURFACE-2026-05-09-01.
- No integration tests for the render loop — validation is playtesting.
- **Three.js mutable-buffer trap when testing `computeAeroForce` and similar reused-output APIs.** `computeAeroForce` returns an `AeroForceResult` whose `.force` and `.applicationPoint` Vector3s are reused across invocations (allocation-free hot path; documented in the function's JSDoc + the `result.force vector is reused` regression test). When writing tests that compare force outputs across two or more calls, snapshot scalars (`const yA = computeAeroForce(...).force.y;`) into local consts IMMEDIATELY after each call. Do NOT hold the returned object handle and read `.force.y` later — both handles point at the same buffer, the second call has already overwritten the first, and your delta computation will silently return 0. Existing tests in `aerosurface.test.ts:858` and similar sites already follow this pattern with an explicit "Snapshot immediately after each call" comment; treat that comment as a binding convention for new tests. Recurring foot-gun — caught at WP14.9b P1.7 closed-form non-dim test (first version returned `dY30 = 0` due to buffer aliasing).
- **Drag-component test extraction at non-zero AoA — surface normal and airflow direction are NOT orthogonal.** When writing a test that isolates the drag component of an `AeroForceResult` at non-zero AoA, the naive approach of "project Δforce onto a single world axis" fails. With identity body quaternion: lift = `q·A·cl·normal_world` (along +Y for a wing); drag = `q·A·cd·airflow_dir_world` (along the airflow direction, e.g. `(0, vDown/|v|, vFwd/|v|)` for a descending flow). At AoA > 0 these are NOT orthogonal, so `force.y` contains both lift AND a drag component (`q·A·cd·airflow_y_hat`), and similarly `force.z` is drag-only only when airflow is purely along ±Z. Two correct approaches: (a) **project ΔF onto the airflow direction** to recover `q·A·Δcd` as a scalar (drag is purely along that direction), then orthogonal-to-airflow Δ ≈ 0 is a sanity check; (b) **solve a 2D linear system** for cl and cd from the baseline force (`F.y = q·A·(cl·1 + cd·airflow_y_hat)`, `F.z = q·A·(cl·0 + cd·airflow_z_hat)` with identity quat). Pattern (a) is simpler for closed-form sanity; pattern (b) recovers both coefficients. **Origin:** WP14.11.5 Phase 2 P2.2 closed-form D18 induced-drag test — initial test asserted `augLiftY ≈ baseLiftY` and failed at 887 vs 910 because Y-axis included drag's Y component (caught at first Vitest run; rewrote to airflow-projection pattern).

## Current Phase

**Phase 2 — Mission System MVP** (see `docs/product/roadmap.md`).

**Goal:** Add structured gameplay — the four mission types from the vision (free flight, waypoint, takeoff/landing, combat), each minimally playable, with mission-select + in-mission HUD.

**Exit criteria:** From the main screen a player can pick any of the four mission types, play it to completion (or failure), and return to mission select.

**Status (2026-05-25 evening, post-WP14.19 CLOSE — D14→D27 cascade end on Branch B-accept):** WP14.19 closed at ship commit `eafc91e`. arch.md Revision 2026-05-25 evening (D27, commit `b69d267`) landed the mission JSON spawn AS recalibration to V_trim=78 — the recency-bias close-out for the player-facing surface that the harness-shaped debugging discipline of D14-D26 missed. All 5 mission JSONs (`public/missions/phugoid-probe-{low,mid,high}.json`, `free-flight.json`, `waypoint-patrol.json`) updated from `linvel.z: -30` (WP14.5-era broken-integrator convention) to `linvel.z: -78` (V_trim derivation). Browser walkthroughs via Playwright MCP confirmed coherent V_trim flight across all 3 mission types: phugoid-probe-mid does textbook phugoid (alt 50→108→0 over 30s); free-flight at throttle=0 glides (alt 50→87/AS 78→52 over 8s); waypoint-patrol reaches WP1 in ~1.6s. Vitest 592/592 + Playwright e2e 15/15 + tsc both configs clean + build clean. Deployed-symmetric score -7,287 (24× past -300 numerical Branch A threshold; Branch B-accept range; numerical Branch A deferred to Phase 3 playtest or deliberate WP14.20 tune cycle). **16-SURFACE close at finalize.** CLAUDE.md Rule #9 amended at D27 (scope extended from "level-flight score-function fixtures" to "any spawn-into-level-flight initial condition"). Phase 2 mission content (WP15/WP16/WP17) UNBLOCKS. Critical path: `... → WP14.19(CLOSED) → WP15(NEXT, takeoff/landing) → WP16(combat) → WP17(four-mission polish) → ship`. **Earlier 2026-05-24 status, post-D21 architect cycle — score-function revision chosen; WP14.14b NEXT:** D21 architect cycle landed 2026-05-24 evening (arch.md Revision 2026-05-24 evening). Triggered by a post-WP14.14-finalize in-browser inspection that revealed even the cascade's empirical best CANNOT sustain 1 second of level flight at the phugoid-probe-mid throttle 0.15 entry conditions (pitch −73°, AS 16.5 m/s, nose-first dive at t≈1s — was NOT captured in any numerical proxy). Independent plan-time derivation per CLAUDE.md Rule #5 surfaced the structural blind spot: `targetAirspeed: {low:25, mid:30, high:40}` was ~2× lower than the airframe's L=W equilibrium AS (derived `{low:45, mid:60, high:85}`) — the score function has been measuring deviation from the wrong target for the entire D14/D17/D18/D19/D20 cascade. D21 picks score-function revision singularly: add criterion 0 "level-flight-maintenance" probe (1s window, alt drop ≤20m, AS ≥10 m/s) + per-regime target_AS recalibration to derived equilibrium + `AS_ENVELOPE` 30→25 tighten. arch.md adds Rule #7 ("diagnostic infrastructure precedes mechanism interpretation") and Rule #8 ("score function targets are physical quantities; derive them, do not guess"). WBS additions: **WP14.14b** (D21 implementation, tooling-only score.ts + Vitest, size S 2-3h) and **WP14.15** (re-tune at WP14.13 widened bounds under new score function, size S 2-4h). Bound-widening REMOVED from D21+ menus per `feedback_optimizer_bounds_are_floor.md` "Failure mode at second widening". Option-c REMOVED (still ships under wrong score function). Fifth-mechanism deferred (dive at t=1s is negative-α nose-down, not positive-α stall). SURFACE-2026-05-24-04 transitions to **partial** at D21 cycle landing; full close at WP14.15 Branch A with 10 transitively-blocked chain SURFACEs (11 total). Production `aircraft.json` UNTOUCHED. Critical path: `... → WP14.14(ESCALATED) → D21 arch cycle(DONE) → WP14.14b(NEXT) → WP14.15 → WP16 → WP17 → ...`. **Confidence calibration:** P(WP14.14b Branch A) ≈ 95% (tooling-only); P(WP14.15 Branch A | WP14.14b Branch A) ≈ 40-55%. **Open SURFACE items:** **SURFACE-2026-05-24-04 (high — partial, full close pending WP14.15 Branch A)**; **SURFACE-2026-05-24-05 (medium — parity-diff under `--link`, diagnostic, non-blocking)**; SURFACE-24-03 + SURFACE-24-01 + SURFACE-23-01 + the chain of 7 transitively-blocked (-17-03, -17-01, -16-04, -16-01, -12-03, -12-01, -11-04) all resolved-by-cascade-but-partial; transitively close on WP14.15 Branch A; SURFACE-24-02 (low — axis-naming docs errata); SURFACE-17-02 (medium — arch.md D17 cross-product order errata, non-blocking); SURFACE-11-02 (medium); SURFACE-16-03 / SURFACE-16-02 / SURFACE-12-02 (low); SURFACE-04-19-01 (Phase 3 — bundle).

**Earlier status (2026-05-24, post-WP14.14 ESCALATED — D20 cascade refuted; D21 cycle NEXT):** Phase 1 closed (WP1–WP9.6). Phase 2 progress: WP10/10.5/11/12/13/14 shipped 2026-05-12; D14 cascade landed (WP14.6/7/8 + WP14.5-retry ✓). D17+D16+D18+D19+D20 cascade mechanism layers all shipped: **WP14.9b (D17 β4 non-dim) ✓ 2026-05-17 commit `0df9a07`**; **WP14.10 (D16 β5 non-dim) ✓ 2026-05-23 commit `27324aa`**; **WP14.11.5 (D18 drag polar) ✓ 2026-05-23 commit `a93c277`**; **WP14.13 (D19 widened-bounds tune) ESCALATED 2026-05-24 commit `e5933f4`**; tooling fix **`tune-cli-link-flag` ✓ commit `58badfe`** (ratio 0.67× → 1.0×, deployed score ~14× better than WP14.12 at WP14.13 widened bounds: −6.7M); **D20 architect cycle landed 2026-05-24 PM** (arch.md Revision 2026-05-24 PM) chose further-bound-widening + `--link`; **WP14.14 ESCALATED 2026-05-24 commit `2ba50d6`** — D20 further-bound-widening REFUTED at linked search; all 4 restarts saturated NaN-floor; deployed score −2.003e9 = REGRESSION ~300× vs linked-smoke at WP14.13 bounds. Empirical optimum sits near WP14.13 widened bounds (k_wing≈1.29, area≈7.9), not the 2×-further bounds D20 hypothesized. β5 collapsed under heavy drag (clAlphaDot_wing 0.44→0.00 AT FLOOR) — physically coherent signal that drag-dominated dissipation makes AoA-rate damping redundant. Filed **SURFACE-2026-05-24-04 (high — D21 cycle driver)** with option-c "accept current state" promoted to primary candidate per `feedback_retune_attempt_budget.md` attempt-3 logic; D19d (envelope re-calibration) secondary; fifth-mechanism candidates tertiary; further-widening REMOVED from menu. Also filed **SURFACE-2026-05-24-05 (medium — parity-diff under `--link`: optimizer score 1.50× more negative than score-deployed.mjs re-score at bit-identical params; harness deterministic per md5 stability so divergence is in eval-path)**. Phase 2 mission content (WP15/16/17) remains paused — pause line moves from "post-WP14.14 branch A" to "post-D21 branch A". Critical path: `... → tune-cli-link-flag(DONE) → WP14.14(ESCALATED) → D21 architect cycle (NEXT) → WP14.15-or-equivalent → WP16 → WP17 → ...`. **D21 menu (ranked):** (1) option-c — ship linked-smoke globalBest at WP14.13 widened bounds (deployed −6.7M) as Phase 2 starting point with relaxed envelope, defer flyable-feel to Phase 3 playtest per `feedback_operator_as_external.md`; (2) D19d — score-envelope re-calibration to formalize "deployable at relaxed envelope" (tooling-only change, S-M); (3) fifth-mechanism candidates (bank-angle-coupled drag / stall-region CD steepening / dα/dt smoothing) — defer until (1) + (2) tried; (4) D19b Ixx-anchored inertia — still rejected (causally weak). **Open SURFACE items:** **SURFACE-2026-05-24-04 (high — D21 cycle driver, supersedes -24-03 as actionable arch-revision driver)**; **SURFACE-2026-05-24-05 (medium — parity-diff under `--link`, diagnostic)**; SURFACE-24-03 + SURFACE-24-01 + SURFACE-23-01 + the chain of 7 transitively-blocked (-17-03, -17-01, -16-04, -16-01, -12-03, -12-01, -11-04) all resolved-by-cascade-but-partial; transitively close on the D21 chosen path's success; SURFACE-24-02 (low — axis-naming docs errata); SURFACE-17-02 (medium — arch.md D17 cross-product order errata, non-blocking); SURFACE-11-02 (medium); SURFACE-16-03 / SURFACE-16-02 / SURFACE-12-02 (low); SURFACE-04-19-01 (Phase 3 — bundle).

## Key Decisions

See `docs/product/arch.md` for the full list and rationale. Highlights:

- **D1: Fixed-timestep physics.** Accumulator pattern. Non-negotiable for stable aerodynamics.
- **D2: Aerosurface primitive** — per-surface lift/drag (Khan & Nahon 2015), not a monolithic flight model. Produces correct-feeling dynamics (banking-to-turn, stall, adverse yaw) without hand-coded rules.
- **D3: Flight-model constants in JSON.** Enables rapid tuning — the single biggest feel risk (R2 in research).
- **D4: Flat terrain in Phase 1.** Flat plane + skybox + 2–3 landmarks. Heightmap is a Phase 3 polish swap via the `terrain.ts` interface.
- **D6: No ECS in Phase 1.** Single aircraft, simple world. Reconsider at Phase 2 if entities multiply.
- **D9: Static deploy, backend-less.** Aligns with "no-install" vision principle.
- **D10/D11/D12/D13 (Phase 1→2 boundary):**
  - **D10:** Per-surface `incidenceRad` (β1) is the trim mechanism (shipped WP6.5).
  - **D11:** Missions are declarative JSON files in `public/missions/`; combat (WP16) registers an optional `scriptHook` for AI enemy behavior. Other three mission types are declarative-pure.
  - **D12:** HUD is a DOM overlay (`src/hud/dom-hud.ts`) implementing a `HUD` interface; waypoint arrows project world coords via `THREE.Vector3.project()`. Three.js ortho is the Phase 3 swap point.
  - **D13:** Per-surface `clAlphaDot` (β5) is the phugoid-damping mechanism (lands in WP10.5; default 0; tuning per Phase 2 mission as needed).
- **D14/D17/D16 (Phase 2 physics cascade):**
  - **D14:** Headless Node-side harness + Nelder-Mead optimizer is the canonical tuning methodology for physics-mechanism coefficients in `aircraft.json` (`npm run tune`). Replaces manual lil-gui + Playwright-probe loops; codified as CLAUDE.md Rule #3.
  - **D17:** β4 pitch-rate damping uses textbook non-dimensional form `cl += clQ · ω_along_dampAxis · c̄ / (2·max(V, V_REF))` (shipped WP14.9b). Supersedes earlier D15 (refuted at verify-self).
  - **D16:** β5 AoA-rate damping uses textbook non-dimensional form `cl += clAlphaDot · dα/dt · c̄ / (2·max(V, V_REF))` (shipped WP14.10). Structurally parallel to D17.
- **D18 (2026-05-23, third mechanism layer):** Drag polar revision — per-surface optional `inducedDragK?` (CL² induced-drag coupling) + top-level optional `fuselageDrag?: {cd0, area}` (body-level parasitic drag at body origin). Lands in WP14.11.5; tuned in WP14.12. Closes-by-implementation SURFACE-2026-05-23-01 at WP14.12 branch A.

## Key Risks

See `docs/product/research.md` for full list. The two that drive the critical path:

- **R2 — Flight-feel tuning is iterative.** Addressed by D3 (JSON config + lil-gui). Budget real time on WP7.
- **R6 — Combat mission scope creep.** Keep WP16 minimal: one AI, one weapon, simplified hit detection. Revisit if Phase 2 slips.
