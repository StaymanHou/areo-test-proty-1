# Changelog

## 2026-06-13

- **Feature shipped:** WP22 deploy + share — Phase 3 deploy milestone closed at ship commit `f8d804b`. Web flight sim now live at https://staymanhou.github.io/areo-test-proty-1/. Single-phase XS config-only feature: `.nvmrc` (Node 22 pin) + `vite.config.ts` (`base: '/areo-test-proty-1/'`) + `public/.nojekyll` + `.github/workflows/deploy.yml` (`actions/deploy-pages@v4` flow with build → upload-artifact → deploy split, `pages: write` + `id-token: write` perms, `concurrency: pages`). Mid-feature host pivot Cloudflare Pages → GitHub Pages (operator 2FA lockout on dormant Cloudflare account). Mid-build BASE_URL discovery: 3 runtime fetch sites (`src/mission/loader.ts:16`, `:28`, `src/engine/scripted-input.ts:configNameToPath`) used hardcoded leading-`/` paths that 404'd under sub-path deploy; resolved inline via `import.meta.env.BASE_URL` prepend (SURFACE-2026-06-13-01, resolved same commit). First GH Actions deploy failed at `configure-pages@v5` with "Get Pages site failed" (Pages-enable propagation chicken-and-egg); resolved by operator job re-run after Settings → Pages → Source = GitHub Actions toggle. Verify-self via Playwright MCP against the live URL: 8/8 observable outcomes PASS (HEAD `/` 200 + text/html + GitHub headers, HEAD assets 200 + application/javascript, splash clears, 4 mission tiles render, debug-gate off without `?debug=true`, ON with it, deep-link `?mission=free-flight&debug=true` yields finite altitude/airspeed). Verify-human PASS by operator playtest. Code-quality review: 0 CRITICAL, 3 MAJOR auto-backlogged (vite base hardcoded, no CI test gate, unnecessary 404 fallback step) + 4 MINOR auto-backlogged. Final gates: Vitest 793/793 + tsc + production build clean pre-ship.
- **Backlog resolved:** SURFACE-2026-06-13-01 — closed inline at WP22 ship commit `f8d804b`. Anti-pattern (leading-`/` absolute fetch URLs in `src/`) documented; followup task workflow optional to add a lint rule or CONVENTIONS.md note.
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-surface01-stale-status — closed at WP22 finalize sweep (stale "commit pending" wording on SURFACE-2026-06-13-01 updated to reference ship SHA).
- **Milestone:** WP22 deploy + share — Phase 3 milestone "Deploy to a public URL, shareable link" CHECKED in `docs/product/roadmap.md`. Phase 3 critical path collapses to `WP22(DONE) → WP23 playtest → v1 ship` — only WP23 (3–5 casual-player playtest sweep) remains for v1 ship.
- **Feature shipped:** WP24 aircraft selection UI — ship commit `f3daffb`. Promotes the deep-link-only MiG-15 jet (WP14.21) into a player-facing choice on the mission-select screen via a "Trainer (Cessna) / Jet (MiG-15)" segmented picker; localStorage persists the pick; mission tiles label their pinned-config overrides inline ("Combat [MiG-15]"); boot-time resolution precedence is URL `?config=` > preloaded-mission `config?` > localStorage > default; mid-session airframe change reloads via `?mission=<id>` deep-link so the next boot honors the pick. Three phases (data module + boot wiring + picker UI); 30 new test cases (21 Vitest aircraft-options + 9 Vitest select picker + 6 Playwright e2e); 1 codify-time test bug caught and fixed (Playwright `addInitScript` clearing localStorage across reload). Negotiates `roadmap.md:62` v1 "multiple aircraft selection" exclusion — exclusion struck for Trainer + Jet pairing; Aerobatic stays a test fixture pending SURFACE-2026-06-06-06 Phase B feel-tune. Mode 4 full-autopilot, zero back-loops, ran end-to-end in one orchestrator session. Final gates: Vitest 823/823 + Playwright e2e 53/53 + tsc + production build clean.
- **Backlog resolved:** SURFACE-2026-06-06-06 Phase C — closed at WP24 ship commit `f3daffb`. Mission-select airframe picker shipped; vision-constraint exclusion negotiated. Phase A previously closed at WP14.20; Phase B (Aerobatic feel-tune) remains deferred per WP24 spec Q3 resolution.

## 2026-06-07

- **Feature shipped:** WP17 Phase 2 verification — Phase 2 exit gate closed at ship commit `88054eb`. Test-only WP: `tests/e2e/phase2-integration.spec.ts` adds 8 new tests (4 mission-select→click→play→terminal-or-Escape-abort→return for each of the four mission types, asserting outcome banner contents where applicable + clean console / no-NaN discipline; 4 FPS sanity probes via rAF-injected per-frame timer measuring median frame budget < 33ms (≥30 FPS) for each mission — local Chromium reads 18-21ms ≈48-55 FPS). `tests/e2e/phugoid-probe.spec.ts` tightened: per-probe spawn-relative `maxAbsAltDelta` + `maxAbsPitchDelta` gates (low=100m/27°, mid=100m/27°, high=250m/30° — sized to ≥1.5× the WP17-baseline maxima of low 49.7m/21.1°, mid 58.6m/18.0°, high 141.5m/14.4°; the existing absolute envelopes 5000m/200m·s/180° retained as outer guards). Zero `src/` changes — pure test addition. Final gates: Vitest 700/700 + Playwright e2e 35/35 + tsc (both configs) + build all clean. Workflow ran end-to-end through 3 phases under full-autopilot drive mode with zero back-loops; CLI Observable outcomes collapsed verify-auto/self/codify into a single gate per phase (test-only WP with no integration boundary).
- **Backlog resolved:** SURFACE-2026-05-12-02 — closed retroactively. Resolution happened 2026-06-06 across two commits (`14975f4` pruned phugoid-probe missions from the home-menu manifest; SURFACE-2026-06-06-07 removed the manifest pre-check in `src/main.ts:431-439` so deep-link reachability no longer requires manifest membership). Backlog entry hadn't been updated; WP17 verified the closure (3 phugoid-probe e2e tests still GREEN under deep-link-only access).
- **Milestone:** WP17 (Phase 2 verification) — **Phase 2 exit gate CLOSED.** All Phase 2 exit-criteria milestones in `docs/product/roadmap.md` are `[x]`: mission framework, free-flight, waypoint-patrol, takeoff/landing, combat, mission-select + HUD. From the main screen a player can pick any of the four mission types, play to win/lose/abort, and return to mission-select. Phase 3 (v1 ship — WP18 onboarding + WP19 audio + WP20 visual polish → WP21 cross-browser → WP22 deploy → WP23 playtesting) unblocks.
- **Feature shipped:** WP16 combat mission — 4th Phase 2 mission ships at commit `5825f09`. Combat-ai hook (`src/mission/hooks/combat-ai.ts`) implements the full WP16 stack per arch.md D11: 32-slot allocation-free player-projectile pool (Space-fire @ 600 m/s × 5 ROF × 1500m range), 16-slot return-fire pool (target shoots back @ 400 m/s × 2 ROF with 0.3s lead aim), per-tick AABB hit detection (projectile vs target, return-fire vs player), CombatState module-level singleton (allocation-free per-mission reset; cleaner than HookState bag for render-loop + window.__combat debug-accessor reuse). MissionRunner extended with hook-driven `setHookFailFlag(reason?)` API for fail-by-HP loss path. DomHud extended with `setCombatHP(playerHp, targetHp)` rendering 2 colored HP rows under the centered objective; `(null, null)` hides for non-combat missions. Main.ts wires the failSignal callback + hazard-orange target BoxGeometry (color/Z-tilt swap on destruction) + dark-red return-fire mesh pool. `combat.json` uses `config: "mig15"` (WP14.20 plumbing) — the Cessna couldn't aim at ground targets via chase-camera so per-mission airframe swap was the cleanest tuning lever. Phase 5 P5.2 retuned 9 constants (TARGET_HP=3, PLAYER_HP=6, RETURN_FIRE_SPEED=400, ROF=2, LEAD=0.3, target geometry, etc.) live-verified both win + loss paths reachable via scripted-input. `tests/e2e/combat.spec.ts` ships 2 e2e probes (win 5.2s, loss 6.4s); 22 new unit cases in combat-ai.test.ts + 5 runner.test.ts + 5 dom-hud.test.ts. F9b back-loop mid-Phase 4 added 3 debug accessors (`getReturnFireSnapshot`, `getReturnFireCooldown`, `getPlayerHp`) to `window.__combat` for verify-self observability. Vitest 700/700 + Playwright e2e 27/27 + tsc clean + build clean.
- **Backlog resolved:** SURFACE-2026-06-07-01 — filed AND mitigated-in-WP16 in the same feature; documented the target-position spec deviation (y=0 + halfExtents.y=8 vs spec's y=2 + halfExtents.y=2) for Phase 3 visual polish re-validation.
- **Milestone:** WP16 (Combat mission) — Phase 2 exit-criteria milestone "Combat mission: basic weapons, one AI enemy (air or ground), damage model" ADVANCES from `[ ]` to `[x]`. All four Phase 2 mission types now playable from the main screen; only WP17 (Phase 2 verification) remains for Phase 2 exit gate.
- **Feature shipped:** WP18 onboarding pass — first-load UX closes the vision-stated "new player flying within 30 seconds" claim via three additions at ship commit `63e07fa`. (1) Inline splash overlay in `index.html` paints on first frame before the JS bundle parses; `main.ts` updates stage labels at each await ("Loading physics…" → "Loading scene…" → "Ready") then detaches the splash when mission-select renders (menu path) or in `startMission` (deep-link path). (2) `src/hud/key-hints.ts` `KeyHintsOverlay` shows control bindings (Pitch W/S, Roll A/D, Yaw Q/E, Throttle Shift/Ctrl, Camera V, Abort Esc; combat adds Fire/Space) for ~20s after every mission start — opaque ≤10s, linear fade 10→20s, detached at 21s; fade timer ticks from `onPhysics` (fixed 60Hz) so the window is deterministic under the `?script=` harness AND paused missions don't burn the timer; `pointer-events: none` so the overlay never blocks canvas clicks; re-shown on every fresh mission entry. (3) `tests/e2e/time-to-airborne.spec.ts` codifies the 30s gate — measures URL-open → click free-flight → AS>40 + every numeric state field finite ≤ 30s; measured 1.1s on dev-server cold-load (27× safety margin). Final gates: Vitest 708/708 (700 + 8 new key-hints unit tests) + Playwright e2e 42/42 (37 baseline + 2 splash + 4 key-hints + 1 time-to-airborne) + tsc clean both configs + build clean. Verify-self Phase 2 triggered the Subagent Re-Verification Heuristic exactly once (subagent reported a BLOCKING FAIL on per-mission re-show that was mechanically implied by sibling PASSes; orchestrator-side re-run of the e2e gate × 3 confirmed PASS) — no back-loop required. WP17 retrospect's "CLI outcome IS the gate" pattern reused for Phase 3 (test-only WP collapses verify loop into a single check). Spec-vs-impl deviation logged: spec listed Arrow keys for pitch, default keymap uses WASD post-SURFACE-2026-06-06-01 — updated hint labels to match. Workflow ran end-to-end through 3 phases under full-autopilot drive mode with zero back-loops; 1 cosmetic SURFACE filed (-07-02 below).
- **Backlog resolved:** none — WP18 added a new SURFACE (-07-02) rather than closing existing items.
- **Milestone:** WP18 (Onboarding pass) — Phase 3 milestone "Onboarding: new player is flying within 30 seconds — no tutorial, in-world prompts only" ADVANCES from `[ ]` to `[x]`. WP19 (audio, S) + WP20 (visual polish, L) remain for Phase 3 exit; both unblocked.
- **Feature shipped:** WP19 audio — the v1 audio surface ships at commit `7467f10`. Phase 1 adds the AudioEngine singleton + continuous loops: `src/audio/engine-loop.ts` synthesizes a sawtooth oscillator mapped from throttle 0→1 to frequency 90→340 Hz (gain 0→0.2, 50ms `linearRampToValueAtTime` smoothing) and `src/audio/wind.ts` plays a procedurally-generated 1s pink-ish noise buffer through a `BiquadFilterNode` lowpass (cutoff 200→2000 Hz from airspeed 10→150) + gain envelope (fully silent below AS=10 m/s, ramps to 0.15 at 150 m/s). Phase 2 adds three one-shot synthesizers in `src/audio/sfx.ts`: `playFire` (200ms sawtooth burst + lowpass sweep), `playImpact` (150ms filtered-noise burst), `playCrash` (800ms low-saw + noise envelope); a 16-slot ring buffer records each trigger for verify-self introspection via `window.__audio.getRecentOneShots()`. The AudioContext is lazily created in `AudioEngine.start()` and resumed on first user gesture (mission-select click + deep-link entry path) per Safari/iOS autoplay restrictions (research R4). Wiring: `combat-ai.ts` extended with `onFire?`/`onImpact?` callbacks in `registerCombatAi` (fired from `tryFireGun` + `checkProjectileHits`); `main.ts` statusChange handler calls `triggerCrash()` when `getFailReason() === 'crash'` (new `MissionRunner.getFailReason(): 'crash' | 'timeout' | 'out-of-bounds' | 'hook' | null` accessor; aborts return null so they stay silent). No external audio assets — all sound synthesized; ~3 KB module growth, no impact on the pre-known >500 kB bundle SURFACE-2026-04-19-01. Final gates: Vitest 741/741 GREEN (+20: 13 audio-engine + 7 sfx + 7 runner getFailReason) + Playwright e2e 47/47 GREEN (+5 audio cases: click-path resume, deep-link scripted throttle-up frequency response, production no-debug-accessor, combat fire trigger, combat impact trigger) + tsc clean both configs + build clean. Two test-triage entries during codify (warning-filter widening for headless WebGL noise + e2e wait-strategy shift to ring-buffer-polling for mission-ends-early); neither implicated implementation. Workflow ran end-to-end through 2 phases under full-autopilot drive mode with zero implementation back-loops.
- **Backlog resolved:** none — WP19 added a new SURFACE (-07-03 crash-trigger live-verification coverage gap, deferred to Phase 3 WP21/WP23 bundle) rather than closing existing items. The crash-trigger wiring is statically obvious and unit-tested at all three layers (runner sets `_failReason='crash'` at the y≤0 + |vY|>2 branch; AudioEngine.triggerCrash records the ring entry; main.ts conditional is a single `&&` chain). The aerodynamic damping at V_trim spawn keeps |vY| below the 2 m/s crash threshold on every scripted-dive recipe; resolving this physically requires either lowering CRASH_VSPEED_THRESHOLD (Phase 3 playtest decision) or introducing a test-only fixture (out of WP19 scope).
- **Milestone:** WP19 (Audio) — Phase 3 milestone "Audio: engine, wind, weapon, crash sounds" ADVANCES from `[ ]` to `[x]`. WP20 (visual polish, L) is the last Phase 3 content WP; WP21 cross-browser depends on it.
- **Feature shipped:** WP20 visual polish pass — the v1 visual surface ships at commit `28bc898`. Four-phase impl: (1) `src/world/skybox.ts` extended with `stampCloudRGBA`/`stampCloudsRGBA` + `applyHorizonHazeRGBA` + `mulberry32` deterministic RNG; default `faceSize` 256→512; 6 cloud blobs per side face. `src/world/scene.ts` HemisphereLight replaced with `DirectionalLight(0xffeecc, 1.0)` at `(200, 220, 60)` casting PCF soft shadows (1024² map, ±250m frustum, bias -0.0005) + `AmbientLight(0xb0c8e0, 0.45)` for cool sky-tinted fill. Aircraft + landmarks already had cast/receive set; placeholder mesh updated. (2) New module `src/aircraft/aircraft-mesh.ts` exports `buildAircraftMesh(config, variant)` + `inferAircraftVariant(config)` heuristic. Cessna variant: cylindrical fuselage (r=0.45 L=5, rotated to Z-axis) + tapered nose cone + blue centerline stripe + high-wing rectangular slabs (+0.55 above centerline) + visible wing struts + trapezoidal swept v-fin via Shape+ExtrudeGeometry. MiG-15 variant: shorter thicker cylindrical fuselage (r=0.7 L=4.4) + intake cone at nose + dorsal hump + 35°-swept wings (mirrored via scale.x) + swept h-stab (30°) + tall swept v-fin. `src/aircraft/rigidbody.ts` refactored to delegate; `AircraftCreateOptions.meshVariant?` added for explicit override; auto-infers from `config.thrust.maxN >= 20000` cutoff. (3) New module `src/world/particles.ts` — singleton pool of 256 particles, kind-tagged emit (`muzzle-flash` 8p × 0.15s warm-yellow; `impact` 16p × 0.4s hot-orange; `ground-dust` 24p × 0.8s brown-grey + half-gravity), CPU update with drag decay + per-kind gravity + age-based alpha fade, shared `Points` mesh with `PointsMaterial(vertexColors, AdditiveBlending)`. Combat-ai callback signature widened from `() => void` to `(pos: {x,y,z}) => void` — back-compat preserved (existing `() => {}` no-ops still type-check). `main.ts` wires particles mount + per-render-frame update (wall-clock dt via local `lastRenderMs`, capped at 0.1s) + emit at fire/impact/crash + `window.__particles` debug accessor + `particles.resetParticles()` on mission start. (4) Polish sweep — `src/hud/key-hints.ts` CSS re-anchored from `top: 5rem; right: 1rem` to `bottom: 1.5rem; left: 1rem` (closes SURFACE-2026-06-07-02); combat target visual decoupled from hit-detection AABB — collider unchanged at `halfExtents=(10,8,10)`, y=0; visual mesh changed from `BoxGeometry(20, 16, 20)` straddling y∈[-8,+8] to `BoxGeometry(20, 4, 20)` at `y=2` (closes SURFACE-2026-06-07-01). Also: removed dead duplicate `DirectionalLight` block from `main.ts:108-110` (pre-Phase-1 leftover that would have conflicted with the new scene.ts sun). Switched `PCFSoftShadowMap` → `PCFShadowMap` after one mid-workflow triage (audio.spec.ts e2e surfaced Three.js r170+ deprecation warning; high-confidence code regression, auto-fixed). Final gates: Vitest 793/793 GREEN (+52 net: 19 skybox cloud/haze/RNG + 2 scene-composition jsdom + 11 aircraft-mesh variants + 1 castShadow rigidbody + 18 particles + 2 callback-position combat-ai - 1 obsoleted by Phase 2 default-variant override) + Playwright e2e 47/47 GREEN (unchanged total post-triage-fix) + tsc clean both configs + build clean (bundle 2,852 → 2,890 kB, +38 kB net). Workflow ran end-to-end through 4 phases under full-autopilot drive mode with zero implementation back-loops; 1 verify-codify triage (PCFSoftShadowMap deprecation, classified high-confidence code regression, auto-fixed).
- **Backlog resolved:** SURFACE-2026-06-07-02 — closed by WP20 Phase 4 P4.1 (key-hints overlay re-anchored to bottom-left, clearing the lil-gui occlusion under `?debug=true`; also benefits production users by keeping hints out of the airspeed/altitude HUD zone).
- **Backlog resolved:** SURFACE-2026-06-07-01 — closed by WP20 Phase 4 P4.2 (combat target visual decoupled from hit-detection AABB; collider unchanged at gameplay-tuned dimensions, visual mesh now a 4m × 20m × 20m ground building sitting on terrain — the "ground target" silhouette the WP16 spec intended).
- **Milestone:** WP20 (Visual polish pass) — Phase 3 milestone "Visual polish pass: skybox, terrain textures, aircraft model, basic effects (contrails, explosions)" ADVANCES from `[ ]` to `[x]`. All three Phase-3 polish content WPs (WP18 onboarding + WP19 audio + WP20 visual) now closed; WP21 cross-browser QA unblocks as the last gate before WP22 deploy + WP23 playtest.

## 2026-06-06

- **Feature shipped:** controls-feel-pass — addressed operator's "controls aren't there yet" complaint post-WP14.19 via two single-knob changes: Phase 1 added a cubic-`x³` input curve to `src/aircraft/controls.ts` (default `stickCurve: 'cubic'`, opt-in `'linear'` for tests) softening small-input sensitivity by 75% while preserving full-deflection authority + ramp-rate independence, plus a `resetSticks()` helper for mission-start; Phase 2 capped `wings.maxDeflectionRad` to 5° (0.0873 rad) in `public/config/aircraft.json` to bring full-deflection terminal roll rate from ~550°/s (snap-roll) to ~179°/s (WWII-fighter / casual-gamer territory). Codified by new `src/aircraft/roll-rate.test.ts` (3 deterministic Vitest cases anchoring the ≤200°/s firm gate and aileron sign convention). Two commits: `9ef802e` (Phase 1) + `3727c34` (Phase 2 + Phase 1 curve iteration). Operator approved at verify-human ("all pass"). Mid-workflow lessons documented in WIP retrospect; 4 follow-up SURFACEs filed (06-01 WASD keymap, 06-02 pitch envelope IMMEDIATE-NEXT, 06-03 missing clP roll-damping arch mechanism, 06-04 scripted-input URL mode for reliable live feel verification).
- **Feature shipped:** scripted-input-harness — deterministic URL-script harness (`?script=hold:<Key>@<startSec>:<endSec>`) for in-browser physics observation, replacing Playwright `dispatchEvent` (OS-scheduler-jitter dependent) per SURFACE-2026-06-06-04. Synthesizes input directly at the `onPhysics` tick boundary; log buffer latches at first `isScriptComplete()` for byte-stable observation. Includes throttle override (`hold:Throttle=0.6@0:5.0`), airframe swap (`?config=aerobatic` loads `/config/aircraft-aerobatic.json`, path-traversal-defended). Ship commit `14975f4` (8 files, +918/-3): parser + runner modules with 23 Vitest cases, 4 e2e tests including byte-identical-rerun determinism gate (stable 6/6), project-local CLAUDE.md `### Browser-walkthrough discipline` subsection mandates harness for all future agent-side automated probes > 2s observation window + Quickstart cheatsheet. Verify-self 5/5 PASS at browser-walkthrough (maxPitch=55.28° under hold:ArrowUp; aerobatic terminal AS 124.98 vs default 81.89; throttle override observable at tick 31). Additional finalize bundle: pruned phugoid-probe-{low,mid,high} from `public/missions/index.json` home-menu (D14→D27 cascade closed; deep-link via `?mission=` still works for e2e tests).
- **Backlog resolved:** SURFACE-2026-06-06-04 — closed by scripted-input-harness ship commit `14975f4`.
- **Backlog resolved:** SURFACE-2026-06-06-02 — closed as **Path A (physically correct behavior, not a bug)**. The Cessna-class production airframe (mass=1000 kg, T_max=6000 N, T/W=0.61) physically cannot complete a backflip from cruise; matches real Cessna 172. The Step 0 jet-experiment + browser walkthrough via the new harness at `?config=aerobatic` (mass=500, T_max=12000, T/W=2.4) reached **+180° full inversion** under same script — confirming the simulation IS physically faithful when airframe parameters match an aerobatic-class plane. Aerobatic gameplay needs a second aircraft.json, deferred to Phase 3 multi-aircraft work per CLAUDE.md "Not goals for v1" exclusion. Project objective sharpened at this session ("verify technical feasibility of simulating a near-real physics flight model" — Rule #3 carve-out (b) scope clarified as VALUES not non-physical MECHANISMS). Code changes: `pitch-envelope.test.ts` backflip assertion flipped from `>+90°` to `<90° AND >30°` (codifies Cessna-class ceiling); `scripted-input-runner.ts` `pitch_deg` field fixed from Euler YXZ `.x` (gimbal-locks at ±90°) to `atan2(fwd.y, -fwd.z)` (full ±180° range, caught during Path A walkthrough); 3 diagnostic test files committed as audit-trail (pitch-envelope, pitch-envelope-stall-probe, pitch-envelope-aerobatic-probe). Full Vitest 634/634 GREEN for the first time this session.
- **Task closed:** `phugoid-probe-harness-refactor` — rewrote `tests/e2e/phugoid-probe.spec.ts` (3 throttle probes) to use the `?script=hold:Throttle=<v>@0:end` harness + `getScriptedLog()` per-tick iteration instead of 30 × `page.waitForTimeout(1000)` + `getState()` polling; now asserts NaN-free + altitude/AS/pitch envelope across all 3600 ticks of each ~60s flight rather than 30 sparse samples. Dogfooding the harness against existing tests surfaced a deep-link regression (the prior phugoid menu-prune commit `f77aa36` broke `?mission=<id>` for any id not in `public/missions/index.json`); fixed inline by removing the manifest pre-check in `src/main.ts` so deep-link delegates to `startMission`, which handles fetch failure gracefully via `errorForId`. Manifest now governs ONLY menu UI visibility, not deep-link reachability. Verify via `npm run test:e2e` (19/19 green); tsc both configs + build clean.
- **Backlog resolved:** SURFACE-2026-06-06-05 — closed by the `phugoid-probe-harness-refactor` task; phugoid-probe spec now dogfoods the scripted-input harness as designed.
- **Backlog resolved:** SURFACE-2026-06-06-07 — `?mission=<id>` deep-link regression (discovered + fixed inline during the same task); manifest pre-check in `src/main.ts` removed so the deep-link contract documented in the prior session-pause note is now actually honored.
- **Feature shipped:** `per-mission-airframe` (SURFACE-2026-06-06-06 Phase A — plumbing only) — adds optional `config?: string` field to mission JSON, threaded through `parseMission` (with path-traversal defense via `CONFIG_NAME_REGEX` reused from `engine/scripted-input.ts`) and `main.ts` boot path to drive per-mission `loadAircraftConfig`; precedence URL `?config=` > mission `config?` > default `aircraft.json`; deep-link-only fixture `public/missions/aerobatic-test.json`. Ship commit `bb1c242`. Phase B (full feel-tune of aerobatic airframe) + Phase C (aerobatic mission content) remain deferred per vision constraint `docs/product/roadmap.md:62` "multiple aircraft selection — v1 ships with one aircraft."
- **Backlog resolved:** SURFACE-2026-06-06-06 — Phase A CLOSED by the feature ship; Phase B + Phase C remain deferred (status: partial), to be re-promoted if multi-aircraft is negotiated into v1 or absorbed into WP16 combat.
- **Milestone:** WP14.20 (Per-mission airframe selection plumbing) — Phase 2 work package complete; Phase 2 exit-criteria milestone DOES NOT advance at this commit (WP15 takeoff/landing + WP16 combat still pending).
- **Feature shipped:** `jet-airframe` (WP14.21) — first non-default airframe consuming the WP14.20 per-mission `config?` plumbing. Ships `public/config/aircraft-mig15.json` (MiG-15-class: mass=3000 kg, T/W ≈ 1.0, V_trim ≈ 180 m/s, wing area 18 m², jet-wing `incidenceRad=0`, `maxDeflectionRad=0.175`) + deep-link-only `public/missions/jet-test.json` (NOT in `index.json`) + 2 e2e scenarios (`tests/e2e/jet-airframe.spec.ts`: terminal AS > 120 m/s + backflip pitch crosses ±90°) + 1 Vitest config-parse case. Damping coefficients inherited from D14→D27 cascade-tuned Cessna values per CLAUDE.md Rule #3 carve-out (b) — generalized cleanly to V_trim=180 (2.3× higher AS than Cessna) on first attempt; non-dim damping forms behaved as textbook predicted. Operator playtest verify-human PASS (load-bearing under full-autopilot per documented carve-out — `feedback_operator_as_external.md`). Ship commit `01674bf`. Phase 4 (feel-tune iteration) SKIPPED — conditional gate not triggered. **Vision-constraint negotiation:** Option α (deep-link-only fixture, NOT v1 home menu) singularly chosen at spec stage per `feedback_surface_or_means_or.md`; honors `docs/product/roadmap.md:62` v1 multi-aircraft exclusion while shipping the operator-queued IMMEDIATE-NEXT deliverable. AUXILIARY to Phase 2 exit criteria (the four v1 missions remain Cessna-default). SURFACE-2026-06-06-03 (clP roll-damping gap) did NOT fire at operator playtest at jet AS regime — added as a data point to that pending SURFACE.
- **Milestone:** WP14.21 (Jet airframe — MiG-15-class config + deep-link jet-test mission) — auxiliary Phase 2 work package complete; Phase 2 exit-criteria milestone DOES NOT advance at this commit (WP15 + WP16 still pending).
- **Feature shipped:** WP15 takeoff-landing-mission — fourth Phase 2 mission added at ship commit `3a2902c`. Mission JSON `public/missions/takeoff-landing.json` spawns at V_trim=78 m/s with throttle=1.0 over the +Z runway end; four ordered objectives (3 reach-waypoints + 1 touchdown using the existing `TouchdownObjective` AABB+vSpeed machinery, no new runner code, no schema change) form the take-off → climb → pattern → land arc. `tests/e2e/takeoff-landing.spec.ts` +2 (deep-link spawn-finite + scripted rotate-and-climb arc); `src/mission/parse.test.ts` +1; `public/missions/index.json` +1 entry. AC4 crash-vs-touchdown ordering preserved: `CRASH_VSPEED_THRESHOLD=2 m/s < maxVSpeed=4 m/s` → gentle landing wins, hard impact crashes. **Plan-time probe surfaced SURFACE-2026-06-06-09:** Cessna at T/W≈0.6 cannot take off from rest on 600m runway (~1.1 m/s² accel, would need ~70s + ~2700m to reach V_trim); spec's "parked-on-runway from rest" framing dropped at P1.1; mission adopted V_trim spawn convention matching all other v1 missions per CLAUDE.md Rule #9; true-takeoff-roll gameplay deferred to a future thrust-raising arch revision OR runway-extension WP. Verify-human (operator playtest of the full arc) deferred under full-autopilot per `feedback_operator_as_external.md` carve-out. Vitest 641/641 + Playwright e2e 25/25 + tsc both configs + build clean.
- **Backlog resolved:** SURFACE-2026-06-06-09 — filed AND mitigated-in-WP15 in the same feature; V_trim spawn convention adopted, true-takeoff-roll deferred.
- **Milestone:** WP15 (Takeoff/landing mission) — Phase 2 exit-criteria milestone "Takeoff/landing mission (airfield with runway, touchdown detection)" ADVANCES from `[ ]` to `[x]`. Phase 2 four-mission set now 4-of-4 implemented; only WP16 combat remains for Phase 2 exit criteria.
- **Task closed:** `wasd-keymap-rebind` — `DEFAULT_KEY_MAP.pitchUp` / `pitchDown` in `src/engine/input.ts` rebound from `ArrowUp` / `ArrowDown` to `KeyW` / `KeyS`, completing the WASD-as-unified-flight-stick keymap (roll already on `KeyA`/`KeyD`). 3 e2e specs updated from `hold:ArrowUp` → `hold:KeyW` (`takeoff-landing.spec.ts`, `jet-airframe.spec.ts`, `scripted-input.spec.ts`); `controls.test.ts` was indirection-safe via `DEFAULT_KEY_MAP.pitchUp` references. Arrows-as-alternates option deferred (would require `KeyMap` schema extension to array form). Vitest 641/641 + Playwright e2e 25/25 + tsc both configs + build clean.
- **Backlog resolved:** SURFACE-2026-06-06-01 — closed by the `wasd-keymap-rebind` task; default flight-stick keymap now matches modern flight-sim convention.
- **Task closed:** `arch-md-errata-bundle` — three independent prose-only fixes to `docs/product/arch.md` landed in one commit: (a) D17 dampAxis cross-product order — 5 substitutions `(position × restNormal)` → `(restNormal × position)` (lines 704, 721, 731, 774, 784 discovered during edit); line 731 v-stab Risk 3 math rewritten honestly to acknowledge dampAxis is anti-yaw under the corrected order (practically inert because v-stab clQ defaults to 0). (b) Iyy/Ixx misnaming at line 808 replaced with Y-up-convention-correct analysis (pitch is rotation about body X = Ixx; current Ixx=1500 is already 1.11× Cessna, not 2.2×). (c) NaN-penalty formula sign at line 409 flipped (`-` → `+`). Additionally cleaned 4 stale comment sites in `src/aircraft/physics-core/aerosurface.ts` (lines 35-38, 141-147, 206-213, 576-582) that referenced the erratum; lines 576-582 carried a real sign-analysis bug (wrong cross-product AND cancelling-sign-error in the reasoning chain — got to the right damping conclusion via two wrongs that cancelled), now corrected. Code behavior unchanged. Vitest 641/641 (after a known SURFACE-2026-05-16-02 perf-flake on the parallel-load first run) + tsc both configs + build clean.
- **Backlog resolved:** SURFACE-2026-05-17-02 — arch.md D17 dampAxis cross-product order corrected to `(normal × position)`; matches what the code has always computed.
- **Backlog resolved:** SURFACE-2026-05-24-02 — arch.md Iyy/Ixx axis-naming error at line 808 corrected; pitch-axis inertia is Ixx in this Y-up convention.
- **Backlog resolved:** SURFACE-2026-05-16-03 — arch.md §D14.4 NaN-penalty formula sign typo fixed (`-1e9 - tick` → `-1e9 + tick`); matches the intent-correct implementation in `tools/tune/score.ts` already shipped.
- **Task closed:** `perf-flake-isolate` — moved load-flaky wall-clock perf assertion out of the default Vitest suite to resolve SURFACE-2026-05-16-02 (4 consecutive flakes this session under various parallel-load triggers). Picked option (b) per single-knob discipline: extracted the 1000-call `applyForces` perf test from `src/aircraft/physics-core/flightmodel.test.ts:357-369` into standalone `src/aircraft/physics-core/flightmodel.perf.test.ts` (duplicating `baselineRaw()` + `RAPIER.init` so the new file is self-contained); added `**/*.perf.test.ts` to `vitest.config.ts` `exclude` so default `npm run test` skips it (640/640 GREEN, was 641); created `vitest.perf.config.ts` with `include: ['**/*.perf.test.ts']` and added `npm run test:perf` script via `--config` flag. Empirical pivot at T4 act-time: first plan named `vitest run <path>` as the perf script body, but vitest 4.1.4's positional file-path filters still respect `exclude` (verified: returned "No test files found"), so the dedicated `--config` route is the correct invocation-flipped shape. Perf suite runs in 33ms isolation; regression signal preserved without flake tax on codify cycles. tsc both configs + build clean.
- **Backlog resolved:** SURFACE-2026-05-16-02 — perf-assertion flake eliminated via the perf-only invocation split; codify-cycle tax (4 consecutive triggers this session) closed.
- **Task closed:** `arch-wbs-size-guard-archive` — archived historical D-cycle content from `docs/product/arch.md` (2645→144 lines, 18× smaller) and Phase 1 + Phase 2 completed WPs from `docs/product/wbs.md` (1059→141 lines, 7.5× smaller) into two cycle-scoped directories (`docs/product/archive/phase-1-flight-poc/` + `docs/product/archive/phase-2-physics-cascade/`). Both live docs now clear the 300-line entry-skill size guard with margin. Picked shape (a) per single-knob discipline (cycle-archive convention matches `/product-finalize`); rejected shape (b) split into `arch-current.md` + `arch-history.md`. Mid-act discovery: wbs.md tail (Dependency map + Session Pause notes + WP-shipped narrative, lines 834-1059) was missed by plan's "Phase 2 completed WPs" scope; appended to existing Phase 2 archive rather than back-looping. Used `awk` for atomic large-file line-range extraction + Write for wholesale live-doc rewrites (Edit would have required dozens of `old_string`/`new_string` pairs for a structural ~95% replacement). All content preserved verbatim; archives total 3521 lines. Tests 640/640 GREEN unaffected; no code touched.
- **Backlog resolved:** SURFACE-2026-06-06-08 — arch.md + wbs.md size-guard bloat closed via cycle-scoped archival; both files now well under 300 lines.
- **Task closed:** `roll-damping-investigation` — pre-arch investigation spike (per `feedback_investigation_before_architect.md`) on SURFACE-2026-06-06-03 ("airframe has no roll-damping mechanism"). Plan-time CLAUDE.md Rule #5 derivation showed the SURFACE prose was structurally wrong: D17's existing `clQ + dampAxis` IS the β6 roll-damping mechanism for wings (because `dampAxis = (restNormal × position).normalized()` evaluates to ±Z = roll axis when `position.x ≠ 0` and `normal = +Y`). Production `aircraft.json` already ships `clQ_wing = 1.83`. Vitest investigation spec (`src/aircraft/roll-damping-investigation.test.ts`, 3 cases, 28ms total) measured sustained roll at V_trim full-aileron under production config: **179.3°/s at clQ_wing=1.83**, already UNDER the 200°/s firm gate. Three-hypothesis rule-out chain (sign / reduced-frequency length / undertuned) all refuted as actionable: (c) clQ correctly damps (clQ=10 → 137.6°/s, 28% reduction vs clQ=0 control at 192.3°/s; isAntiDamping=false); (b) wingspan would give 8× larger damping but the chord-based formula already meets the gate so a D28 cycle just for textbook correctness isn't cost-justified; (a) sweep {1.83→20} shows first-clQ-meeting-gate = 1.83 = production. Root cause of SURFACE prose mismatch: the "550°/s terminal" claim was from a pre-Phase-2-cap state (`maxDeflectionRad: 25°` on wings) that no longer exists; the Phase 2 cap to 5° already brings sustained roll into envelope. **No D28 architect cycle. No `aircraft.json` change. No `arch.md` change. No `aerosurface.ts` change.** Investigation spec persists as diagnostic codification. Vitest 643/643 GREEN (640 + 3 new). Second observation of "SURFACE prose lags actual state" pattern this week (after `arch-md-errata-bundle`'s 5th-line discovery).
- **Backlog resolved:** SURFACE-2026-06-06-03 — clP gap closed by investigation finding (existing D17 mechanism already adequate at production tune; 179.3°/s sustained < 200°/s firm gate); no architectural action required.

## 2026-05-25

- **Task closed:** D24 architect cycle — arch.md Revision 2026-05-25 written + WBS updated + 13-SURFACE cascade chain walked back at `workflow/backlog.md` (each marked `resolved-by-cascade-walk-back-pending-WP14.19-Branch-A` with citation to commit `46f9b42` / SURFACE-2026-05-24-09) + WP14.18 archived as ESCALATED-superseded-by-integrator-fix + CLAUDE.md adds Rule #9 (initial-condition-equilibrium-consistency for level-flight score-function fixtures); D24 chose branch (b) "adjust spawn AS in `tests/parity-fixtures.ts`" per CLAUDE.md Rule #5 independent derivation (post-fix L=W equilibrium AS is low/mid/high ≈ 45/78/128 m/s; current spawn AS=30 m/s is well below mid-equilibrium 78; criterion 0 was measuring "acceleration to equilibrium from below" not "stability around equilibrium"); branches (a) widen damping bounds + (c) tweak aircraft.json REMOVED from menu (the former confounded by upstream spawn-AS mismatch, the latter wrong direction); WP14.19 added (NEXT) as single-WP D24 implementation; production `aircraft.json` UNTOUCHED.
- **Task closed:** D25 architect cycle — arch.md Revision 2026-05-25 afternoon (D25) written + WBS updated + CLAUDE.md Rule #9 AMENDED to remove D24's erroneous "at the fixture's throttle" qualifier; D25 driven by SURFACE-2026-05-25-01 (WP14.19 Phase 2 ESCALATE Branch B) and chose D25-ζ singularly per CLAUDE.md Rule #5 independent derivation — L=W trim AS at a fixed-α airframe is throttle-independent (V_trim = √(2·W/(ρ·S·CL_at_trim_α)) ≈ 78 m/s for this airframe); throttle determines T=D balance AT V_trim (climb/cruise/descent) not V_trim itself; D24's per-throttle T=D-derived spawn AS {45, 78, 128} was structurally wrong on 2 of 3 regimes (low spawn AS=45 had lift=0.33×W → crashed in 5s; high spawn AS=128 had lift=2.7×W → rocket-climbed to alt=636m with pitch +87°); D25-ζ corrects to uniform spawn AS=78 + revert score.ts to all-level-cruise mode + targetAirspeed={78,78,78} + AS_ENVELOPE 25→30; WP14.19 back-loops to Phase 1 re-edit; production `aircraft.json` UNTOUCHED through D25.
- **Task closed:** D26 architect cycle — arch.md Revision 2026-05-25 late afternoon (D26, commit `9b9aa4e`) added per-regime `altEnvelope: {low: 100, mid: 50, high: 200}` to `tools/tune/score.ts` `ScoreEnvelopes` interface (additive, optional, scalar `ALT_ENVELOPE` fallback for back-compat); D26 driven by SURFACE-2026-05-25-02 (WP14.19 Phase 2 D25-ζ re-run ESCALATE Branch B with all 3 regimes finite + c0 PASS but deployed -14,881 dominated by high-regime alt+115m climb + mid/low ground-impact tails); independent CLAUDE.md Rule #5 derivation REFUTED SURFACE-25-02's D26-α primary candidate (widen `inducedDragK_wing` 0.3→0.6: more drag bleeds AS faster → sooner ground impact); D26-β chosen singularly — per-throttle alt behavior is legitimately regime-dependent (T>>D climbs, T<<D descends); cascade meta-lesson surfaced (single observation): score-function dimensions are not uniformly throttle-parameterized (AS dim throttle-invariant per D25; ALT dim regime-dependent per D26).
- **Task closed:** D27 architect cycle — arch.md Revision 2026-05-25 evening (D27, commit `b69d267`) recency-bias close-out for the player-facing surface; CLAUDE.md Rule #9 scope extended from "level-flight score-function fixtures" to "any spawn-into-level-flight initial condition" covering both `tests/parity-fixtures.ts` (D24/D25 scope) AND `public/missions/*.json` (D27 scope); D27 driven by WP14.19 Phase 4 P4.4 browser walkthrough discovery that ALL 5 mission JSONs (`phugoid-probe-{low,mid,high}` + `free-flight` + `waypoint-patrol`) still spawned at `linvel.z: -30` (WP14.5-era broken-integrator convention) despite the cascade updating `tests/parity-fixtures.ts` at D24+D25; airframe drops to ground in ~5s at browser despite coherent harness trajectory; D27-γ chosen singularly per Rule #5 (update all 5 mission JSONs to `linvel.z: -78`); cascade meta-lesson surfaced (single observation): the cascade's debugging discipline was harness-shaped, not user-shaped — `public/missions/*.json` were never grep'd as a V_trim derivation consumer.
- **Feature shipped:** WP14.19 — D14→D27 cascade close (ship commit `eafc91e`). The 11-day physics-tuning cascade is structurally + behaviorally closed: integrator fix at `46f9b42` + spawn AS uniformization to V_trim=78 in parity-fixtures (D24/D25) + per-regime alt envelopes (D26-β) + mission JSON spawn AS recalibration (D27). Production `public/config/aircraft.json` ships WP14.19 D26-β globalBest knobs. Browser walkthroughs at `localhost:5173/?mission=<id>&debug=true` confirm coherent V_trim flight across all 3 mission types (phugoid-probe-mid: alt 50→108→0 over 30s; free-flight: alt 50→87/AS 78→52 gliding; waypoint-patrol: reaches WP1 in ~1.6s). Vitest 592/592 + Playwright e2e 15/15 + tsc both configs clean + build clean. Deployed-symmetric score -7,287 (Branch B-accept under D26-β; 24× past -300 numerical Branch A threshold deferred to Phase 3 playtest or deliberate WP14.20 tune cycle). 17-SURFACE close.
- **Backlog resolved:** SURFACE-2026-05-25-03 — closed by WP14.19 Phase 4 D27 back-loop (mission JSON spawn AS recalibration to V_trim=78 in all 5 mission files).
- **Backlog resolved:** SURFACE-2026-05-25-02 — closed by D26-β per-regime `altEnvelope` extension to `tools/tune/score.ts` (the 88% `inducedDragK_wing` bound-pressure was a side effect of high-regime climb-suppression; D26-β eliminated the structural cause).
- **Backlog resolved:** SURFACE-2026-05-25-01 — closed by D25-ζ uniform spawn AS=78 + score-function reversion to all-level-cruise (target=78 uniform; AS_ENVELOPE 25→30); CLAUDE.md Rule #9 amended at D25.
- **Backlog resolved:** SURFACE-2026-05-24-09 — behavioral close at WP14.19 ship (architectural close was at `fix-resetforces-bug` commit `46f9b42`: Rapier per-tick force accumulator now reset before `applyForces`).
- **Backlog resolved:** SURFACE-2026-05-24-08 — superseded by SURFACE-09; integrator-pathology-driven c0-floor cluster collapsed to 0/4 at WP14.18b under fixed integration.
- **Backlog resolved:** SURFACE-2026-05-24-07 — closed-by-cascade-walk-back; D23-γ-evolved mode dispatch was correct for the broken-integrator era; D25 reverted to all-level-cruise once integrator + spawn AS were correct.
- **Backlog resolved:** SURFACE-2026-05-24-06 — closed-by-cascade-walk-back; D22's drag bounds revision was integrator-pathology-driven; gaming corner vanished under fixed integration.
- **Backlog resolved:** SURFACE-2026-05-24-04 — closed-by-cascade-walk-back; D20 bound-widening was integrator-pathology-driven; empirical optimum sits near WP14.13 bounds under fixed integration.
- **Backlog resolved:** SURFACE-2026-05-24-03 — closed-by-cascade-walk-back; the search-vs-deploy 0.67× ratio was integrator-pathology-driven; `--link` tooling fix from task `tune-cli-link-flag` is retained.
- **Backlog resolved:** SURFACE-2026-05-24-01 — closed-by-cascade-walk-back; D18 drag polar IS shipped at WP14.19 D26-β globalBest in production aircraft.json; the "insufficient" verdict was integrator-pathology-driven.
- **Backlog resolved:** SURFACE-2026-05-23-01 — closed-by-cascade-walk-back; D18 drag polar IS the third mechanism layer and IS shipped at WP14.19 ship.
- **Backlog resolved:** SURFACE-2026-05-17-03 — closed-by-cascade-walk-back; clQ values at WP14.19 globalBest are within the empirical stable region.
- **Backlog resolved:** SURFACE-2026-05-17-01 — closed-by-cascade-walk-back; D17 non-dimensional form superseded D15 Form A and ships at WP14.9b commit `0df9a07`.
- **Backlog resolved:** SURFACE-2026-05-16-04 — closed-by-cascade-walk-back; β4+β5+D18 joint space is finite under fixed integration (Vitest 592/592 + e2e 15/15 confirm).
- **Backlog resolved:** SURFACE-2026-05-16-01 — closed-by-cascade-walk-back; D17 non-dimensional V-scaled form is stable above V_REF; shipped at WP14.9b.
- **Backlog resolved:** SURFACE-2026-05-12-03 — closed-by-cascade-walk-back; D16 non-dimensional form (parallel to D17) ships at WP14.10 commit `27324aa`.
- **Backlog resolved:** SURFACE-2026-05-12-01 — closed-by-cascade-walk-back; aircraft.json ships D26-β globalBest with non-zero clAlphaDot wing+hstab; waypoint-patrol mission verified coherent at V_trim=78.
- **Backlog resolved:** SURFACE-2026-05-11-04 — closed-by-cascade-walk-back; β4 + β5 + drag polar at D26-β globalBest damp the phugoid mode coherently (observable at `localhost:5173/?mission=phugoid-probe-mid&debug=true`).
- **Milestone:** D14→D27 physics-tuning cascade fully resolved — Phase 2 mission content (WP15/WP16/WP17) unblocks.

## 2026-05-24

- **Feature escalated:** WP14.12 8-dim joint tune post-D18 — canonical optimizer pass over `(clQ, clAlphaDot, inducedDragK) × wings+h-stab + (fuselageDrag.cd0, fuselageDrag.area)` at WBS bounds with 4 restarts, seed 42, 1800-tick fixtures; globalBest asymmetric score −1.057e9, deployed-symmetric score (per `tools/tune/score-deployed.mjs`) −92,574,536 vs threshold −300 — ratio ~309,000× past flyable; criterion 1 (all 3 regimes finite at 1800 ticks) PASS, criterion 2 (deployed total ≥ −300) FAIL; D18 drag-polar revision alone refuted as sufficient third mechanism layer; filed SURFACE-2026-05-24-01 (D19 candidate: inertia-tensor revision halving Iyy from 3000→1500 per SURFACE-23-01 second-ranked candidate); no `aircraft.json` change, phugoid-probe.spec.ts stays skipped; 545/545 Vitest preserved.
- **Backlog resolved:** SURFACE-2026-05-23-01 — status updated to resolved-by-cascade-but-partial; D18 chosen path closed; flyability gap remains; D19 (SURFACE-2026-05-24-01) is the actionable next architect cycle.
- **Feature escalated:** WP14.13 D19 widened-bounds joint tune — D19 architect cycle (arch.md Revision 2026-05-24) chose bound-widening over SURFACE-24-01's inertia-revision primary candidate via CLAUDE.md Rule #5 plan-time derivation (filed SURFACE-2026-05-24-02 for the axis-naming docs errata); canonical 8-dim tune at widened bounds (`inducedDragK_{wing,hstab}` upper 0.5→1.5, `fuselageDrag.area` upper 3.0→10.0) with 4 restarts, seed 42, 1800-tick fixtures; globalBest pushed all 3 D18 drag knobs higher (k_wing 0.436→1.246, k_hstab 0.125→0.889, area 2.410→6.911); asymmetric score −2.004e9 (worse than WP14.12), deployed-symmetric NaN at all 3 regimes within 17 sec sim, score saturated −2.999e9 at penalty floor — REGRESSION vs WP14.12 on criterion 1; search-vs-deploy ratio INVERTED to 0.67× for first time in D14/D17/D18/D19 cascade (asym better than deploy); D19 widened-bounds refuted; filed SURFACE-2026-05-24-03 with tooling-fix promoted to primary candidate (mirror-symmetric search via `--link surfaces.0=surfaces.1` in tune CLI — supersedes WP14.11 deferred tooling-fix as now-binding methodological gap); no `aircraft.json` change, phugoid-probe.spec.ts stays skipped; 545/545 Vitest preserved.
- **Backlog resolved:** SURFACE-2026-05-24-01 — status updated to resolved-by-cascade-but-partial; D19 widened-bounds chosen path closed; flyability gap remains; SURFACE-2026-05-24-03 is the actionable next architect cycle.
- **Task closed:** `tune-cli-link-flag` (SURFACE-2026-05-24-03 primary candidate) — added `--link <src>=<dst>` flag (repeatable) to `tools/tune/tune.ts` enforcing symmetric-mirror search; extended `TuneArgs` + `parseArgs` + `buildObjective` + `composeResults` + `ResultsJson.meta` with the audit-trail `links` field; 13 new Vitest cases; 558/558 full suite passes; tsc strict (both configs) + Vite build clean; end-to-end smoke at `/tmp/wp14.13-link/tune-with-link.json` confirmed search-vs-deploy ratio collapses to exactly 1.0× (methodology gap eliminated) AND the linked-symmetric optimizer yields deployed-symmetric globalBest **−6.7M** — ~14× better than WP14.12, ~447× better than WP14.13 — but still ~22,000× past flyable threshold (−300); production `aircraft.json` unchanged.
- **Backlog resolved:** SURFACE-2026-05-24-03 — partial close. Tooling-fix (primary candidate) landed; methodology mismatch resolved; flyability-tune still pending. Next architect cycle re-evaluates remaining D19 candidates (envelope re-calibration, inertia revision re-anchored to Ixx, or new fifth-mechanism) against the now-reliable evidence base.
- **Feature shipped:** WP14.14b D21 score-function revision (tooling-only) — `tools/tune/score.ts` gains criterion 0 "level-flight-maintenance" probe (1-second window from spawn; fail if altitude drops > 20m OR airspeed < 10 m/s; -1e9 + failTick prefer-failing-later encoding) and per-regime `targetAirspeed` re-calibrated from the WP14.8 guesses `{low:25, mid:30, high:40}` to physically-derived L=W equilibrium values `{low:45, mid:60, high:85}` (CLAUDE.md Rule #5 architect-cycle derivation showed the score function had been measuring deviation from target_AS ~2× lower than the airframe's actual equilibrium for the entire D14/D17/D18/D19/D20 cascade); `AS_ENVELOPE` tightened 30→25; `ScoreEnvelopes` interface extended with three `LEVEL_FLIGHT_*` fields; precedence ordering binding (NaN > criterion 0 > envelope); 13 new Vitest cases (criterion 0 case-a/b/c/d/e per arch.md D21 verify-self contract + 5 direct unit tests + 4 anti-regression); `tune.test.ts:makeCleanCsv` cross-impact fixed (per-regime AS via throttle→target mapping); 573/573 Vitest + tsc strict (both configs) + Vite build clean; production `aircraft.json` UNTOUCHED. Triggered by a post-WP14.14-finalize in-browser inspection that revealed even the cascade's empirical best (linked-smoke globalBest at WP14.13 widened bounds, deployed −6.7M) cannot sustain 1 second of level flight at the phugoid-probe-mid throttle 0.15 entry conditions (pitch −73°, AS 16.5 m/s, nose-first dive at t≈1s) — a structural blind spot that no numerical proxy in the cascade had surfaced. arch.md Revision 2026-05-24 (evening) also adds physics-mechanism discipline Rules #7 ("diagnostic infrastructure precedes mechanism interpretation") and #8 ("score function targets are physical quantities; derive them, do not guess").
- **Feature escalated:** WP14.14 D20 further-bound-widening linked-search tune — D20 architect cycle (arch.md Revision 2026-05-24 PM) chose 2× further widening of D19's drag-polar bounds + `--link surfaces.0=surfaces.1` constraint over option-c / D19d / fifth-mechanism candidates via CLAUDE.md Rule #5 architect-cycle derivation through the artifact-optima lens (per `feedback_tune_cli_search_vs_deploy.md` Quaternary use); canonical 8-dim tune at further-widened bounds (`inducedDragK_{wing,hstab}` upper 1.5→3.0, `fuselageDrag.area` upper 10.0→20.0, `surfaces.2.clQ` upper 3.0→4.0) with 4 restarts, seed 42, 1800-tick fixtures, three `--link` flags; wall time 17.3s; globalBest `surfaces.0: clQ=1.93, clAlphaDot=0.00(AT FLOOR), inducedDragK=2.44 / surfaces.2: clQ=3.47(AT BOUND), clAlphaDot=0.92, inducedDragK=2.29 / fuselageDrag: cd0=0.44, area=15.48`; all 4 restarts saturated finalScore −2,999,996,805 at NaN-penalty floor (0-of-4 sub-floor — WORSE than WP14.13's 2-of-4 pattern); deployed-symmetric score (per `tools/tune/score-deployed.mjs` against CSVs from `tools/tune/dump-wp14.14.mjs`) per-regime `low=−999,998,878 (NaN @ t=1123)`, `mid=−3,068,907 (full 1800 finite — clean partial-success!)`, `high=−999,998,289 (NaN @ t=1712)`; total deployed **−2,003,066,074** = REGRESSION ~300× vs linked-smoke at WP14.13 widened bounds (deployed −6,705,355); D20 further-bound-widening REFUTED; the cascade empirically locates the drag-polar sweet spot near WP14.13 widened bounds (`k_wing≈1.29, fuselageDrag.area≈7.9`), not the 2×-further bounds D20 hypothesized; β5 collapsed to AT-FLOOR on wings under heavy drag (physically coherent — drag-dominated dissipation makes AoA-rate damping redundant); filed SURFACE-2026-05-24-04 (high, D21 cycle driver) with option-c "accept current state" promoted to primary candidate per `feedback_retune_attempt_budget.md` attempt-3 logic, D19d (envelope re-cal) secondary, fifth-mechanism candidates tertiary, further-widening REMOVED from menu; also filed SURFACE-2026-05-24-05 (medium, parity-diff: optimizer score 1.50× more negative than `score-deployed.mjs` re-score at bit-identical params under `--link`; harness deterministic per md5 stability so the divergence is elsewhere in the eval path); production `aircraft.json` UNTOUCHED, phugoid-probe.spec.ts stays skipped; 558/558 Vitest + tsc strict (both configs) preserved.
- **Feature escalated:** WP14.15 D21-scored tune-deploy at WP14.13 widened bounds — canonical 8-dim linked-search tune (`--link surfaces.0={clQ,clAlphaDot,inducedDragK}=surfaces.1.*`) under the new D21 score function (criterion 0 level-flight-maintenance + per-regime target_AS={45,60,85} + AS_ENVELOPE 25); wall time 16.4s; globalBest `surfaces.0: clQ=1.598, clAlphaDot=0.472, inducedDragK=1.235 / surfaces.2: clQ=2.343, clAlphaDot=3.567, inducedDragK=0.348 / fuselageDrag: cd0=0.573, area=7.456`; optimizer score −2,999,995,607 (all 4 restarts in criterion-0-floor cluster); deployed-symmetric per-regime via `tools/tune/score-deployed.mjs` + new `tools/tune/dump-wp14.15.mjs`: low=−999,999,967 (criterion 0 fail tick 33, airspeed_collapse), mid=−999,999,958 (criterion 0 fail tick 42, airspeed_collapse), **high=−43,601,263 (criterion 0 PASS — first regime-level c0 PASS in the cascade; criterion 2 envelope penalty only)**; total deployed −2,043,601,188; all 3 regimes stay finite 1800 ticks (zero NaN — qualitatively new vs WP14.13/14.14 NaN cascades, mechanism stack is no longer the bottleneck); no knob saturates upper bound (peak 82% on `surfaces.0.inducedDragK`); search-vs-deploy ratio 1.47× (matches SURFACE-24-05's 1.50× residual under `--link` — non-blocking); Branch B per locked plan (per-regime c0 mixed; not all-pass for Branch A, not all-fail for Branch C); filed SURFACE-2026-05-24-06 (high, D22 cycle driver) with D22a "per-regime target_AS recalibration to drag-aware equilibrium" as primary candidate per CLAUDE.md Rule #8 — D21 derivation premised induced-drag k≈0.05 but optimizer prefers k_wing≈1.235 (25× higher) where equilibrium AS at throttle 0.05/0.15/0.40 shifts to ~10/15/25 m/s, NOT 45/60/85; D22b (AS_MIN relax) secondary; D22c (thrust uprating) tertiary; D22d (fifth-mechanism) quaternary; bound-widening + option-c REMOVED from menu; production `aircraft.json` UNTOUCHED, phugoid-probe.spec.ts stays skipped; 573/573 Vitest + tsc strict (both configs) preserved.
- **Backlog resolved:** SURFACE-2026-05-24-04 — partial-update; D21 score-function revision was directionally correct (criterion 0 fires precisely on the level-flight gap; mechanism stack no longer NaN's) but incompletely calibrated; SURFACE-2026-05-24-06 is the new actionable D22 cycle driver. Chain of 11 (SURFACE-24-04 + 10) remains partial; full close still pending a successful tune-deploy WP.
- **Architecture revised:** D22 — narrow drag-knob bounds to physical-realism range (arch.md Revision 2026-05-24 late evening). Independent derivation per CLAUDE.md Rule #5 REFUTED SURFACE-24-06's primary candidate D22a ("drag-aware equilibrium AS ~10/15/25 m/s"): under WP14.15 globalBest drag (k_wing=1.235, cd0_fus·area_fus=4.272), minimum total drag at L=W is 13,360 N = 2.2× max thrust 6000 N — no level-flight equilibrium exists at any AS at any of the searched throttles. Diagnosis: the optimizer's bound-widening cascade (WP14.12→WP14.13→WP14.14) was driven by score-function gaming (AS-envelope penalty rewards crashing AS via heavy drag), not by physical realism; the Nelder-Mead simplex seed-initialization placed vertices in the mid-range of widening bounds and contracted toward gaming corners. D22-β chosen singularly per `feedback_surface_or_means_or.md`: narrow drag bounds to Cessna-realism range (`inducedDragK_{wing,hstab}` upper 1.5→0.3 = 6× Cessna textbook k≈0.05; `fuselageDrag.cd0` upper 1.0→0.5; `fuselageDrag.area` upper 10.0→1.0). NO code change; NO score.ts edit; NO new schema. WBS additions: WP14.16 (single-WP tune-deploy + binding browser walkthrough, no impl step). WP14.15b (D22 impl WP) DELETED from WBS. SURFACE-24-06 status: partial (full close at WP14.16 Branch A). Generalizable lesson surfaced (single observation, NOT promoted to Rule #9 yet): when bound-saturation signals point at widening, ask whether the optimizer is gaming a score-function gap before honoring the signal. Production `aircraft.json` UNTOUCHED; 573/573 Vitest preserved.
- **Feature escalated:** WP14.16 D22-β tune-deploy at narrowed bounds — canonical 8-dim linked-search tune at D22-β narrowed bounds (k≤0.3, cd0_fus≤0.5, area_fus≤1.0). Wall time ~16s. Optimizer globalBest score −2,013,828,659 (restart 1 ESCAPED c0-floor; restarts 0/2/3 saturated at −2.999e9). globalBest `surfaces.0: clQ=1.416, clAlphaDot=1.740, inducedDragK=0.278 / surfaces.2: clQ=2.240, clAlphaDot=3.301, inducedDragK=0.062 / fuselageDrag: cd0=0.268, area=0.711`. Deployed-symmetric per-regime: low=−33.6M (c0 PASS), mid=−37.6M (c0 PASS), high=−22.0M (c0 PASS); **total deployed −93,235,221 — ALL 3 regimes PASS criterion 0 (first regime-level c0 PASS for ALL regimes in entire D14/D17/D18/D19/D20/D21/D22 cascade — qualitative breakthrough)**; all 3 finite 1800 ticks; ~22× BETTER than WP14.15's −2.04e9 deployed but still ~310,000× past −300 threshold. Trajectory phenomenology: airframe REACHES D21 target AS at t=1s (mid 45.7/high 83.3 ≈ targets 45/60/85) but lacks longitudinal damping to sustain — pitch oscillates ±70°, AS swings ±50 m/s in coupled phugoid+short-period motion. `inducedDragK_wing` saturates new 0.3 bound at 93% (NEAR-BOUND warning per D22 locked rule — do NOT widen, that's the gaming corner returning). Branch B per locked plan (c0+c1 PASS, c2 fails). Filed SURFACE-2026-05-24-07 (high, D23 cycle driver) with D23-α "envelope re-evaluation for natural phugoid oscillation" primary candidate per CLAUDE.md Rule #8 at second-order — observed AS swings ±50 m/s exceed current AS_ENVELOPE=25; either widen envelope to match physical Cessna-class phugoid amplitude OR add stronger damping mechanism; D23-β (thrust uprating) secondary; D23-γ (T=D criterion) tertiary; D23-δ (fifth-mechanism damper) quaternary; bound-widening REMOVED from menu. SURFACE-24-06 partial-update (D22 chosen path closed, mechanism+bounds problem resolved, envelope/damping problem revealed). Production `aircraft.json` UNTOUCHED, phugoid-probe.spec.ts stays skipped; 573/573 Vitest + tsc strict (both configs) preserved.
- **Backlog resolved:** SURFACE-2026-05-24-06 — partial-update; D22-β chosen path landed cleanly; the mechanism-stack-cannot-level-fly hypothesis is REFUTED (all 3 regimes c0 PASS at narrowed bounds); SURFACE-2026-05-24-07 is the new actionable D23 cycle driver addressing the residual longitudinal-damping/envelope-calibration gap. Chain of 12 (SURFACE-24-06 + SURFACE-24-04 + 10) remains partial; full close pending WP14.17 (D23-resolving WP) Branch A.
- **Architecture revised:** D23 — per-regime throttle-mode reframe + T=D regime guard (arch.md Revision 2026-05-24 night). Independent derivation per CLAUDE.md Rule #5 REFUTED both SURFACE-24-07's D23-α (envelope re-evaluation for natural phugoid oscillation) AND D23-β (thrust uprating) candidates. Critical finding: under WP14.16 globalBest drag (k_wing=0.278, cd0_fus·area_fus=0.190), the airframe's D_min at L=W is 1959 N at V=61 m/s; thrust at τ=0.05 is 300 N (0.15× D_min), τ=0.15 is 900 N (0.46× D_min) — **no level-flight equilibrium exists at low OR mid throttles regardless of AS**. The c0 PASS at low/mid was a 1-second-window artifact identical to WP14.15's high-regime artifact / D22 finding. Observed motion in WP14.16 trajectories is non-linear stall-recovery chaos (AS amplitude ±84 m/s vs textbook ±10-40 m/s phugoid; pitch saturates ±90° vs textbook ±5-15°), NOT a linear phugoid. D23-α envelope-widening would accept inverted-attitude motion as "good flight" (fails casual-gamer-feel); D23-β thrust uprating requires 47 kN = 7.8× current (unphysical for small-civilian-aircraft identity). The structural insight: D21's per-regime equal-weight assumption "every throttle = level cruise" was wrong (real Cessna at idle DESCENDS by design). D23-γ-evolved chosen singularly per `feedback_surface_or_means_or.md`: per-regime mode dispatch (`low=controlled-descent`, `mid=slow-flight-or-shallow-descent`, `high=level-cruise` preserves D21 logic unchanged) + optional T=D regime guard (level-cruise only). Tooling-only score.ts change; no aircraft.json change; no bound change; no mechanism addition. WBS additions: WP14.17 (D23 impl, size S) + WP14.18 (re-tune at D22-β bounds under new score function + browser walkthrough, size S). Bidirectional bound-failure-mode lesson now has THREE observations (D22 narrowing + WP14.16 saturation + D23-α-would-have-been); memory update queued for `feedback_optimizer_bounds_are_floor.md` rule 2 at WP14.18 close. SURFACE-24-07 status: partial (full close at WP14.18 Branch A together with chain of 12 = 13 SURFACEs total). Production `aircraft.json` UNTOUCHED; 583/583 Vitest preserved.
- **Task closed:** WP14.17 D23-γ-evolved impl (tooling-only) — `tools/tune/score.ts` gains `RegimeMode` type union (`'controlled-descent' | 'slow-flight-or-shallow-descent' | 'level-cruise'`) + per-mode scoring functions (`controlledDescentScore` for low-throttle sink-rate envelope + nose-down pitch envelope; `slowFlightScore` for mid-throttle AS+sink+symmetric-pitch envelope; `levelCruiseScore` = refactored D21 logic + optional T=D regime guard via `tdGuardPenalty` helper); top-level `regimeScore` dispatches on `envelopes.regimeMode?.[regime]` with `undefined`/missing → fallback to `levelCruiseScore` (back-compat default); `ScoreEnvelopes` extended with 13 optional D23 fields (regimeMode, SINK_RATE_*_*, AS_MID_*, PITCH_*_DEG, TD_GUARD_*, TD_IMBALANCE_LIMIT_N, airframe); `DEFAULT_ENVELOPES` populated with D23 defaults; `airframe` left undefined by default (T=D guard no-op for back-compat); 10 new Vitest cases (7 binding cases per arch.md "Test additions binding for WP14.17" + 3 extra it() blocks covering fallback edge cases + transient-window + mode-mismatch); `tune.test.ts:makeCleanCsv` updated to produce mode-appropriate clean fixtures per regime; new test fixture `LEGACY_LEVEL_CRUISE_ENVELOPES` (`= DEFAULT_ENVELOPES` with `regimeMode: undefined`) migrates 10 pre-D23 tests that assumed all regimes are level-cruise; 583/583 Vitest + tsc strict (both configs) clean; all public exports preserved (`DEFAULT_ENVELOPES, score, regimeScore, firstNanTick, levelFlightMaintenanceCheck, phugoidGrowthPenalty`); `score-deployed.mjs` runs cleanly under D23 defaults producing expected mode-dispatch signal at low+mid (c0-floor — WP14.18 will re-tune); production `aircraft.json` UNTOUCHED; WP14.17 closes nothing in SURFACE chain on its own — WP14.18 is the cascade close gate.
- **Feature escalated:** WP14.18 D23 tune-deploy at D22-β narrowed bounds under D23-γ-evolved score function — canonical 8-dim linked-search tune (verbatim WP14.16 command + bounds + seed + restarts; only the score function changed via WP14.17 commit `4386536`); wall time ~15s; globalBest `surfaces.0: clQ=1.468, clAlphaDot=1.717, inducedDragK=0.277 (92.4% of 0.3 bound) / surfaces.2: clQ=2.243, clAlphaDot=3.302, inducedDragK=0.061 / fuselageDrag: cd0=0.271, area=0.693`; optimizer reported score −2,035,174,819 (restart 1 emerged from c0-floor; restarts 0/2/3 stayed at −2.999e9 c0-floor; emergence ratio 1/4 — NO improvement over D21@WP14.16); deployed-symmetric (via `tools/tune/score-deployed.mjs` under D23 DEFAULT_ENVELOPES) total **−2,999,998,384 = full c0-floor across all 3 regimes** (low=−999,999,967, mid=−999,999,946, high=−999,998,471); mode-specific c0 inspection — ALL 3 regimes pitch up to +24°/+28° within t=1s exceeding all mode pitch envelopes, then gimbal-lock at t≈5s; high regime NaN's at tick 1530 (mechanism stack stability REGRESSED vs WP14.16 all-3-finite); 4 of 4 acceptance criteria FAIL; **D23 mode-dispatch hypothesis REFUTED by evidence**; `inducedDragK_wing` saturation 92.4% = fourth observation of loosen-constraint anti-pattern; filed SURFACE-2026-05-24-08 (high, D24 cycle driver) with D24-α fifth-mechanism damper primary, D24-β/γ envelope tuning secondary/tertiary, bound widening REJECTED per bidirectional rule; production `aircraft.json` UNTOUCHED; phugoid-probe.spec.ts stays skipped; 583/583 Vitest preserved.
- **Task closed:** WP14.18 investigation spike — identified the energy-injecting mechanism that has been the cascade root cause. **`body.resetForces()` + `body.resetTorques()` are never called between physics ticks in `src/aircraft/physics-core/step.ts:32-36`**; Rapier's per-tick force accumulator compounds across ticks; at tick n the applied force = (n+1) × intended. Isolated probe at throttle=0.4 with aero zeroed showed Δvz per tick = −(n+1)·0.040 m/s (vs correct −0.040). Fix verified with explicit `resetForces` call producing −0.0400 at every tick. **This is the root cause of the entire D14→D23 cascade**: 8 architect revisions and 6 substantive tune cycles were chasing symptoms of a one-line integrator bug. Probes at `tools/tune/probe-{wp14.18,thrust-only,noeverything,noaero,fix-test}.mjs`. Filed SURFACE-2026-05-24-09 (high, CRITICAL — supersedes SURFACE-24-08 as D24 cycle driver) with the fix specification + impact assessment + walk-back plan for the 13-SURFACE cascade chain; production code unchanged at task close (D24 architect cycle decides the fix scope + whether to retain/discard the cascade's mechanism additions). Tooling-only; no `aircraft.json` change; no schema change; tests UNCHANGED; 583/583 Vitest preserved.
- **Task closed:** fix-resetforces-bug — added `body.resetForces(true) + body.resetTorques(true)` at the entry of `FlightModel.applyForces` (`src/aircraft/physics-core/flightmodel.ts:132-136`), fixing the cascade root cause identified at the WP14.18 investigation spike (Rapier's per-tick force accumulator was compounding at (n+1)× intended at tick n). Single-point enforcement at the API boundary covers both harness path (via `step.ts`) and browser path (via `src/main.ts:90-95`). WP14.18b re-tune evidence (verbatim WP14.16 command, same bounds + seed + restarts, only the integrator fixed): optimizer best score −2,035,174,819 → **−9,422.98** (216,000× improvement); deployed-symmetric total −2,999,998,384 → **−26,306.38** (114,000× improvement); c0-floor cluster **3 of 4 restarts → 0 of 4 restarts** (all 4 converged to similar emerged scores); all 3 regimes finite 1800 ticks (high regime previously NaN'd at tick 1530); `inducedDragK_wing` saturation **92.4% → 1.5%** (re-anchored near Cessna textbook 0.05); probe-thrust-only.mjs now shows Δvz/tick = −0.040 at every tick (was −(n+1)·0.040). 579/583 Vitest pass; 4 expected failures categorized — 3× `tests/parity-diff.test.ts` (browser-side goldens stale, need regenerating via Playwright) + 1× `tools/tune/harness.test.ts:307` (asserted `basePeak > 400 m/s` on the SURFACE-2026-05-23-01 bug-symptom; with fix baseline correctly tops out at 42 m/s — assertion needs flipping or test deprecation as part of D24 walk-back). CLAUDE.md Rule #7 added codifying the per-tick energy-budget sanity check. Production `aircraft.json` UNTOUCHED. Cascade-recovery posture: project sits at fundamentally healthier state than at any point since WP9.6; remaining gap to flyable (−300 stretch threshold) is ~88× — D24 architect cycle's path forward is short.
- **Backlog resolved:** SURFACE-2026-05-24-09 — root cause fix verified end-to-end; 114,000× deployed-score improvement at WP14.18b. D24 architect cycle still needed for (i) final tune-deploy decision under correct integration, (ii) cascade walk-back of the 13-SURFACE chain (SURFACE-24-08 + -07 + -06 + -04 + -03 + -01 + -23-01 + -17-03 + -17-01 + -16-04 + -16-01 + -12-03 + -12-01 + -11-04), (iii) flip-or-deprecate the 4 Vitest cases anchored to bug-symptom values.

## 2026-05-23

- **Feature shipped:** WP14.10 β5 non-dimensional AoA-rate damping (D16 implementation) — replaced the raw-rate `cl += clAlphaDot · dα/dt` form at the β5 branch with the textbook non-dimensional CL-augmentation `cl += clAlphaDot · dα/dt · c̄ / (2 · max(V, V_REF))` (Etkin & Reid §5.10–5.12), reusing the `chordLength` cache and `BETA4_V_REF` constant from WP14.9/WP14.9b; verify-self triple gate passed at clAlphaDot=5,5,8,0 across throttle-low + throttle-mid regimes (substituted for plan's throttle-high because baseline β4 setup NaN's there — WP14.11 territory); Rule #4 control bit-identical to baseline; plan-time physics derivation matched D16 prose exactly (no arch errata, unlike WP14.9b); 525/525 Vitest + tsc strict (both configs) + build clean.
- **Backlog resolved:** SURFACE-2026-05-16-04 — β5 side closed by WP14.10; full close on both sides awaits WP14.11 joint tune.
- **Backlog resolved:** SURFACE-2026-05-12-03 — mechanism side closed by WP14.10 (D16 non-dim form replaces raw-rate); tuning side awaits WP14.11.
- **Backlog resolved:** SURFACE-2026-05-12-01 — mechanism-side blocker resolved; now blocked by WP14.11 tuning rather than mechanism.
- **Backlog resolved:** SURFACE-2026-05-11-04 — phugoid undamped: architectural + mechanism sides both resolved; tuning side awaits WP14.11.
- **Milestone:** WP14.10: β5 non-dimensional form (D16). D17+D16 cascade impl side both shipped; WP14.11 (joint tune) is the natural next unit.
- **Feature escalated:** WP14.11 joint (clQ, clAlphaDot) tuning retry — canonical `[0..15]^4` optimizer and narrow `[0..3]×[0..10]^4` re-run both failed to find a cross-threshold-flyable point; multiple symmetric-mirror points produce 1800 finite ticks across all 3 throttle regimes (partial success vs WP14.5-retry's zero-finite-points box), but airspeed peaks 230-373 m/s and deployed-config scores ~−100M put every searched point well outside the phugoid-probe envelope; β4 D17 + β5 D16 mechanisms work as designed (numerical finiteness achieved), but airframe energy balance / drag-CD / inertia / phugoid interaction produces unflyable dynamics no (clQ, clAlphaDot) can resolve; new `tools/tune/score-deployed.mjs` utility added to compute the deployed-symmetric airframe score (the tune CLI's reported score reflects an asymmetric search-space airframe — search-vs-deploy mismatch flagged as tooling fix candidate); filed SURFACE-2026-05-23-01 with 5 ranked investigation candidates (drag-CD → inertia → Theodorsen → ω×r retirement → score envelope); no `aircraft.json` change, phugoid-probe.spec.ts stays skipped; 525/525 Vitest + tsc strict (both configs) + build clean.
- **Backlog resolved:** SURFACE-2026-05-17-03 — partial close; bounds-narrowing recommendation applied at WP14.11; superseded by SURFACE-2026-05-23-01 as actionable arch driver.
- **Backlog resolved:** SURFACE-2026-05-16-04 — superseded by SURFACE-2026-05-23-01; β4+β5 mechanism halves both confirmed working (no longer the bottleneck).
- **Backlog resolved:** SURFACE-2026-05-17-01 — D17 reframe complete; β4 mechanism layer no longer suspect; downstream tuning concerns moved to SURFACE-2026-05-23-01.
- **Backlog resolved:** SURFACE-2026-05-16-01 — β4 high-throttle runaway resolved by D17 + WP14.9b at `clQ=0,1`; the remaining airframe-unflyability is no longer a β4 issue.
- **Feature shipped:** WP14.11.5 D18 drag polar implementation (induced drag + fuselage parasitic drag) — added per-surface optional `inducedDragK?` augmenting CD by `k · cl²` at step 4d in `computeAeroForce` (post-β4/β5 cl per textbook lifting-line) and top-level optional `fuselageDrag?: {cd0, area}` body-level drag force at body origin via `addForce` (zero torque) in `flightmodel.ts:applyForces` step 1b; harness path-parser extended to auto-create missing intermediate object segments (enables `fuselageDrag.cd0=0.3` on baseline aircraft.json); triple-gate verify-self PASSED at substituted 600-tick low+mid window (peak AS reduction 7.7× at throttle-low, 1.4× at throttle-mid; Rule #4 control bit-identical to baseline via `diff -q`); 2 documented Rule-#4-addendum substitutions (regime: low+mid plan-time + tick window: 600 instead of 1800 run-time, because baseline β4 NaN's at low+mid given 650-800 ticks — orthogonal β4 runaway pollutes the D18 signal; 1800-tick gate deferred to WP14.12 where joint tuning shrinks β4 alongside D18); 545/545 Vitest (was 525, +20 net new) + tsc strict (both configs) + build clean; no `aircraft.json` change per the D17/D16 two-close-gates split.

## 2026-05-17

- **Feature escalated:** WP14.9 β4 implicit-Euler integration (D15) — attempt-1 moment-amplification-ratio implementation refuted at verify-self (baseline NaN at tick 416, vs pre-D15 tick 417 — no improvement); operator selected Option 3 (full reframe — V-scaling shape itself is wrong, not the explicit-vs-implicit dimension); filed SURFACE-2026-05-17-01 as the actionable D15-rev driver (textbook non-dimensional pitch-rate-damping `cl_q · ω · c̄ / (2V)` parallel to D16); production code reverted to pre-D15 baseline; `chordLength` cache retained on `AeroSurface` for D16 consumption; successor WP pending arch revision via /product-arch.
- **Feature shipped:** WP14.9b β4 non-dimensional pitch-rate damping (D17 implementation) — replaced the WP6.6 airflow-amplification of `ω × r` with the textbook non-dimensional CL-augmentation `cl += clQ · ω_along_dampAxis · c̄ / (2 · max(V, V_REF))` at step 4b alongside β5, restoring linear-V damping force growth (vs pre-D17 cubic-V³); added per-surface `dampAxis` cache with corrected `(normal × position)` sign vs arch.md literal text (SURFACE-2026-05-17-02 errata); verify-self triple gate passed at clQ=1 (Rule #2) + clQ=0 (Rule #4) + Rule #1 live observation across throttle-low/high regimes; SURFACE-2026-05-17-03 filed flagging the empirical stable region is `[0..~1.5]` per surface (narrower than textbook 1–10), recommending WP14.11 optimizer bounds tighten from `[0..15]` to `[0..2]`; 520/520 Vitest + 3/3 parity Playwright + tsc strict (both configs) + build clean.
- **Backlog resolved:** SURFACE-2026-05-17-01 — D17 implementation side closed by WP14.9b; full close awaits WP14.11.
- **Backlog resolved:** SURFACE-2026-05-16-01 — β4 origin record; D17 cascade addressed it via WP14.9b; full close awaits WP14.11.
- **Backlog resolved:** SURFACE-2026-05-16-04 — β4 side closed by WP14.9b; β5 side awaits WP14.10 + WP14.11.
- **Milestone:** WP14.9b: β4 non-dimensional pitch-rate damping (D17 cascade step 1 of 3 for the post-WP14.9-escalation recovery; WP14.10 + WP14.11 remain).

## 2026-05-16

- **Feature shipped:** WP14.7 Node Rapier-WASM harness — `tools/tune/harness.ts` boots Rapier-WASM in Node, accepts deep-path parameter overrides + fixture id + tick count via CLI, emits trajectory CSVs the browser path diffs to within `|Δ|<1e-6`; `parity-diff.test.ts` rewired with browser→(harness | synthetic) precedence and parity-of-divergence semantics; second of 3 WPs in the D14 cascade.
- **Feature shipped:** WP14.8 score function + Nelder-Mead optimizer + tune CLI — `tools/tune/score.ts` implements multi-regime envelope-probing fitness (NaN penalty encodes time-to-first-NaN as a gradient), `tools/tune/optimizer.ts` runs Nelder-Mead with K random restarts + local quadratic regression on the best simplex (dimension-agnostic from day one), `tools/tune/tune.ts` is the `npm run tune` CLI that wires them with the WP14.7 harness; third of 3 WPs in the D14 cascade — fully landed, WP14.5-retry now genuinely unblocked.
- **Milestone:** WP14.8: Score function + Nelder-Mead optimizer + tune CLI (D14 cascade step 3 of 3).
- **Feature shipped:** WP14.5-retry joint (clQ, clAlphaDot) optimizer search — ran `npm run tune` over the 4D joint parameter space for wing-left + h-stab; all 8 sampled points hit the NaN floor with degenerate regression Hessian; filed SURFACE-2026-05-16-04 as the consolidated mechanism-revision driver recommending Option A on both β4 (implicit-Euler form) and β5 (non-dimensional `cl_α̇ · c̄ / (2V)` form); `aircraft.json` + `tests/e2e/phugoid-probe.spec.ts` unchanged; D14 cascade's harness-optimizer delivered empirical evidence in 7.24s of wall-clock that would have taken weeks of hand-tuning.
- **Milestone:** WP14.5 (rescoped): `clAlphaDot` tuning pass via harness — escalated via SURFACE-2026-05-16-04 to forthcoming D15/D16 cascade.

## 2026-05-12

- **Feature shipped:** WP12 HUD — DOM overlay renders alt/airspeed/throttle, objective text, status banner, and projection-ready waypoint arrow in-mission per arch.md D12.
- **Task closed:** WP13 free-flight mission — added Escape-to-mission-select abort path; mission JSON already shipped at WP11 + HUD overlay at WP12.
- **Milestone:** Free flight mission (Phase 2 roadmap).
- **Feature shipped:** WP14 waypoint patrol mission — 2-waypoint glide-reachable patrol with ordered objective tracking + HUD waypoint-arrow wiring + 30s timeout fail; reduced scope from planned 4-waypoint loop because SURFACE-2026-05-12-01 (phugoid tuning side) gates sustained-throttle flight.
- **Milestone:** Waypoint navigation mission (Phase 2 roadmap).
- **Task closed:** WP14.5 `clAlphaDot` tuning pass — option-c disposition; 3 tuning attempts (+5/+10, +1/+2, -1/-2) all diverged catastrophically, surfacing SURFACE-2026-05-12-03 (β5 mechanism in `aerosurface.ts` is dimensionally mismatched and needs arch-level revision); no config change shipped, phugoid-probe spec landed under `test.skip` for re-use after the mechanism revision.
- **Feature shipped:** WP14.6 physics-core extraction + harness↔browser parity test — `src/aircraft/physics-core/` is now a framework-agnostic Node-runnable subset of the flight model, `tests/parity-diff.test.ts` asserts bit-identical trajectories between browser and in-process synthetic stub (1800 ticks, `|Δ|<1e-6`); first of 3 WPs in the D14 cascade; bonus fix to `Aircraft.reset()` clearing stale Rapier force accumulators.
