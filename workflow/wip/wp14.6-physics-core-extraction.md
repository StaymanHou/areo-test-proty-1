---
workflow: feature
state: plan (complete)
drive_mode: full-autopilot
created: 2026-05-12
wp: WP14.6
arch_anchor: docs/product/arch.md Revision 2026-05-12 (afternoon) §D14.2 + §D14.3
---

# Feature: WP14.6 — Physics-core extraction + harness↔browser parity test

**Workflow:** feature
**State:** plan (complete)
**Created:** 2026-05-12

## Problem Statement

WP14.5's hand-tuning of β5 (`clAlphaDot`) burned the 3-attempt budget on three sparse points and produced no usable signal in a continuous 2-knob × 3-regime space. The operator escalated to a major detour: build a Node-side headless physics harness that runs at many multiples of wall-clock, scored against an envelope-probing fitness function, driven by a gradient-free optimizer. arch.md Revision 2026-05-12 (afternoon) landed that decision as D14 with a binding cascade — WP14.6 → WP14.7 → WP14.8 → rescoped WP14.5. **This WP (14.6) is the first step:** make the shipped flight-model code callable outside the browser (file-move reorg into `src/aircraft/physics-core/`) and stand up a harness↔browser **parity test** that asserts bit-identical trajectories (`|Δ|<1e-6` per scalar) across the WASM boundary. At this WP the harness side is a Vitest synthetic stub — pure-TS loop calling the same `physics-core/` modules — which proves the modules are framework-agnostic-callable. WP14.7 will replace the stub with the real Node Rapier-WASM harness.

**Plan-time discovery worth surfacing now (NOT a back-loop, just a refinement of the split criterion):** Every existing file in `src/aircraft/` imports `three`, but `aerosurface.ts`, `config.ts`, `flightmodel.ts`, and `state.ts` import only `Vector3` / `Quaternion` (pure math classes that work in Node — Three exports them with no browser-API dependency). Only `rigidbody.ts` imports rendering primitives (`Scene`, `Mesh`, `BoxGeometry`, `MeshStandardMaterial`, `Group`). The arch.md split rule said "does the file import `three`?" — applied literally, that puts *nothing* under `physics-core/`. The arch *intent* is "does this code require browser APIs to run?", and only `rigidbody.ts`'s mesh-building does. **The plan adopts the intent-rule**: math-only `three` imports (Vector3, Quaternion) are allowed under `physics-core/` and remain in the same `three` package they already use. Rendering primitives (Scene, Mesh, geometries, materials) are the actual browser-coupling and stay in `src/aircraft/rigidbody.ts`. CONVENTIONS.md will document this distinction. This refinement is consistent with the arch revision; no back-loop needed.

**3rd-party probe check:** N/A — no new external APIs. Three.js (Vector3/Quaternion only) and Rapier-WASM are both already integrated and have multi-WP track record. The harness in WP14.7 will use `@dimforge/rapier3d-compat` in Node, which is the same package the browser uses — no new 3rd-party. WP14.6 doesn't run the harness yet.

**Backlog scan:** SURFACE-2026-05-12-03 (high — β5 mechanism revision; downstream of WP14.5-retry which depends on this WP's cascade), SURFACE-2026-05-12-01 (medium — blocked-by -03), SURFACE-2026-05-11-04 (partial), SURFACE-2026-05-12-02 (low — test-mission pollution; not touched here). None of the high-priority items conflict with WP14.6's scope; this WP is part of the cascade that addresses them.

**Memory anchors active for this WP:**
- `feedback_pre_scaffold_checklist.md` — Phase 1 is a destructive-shaped operation (file moves). Use `git mv` not delete+recreate so blame/history survive. Git tree was NOT clean at plan time (`docs/product/arch.md`, `docs/product/wbs.md` modified from D14 work upstream of this WIP); those are this same session's intended deltas and ride forward into WP14.6's first commit. No stashing needed.
- `feedback_verify_self_envelope.md` — applies to the PARITY test, not to the build. At WP14.6 parity covers only `throttle-mid` (0.15) on a single trajectory. WP14.7 extends to low (0.05) and high (0.4); WP14.8's score function expands further. **Documented gap is recorded in the WP14.7 task list of wbs.md so this isn't lost** — no need to over-build here.
- `feedback_asymmetric_fix_no_op.md` — applies later (WP14.5-retry's score function will verify no-op-in-glide-regime). Not relevant to this WP's verification.

## Work Tree

- [x] Phase 1: File-move reorg — extract framework-agnostic physics-core  <!-- status: complete -->
  **Observable outcomes:**
  - CLI: `npm run test` exits 0 with 385/385 cases pass — **bit-for-bit identical** test count and pass/fail outcomes to pre-move baseline; no behavioral changes. ✓ 385/385 in 933ms
  - CLI: `npx tsc --noEmit` exits 0 (strict mode clean) — all import-path updates landed. ✓
  - CLI: `npm run build` exits 0 — Vite bundle still builds clean. ✓ (only pre-existing SURFACE-2026-04-19-01 bundle warning)
  - CLI: `ls src/aircraft/physics-core/` lists `aerosurface.ts`, `flightmodel.ts`, `state.ts`, `config.ts`, `rigidbody-core.ts`, `step.ts`. ✓
  - CLI: `ls src/aircraft/` (top-level) still lists `rigidbody.ts` and `controls.ts` (kept), but no longer lists the moved files. ✓
  - Browser: `npm run dev` + open `/?debug=true` boots without errors; aircraft renders; `window.__aircraft.getState()` returns finite values at 5s (existing WP9.6 smoke contract still holds). This is the cheapest non-regression check beyond the tests. (Pending — covered by verify-self in Phase 3 + WP9.6 e2e regression anchor at verify-auto.)
  - [x] P1.1 Confirmed git tree state (docs/product/arch.md + wbs.md modified — D14 session deltas to ride forward); WIP file (this file) added to tree.  <!-- status: complete -->
  - [x] P1.2 `git mv` 4 source files + 4 tests under `src/aircraft/physics-core/`. Git rename detection (`R` status) preserved on all 8.  <!-- status: complete -->
  - [x] P1.3 Extracted `AircraftBody` class into `src/aircraft/physics-core/rigidbody-core.ts` (Rapier body + collider + mass/inertia + `readBodyState` + `reset` + `setMassProperties` + bodyState scratch). `src/aircraft/rigidbody.ts` `Aircraft` now extends `AircraftBody` and adds only the Three.js mesh (`buildPlaceholderMesh` + `syncMesh`). Public `Aircraft` API surface bit-identical for `src/main.ts` (inheritance is transparent).  <!-- status: complete -->
  - [x] P1.4 Created `src/aircraft/physics-core/step.ts` — single-tick driver `step(world, aircraft, flightModel, {throttle}, dt)` that wraps `flightModel.applyForces` + `world.step` (the same sequence main.ts runs inline). Controls/aileron/elevator deflections are NOT included — those are browser-input-driven; harness fixtures use pre-set surface state.  <!-- status: complete -->
  - [x] P1.5 Updated importers. Known set + 1 missed (caught by tsc): `src/main.ts`, `src/engine/tuning.ts`, `src/engine/tuning.test.ts`, `src/hud/{HUD,dom-hud,dom-hud.test,format}.ts`, `src/mission/{runner,runner.test,types,parse}.ts`, **`src/mission/hooks/registry.ts`** (initially missed; surfaced by `tsc --noEmit` and fixed). Intra-aircraft imports updated: `rigidbody.ts`/`rigidbody.test.ts`/`stability.test.ts` now reach into `./physics-core/`; `flightmodel.ts`/`flightmodel.test.ts` use `../rigidbody` for the Three-side `Aircraft`. `config.test.ts` JSON path bumped to `../../../public/...`.  <!-- status: complete -->
  - [x] P1.6 Test sweep: full Vitest 385/385 green; tsc strict clean; `npm run build` clean.  <!-- status: complete -->
  - [x] verify-auto: scoped tsc --noEmit clean + scoped Vitest 145/145 in 234ms (physics-core/* + rigidbody.test.ts + stability.test.ts) + npm run build clean. Pre-existing SURFACE-2026-04-19-01 bundle warning unchanged.  <!-- status: complete -->
  - [x] verify-self: full Vitest 385/385 + tsc strict clean + directory structure correct + Playwright e2e 9/9 PASS in 20.2s (3 phugoid-probe specs intentionally test.skip()'d from WP14.5 — to be un-skipped at WP14.5-retry). casual-flight.spec.ts (WP9.6 regression anchor) green — `window.__aircraft.getState()` works through the live browser path that import changes touched. Integration boundary (rigidbody.ts → main.ts) satisfied by the casual-flight + mission-select + hud + waypoint-patrol e2e suite.  <!-- status: complete -->
  - [x] verify-human: SKIPPED — full-autopilot drive mode per `feedback_operator_as_external.md` (operator-as-external-tester deviation continues; verify-self at live-system bar is the acceptance gate).  <!-- status: skipped-full-autopilot -->
  - [x] verify-codify: codification analysis — moved files' behavior is intrinsically covered by their (also-moved) test files (245+ cases). `Aircraft extends AircraftBody` covered by existing rigidbody.test.ts + stability.test.ts (still green). Integration boundary (rigidbody.ts → main.ts) covered by 9-spec Playwright suite. NEW step.ts gets a small 4-case structural test (`src/aircraft/physics-core/step.test.ts`) — one-tick advancement, thrust-applies-per-throttle, no-op-at-rest, and a determinism-vs-inline-path case that codifies the contract WP14.7's harness depends on. Triage handled (1 over-precise assertion at f32-round-trip boundary, loosened to `toBeCloseTo(dt, 6)`). Final: 389/389 Vitest (was 385, +4 new step.test.ts).  <!-- status: complete -->

- [ ] Phase 2: Browser trajectory hook + parity test (single fixture, synthetic stub)  <!-- status: NOT-STARTED; depends on Phase 1 -->
  **Observable outcomes:**
  - Browser: with `?debug=true`, `window.__aircraft.getTrajectory()` returns an array of trajectory rows `{tick, posX, posY, posZ, vX, vY, vZ, pitch, yaw, roll, airspeed}` of length up to 1800 (ring buffer), each row finite, monotonically increasing `tick`. `getTrajectory()` returns `[]` when no mission/sim is running.
  - CLI: `npm run test:e2e` exits 0 with 10/10 — existing 9 (casual-flight, mission-select × 3, hud × 2, waypoint-patrol × 2, free-flight × 1) + new 1 parity emitter (`parity.spec.ts`).
  - CLI: `npm run test:e2e` produces `test-results/browser-trajectory-throttle-mid.csv` as a Playwright test artifact, length ≥ 1800 rows + header.
  - CLI: `npm run test` exits 0 with 386/386 (385 existing + 1 new parity-diff Vitest case using a synthetic stub). The synthetic-stub case loads the Playwright-emitted CSV, runs the same fixture through a pure-TS loop calling `physics-core/step.ts`, diffs column-by-column at `|Δ|<1e-6` per scalar (shortest-arc for angles).
  - [ ] P2.1 Add `Aircraft.getTrajectory()` method backed by a fixed-size ring buffer (capacity 1800; one slot written per physics tick from `src/main.ts`'s onFixedTick). Ring buffer impl is intentional — 1800 entries × ~88 bytes is ~160KB, negligible. **Buffer must be exposed only when `?debug=true` is set**, consistent with the existing `window.__aircraft` pattern.  <!-- status: NOT-STARTED -->
  - [ ] P2.2 Wire the ring buffer to `window.__aircraft.getTrajectory()` (read-only — returns a defensive copy or a sliced view; whichever is cheapest while preventing mutation by test code).  <!-- status: NOT-STARTED -->
  - [ ] P2.3 Add `tests/e2e/parity.spec.ts`: loads `/?mission=free-flight&debug=true`, deterministically sets fixture initial state via a new debug hook `window.__aircraft.runFixture({position, linvel, throttle, ticks})` that pauses normal control input, resets the body to the fixture state, advances exactly `ticks` fixed-dt steps, then returns the trajectory. Writes CSV to `test-results/browser-trajectory-throttle-mid.csv`. Fixture: position `(0,50,0)`, linvel `(0,0,-30)`, throttle `0.15`, ticks `1800`.  <!-- status: NOT-STARTED -->
  - [ ] P2.4 Add `tests/parity-diff.test.ts` (Vitest): reads the Playwright-emitted CSV if present (skip with explanatory log otherwise — `test:e2e` runs may not have happened yet in some CI contexts; Vitest must still pass in that case). Re-runs the same fixture through a pure-TS loop calling `physics-core/step.ts` and the parser-loaded `aircraft.json`. Diffs row-by-row, column-by-column. Tolerance `|Δ| < 1e-6` per scalar; angles use shortest-arc distance `min(|a-b|, 2π-|a-b|)`. Fails on first divergent row, reporting which column + tick.  <!-- status: NOT-STARTED -->
  - [ ] P2.5 Add a tiny helper `tests/parity-fixtures.ts` exporting the fixture set (only `throttle-mid` at this WP; WP14.7 adds `throttle-low` and `throttle-high`). Shared between Playwright and Vitest so they probe identical inputs.  <!-- status: NOT-STARTED -->
  - [ ] verify-auto  <!-- status: NOT-STARTED -->
  - [ ] verify-self  <!-- status: NOT-STARTED -->
  - [ ] verify-human  <!-- status: NOT-STARTED -->
  - [ ] verify-codify  <!-- status: NOT-STARTED -->

- [ ] Phase 3: Conventions doc + verify-self in browser + finalize  <!-- status: NOT-STARTED; depends on Phase 2 -->
  **Observable outcomes:**
  - CLI: `cat CONVENTIONS.md | grep -A 6 "physics-core"` returns the new boundary-rule paragraph.
  - Browser: `npm run dev` + `/?debug=true` runs WP9.6's casual-flight pathway (the regression anchor): aircraft state finite + moving at 5s; zero NaN/Infinity in console; no JS errors. **Implicitly covered by `tests/e2e/casual-flight.spec.ts`** — Phase 3 is mostly the "did we accidentally break anything" sweep + docs polish.
  - CLI: full check sweep — `npm run test` 386/386 + `npm run test:e2e` 10/10 + `npx tsc --noEmit` clean + `npm run build` clean. All green.
  - [ ] P3.1 Append a short paragraph to CONVENTIONS.md documenting the `src/aircraft/physics-core/` boundary. Rule (codified): "Modules under `physics-core/` must be importable from Node (no DOM globals, no `requestAnimationFrame`, no `window.*`, no rendering Three.js classes — `Vector3` and `Quaternion` are math-only and allowed). Modules outside `physics-core/` may freely use Three.js rendering primitives and DOM. The split criterion is **'requires a browser API to run'**, NOT the literal text 'imports three' — Three.js exports pure-math classes that work in Node." Cross-link to arch.md Rev 2026-05-12 (afternoon) §D14.2.  <!-- status: NOT-STARTED -->
  - [ ] P3.2 verify-self via the project's live-system probe pattern — boot `npm run dev`, hit `/?debug=true`, confirm aircraft flies and `window.__aircraft.getTrajectory()` populates over 30s (proves the ring buffer fills and the hook works end-to-end outside the parity spec).  <!-- status: NOT-STARTED -->
  - [ ] P3.3 Final tsc strict + build sweep; one commit groups the Phase 3 doc + any micro-fixes.  <!-- status: NOT-STARTED -->
  - [ ] verify-auto  <!-- status: NOT-STARTED -->
  - [ ] verify-self  <!-- status: NOT-STARTED -->
  - [ ] verify-human  <!-- status: NOT-STARTED -->
  - [ ] verify-codify  <!-- status: NOT-STARTED -->

## Current Node

- **Path:** WP14.6 > Phase 2 > P2.1
- **Active scope:** Phase 1 COMPLETE (all leaves [x] including verify-codify with step.test.ts +4); next is Phase 2 — browser trajectory hook + parity test with synthetic stub.
- **Blocked:** none
- **Unvisited:** Phase 2, Phase 3
- **Open discoveries:** none (1 triage entry documented for the f32-precision-loosening fix; no production impact, no SURFACE needed)
- **Blocked:** none
- **Unvisited:** Phase 1 verify-self/verify-human/verify-codify; Phase 2 (browser trajectory hook + parity test); Phase 3 (conventions doc + verify-self + finalize)
- **Open discoveries:** none — file move was clean; one importer (`src/mission/hooks/registry.ts`) was missed at plan-time grep and caught by tsc, no surface needed.

## Discoveries

<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry also logged to workflow/backlog.md -->

(none yet — populated as the WP progresses)

## Test Triage — step.test.ts "advances the world by exactly one tick and sets world.timestep to dt"
Classification: Obsolete test — over-specified precision in a newly-authored test (Phase 1 verify-codify). The assertion `toBeCloseTo(1/60, 10)` expected ~5e-11 tolerance; Rapier's `world.timestep` setter round-trips through f32 producing ~8.7e-10 error.
Confidence: high
Evidence: Vitest output shows `expected 0.01666666753590107 to be close to 0.016666666666666666, received difference is 8.692344032057431e-10` — f32 bit-pattern truncation of f64 `1/60`. Production sequence (`world.timestep = dt; world.step()` in `src/main.ts:86-87`) already does this; no real-world correctness implication. The other 3 step.test.ts cases pass with looser precision and exercise the actual contract (one-tick advancement, thrust application, determinism vs. inline path).
Action: Loosen the assertion to `toBeCloseTo(dt, 6)` (~5e-7 tolerance, comfortably above f32 round-trip). No production code change.

## Plan-time notes / acceptance gates

**Final acceptance gates (binding for verify-codify at Phase 3 close):**
- 386/386 Vitest pass (385 existing + 1 new parity-diff)
- 10/10 Playwright (9 existing + 1 new parity emitter)
- `npx tsc --noEmit` strict clean
- `npm run build` clean
- `git mv`-based moves so file history survives
- CONVENTIONS.md has the new boundary rule paragraph
- No behavioral changes to gameplay or physics — WP9.6's casual-flight regression anchor continues green

**Out of scope at WP14.6 (binding for the next WPs):**
- Node Rapier-WASM harness — that's WP14.7
- All three fixtures (low/mid/high throttle) — WP14.7 extends from mid-only
- Score function and optimizer — WP14.8
- Actually tuning β5 — WP14.5-retry

**Why the synthetic-stub parity test is enough at WP14.6:** the stub proves `physics-core/` modules are callable outside the browser, which is the load-bearing structural assertion. Bit-identity to browser via a pure-TS loop ALSO catches the most likely regression class (someone accidentally imports a Three.js rendering primitive into `physics-core/`). WP14.7 will swap the stub for the real Node Rapier-WASM harness, at which point the parity test becomes the cross-WASM-boundary determinism guard the arch revision actually wants.
