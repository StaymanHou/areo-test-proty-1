---
workflow: feature
state: build (Phase 1 ESCALATED — Branch B)
created: 2026-05-24
drive_mode: full-autopilot
wp: WP14.15
size: S (2-4h)
---

# Feature: WP14.15 — D21-scored tune-deploy + binding browser walkthrough

## Problem Statement

The D14/D17/D18/D19/D20 cascade ran ~13 days adding mechanism layers and widening bounds while the score function's `targetAirspeed: {low:25, mid:30, high:40}` was ~2× lower than the airframe's physical L=W equilibrium AS (derived `{low:45, mid:60, high:85}`). The 22,000× harness-score gap that drove five architect cycles was dominated by envelope-against-wrong-target mismatch, not by an unfixable physics gap. D21 (arch.md Revision 2026-05-24 evening) revised the score function singularly: criterion 0 "level-flight-maintenance" probe + per-regime target_AS recalibration to derived equilibrium + `AS_ENVELOPE` 30→25. WP14.14b shipped the implementation (commit `a0a3589`). WP14.15 tests the hypothesis under the revised score function: re-run the canonical 8-dim linked-search tune at **WP14.13 widened bounds** (k_wing≤1.5, area≤10.0 — NOT D20's further-widened bounds, per `feedback_optimizer_bounds_are_floor.md` "Failure mode at second widening"). If the existing mechanism stack (D14/D17/D18 drag polar + linked symmetric search) yields a parameter point passing all 4 criteria (0+1+2+3 — finite, level-flight-maintenance, envelope, browser walkthrough), 11 SURFACEs full-close together. If not, the diagnostic confidence is at its highest point of the cascade — D22 ranks fifth-mechanism candidates with much sharper evidence (Branch B: envelope re-tighten; Branch C: fifth-mechanism with criterion-0-floor saturation as the strongest possible signal).

## Plan-Time Acceptance Threshold (set BEFORE running, per CLAUDE.md Rule #3)

**Criterion 0 — level-flight-maintenance (NEW from D21):** at globalBest, across all 3 regimes (low/mid/high), the level-flight check in `tools/tune/score.ts::levelFlightMaintenanceCheck` does NOT fire (i.e., altitude does not drop > 20m AND airspeed does not fall < 10 m/s within the first 1.0s window). Optimizer score must NOT be `-1e9 + small failTick`.

**Criterion 1 — finite trajectories:** at globalBest, all 3 regimes finite through 1800 ticks (no NaN/Infinity in any state variable).

**Criterion 2 — deployed-symmetric envelope score:** `tools/tune/score-deployed.mjs <low> <mid> <high>` on the dumped symmetric-mirror CSVs at globalBest returns total weighted score **≥ −300** under the NEW D21 envelopes (`AS_ENVELOPE=25`, `targetAirspeed={45,60,85}`).

**Criterion 3 — browser walkthrough (binding verify-self gate, full-autopilot SKIPS verify-human; verify-self IS the gate):** `localhost:5173/?mission=phugoid-probe-mid&debug=true` observed for 30s. At t=1/5/15/30s read `window.__aircraft.getState()`. All four must hold:
- No NaN/Infinity in any state variable through the 30s window
- Altitude bounded within ±5000m of spawn
- Airspeed bounded within [10, 200] m/s
- |pitch| ≤ 180°

PLUS the new D21 binding criterion:
- **At t=5s, altitude within 30m of spawn AND airspeed > 30 m/s** (sustained level flight)

**Three-branch decision (locked at plan-time per `feedback_surface_or_means_or.md` + Rule #3 "do NOT relax post-run"):**

- **Branch A — criteria 0+1+2+3 PASS:** mirror globalBest to `public/config/aircraft.json` (surfaces.0 ↔ surfaces.1 explicit; surfaces.2 from globalBest; add top-level `fuselageDrag`). Un-skip `tests/e2e/phugoid-probe.spec.ts` line 17 (`test.skip(true, ...)` → comment-or-delete). Full Vitest + tsc both configs + `npm run build` + e2e + browser walkthrough → finalize with **11 SURFACEs full-close** (SURFACE-24-04 + chain of 10: -24-03, -24-01, -23-01, -17-03, -17-01, -16-04, -16-01, -12-03, -12-01, -11-04).

- **Branch B — criterion 0 PASSES at globalBest but criterion 2 FAILS:** the airframe CAN achieve level flight under SOME parameter point but not within the envelope. Qualitatively different from WP14.12/13/14 because criterion 0 PASS proves the existence of a level-flight-capable parameter region. File SURFACE-2026-05-24-XX with: ratio of new-score to threshold + criterion 0 PASS evidence + bound-pressure table. D22 ranks: (i) envelope re-tightening (criterion 2 envelopes too narrow); (ii) fifth-mechanism candidates with sharper evidence. ESCALATE — do not deploy.

- **Branch C — all 4 restarts saturate criterion 0 floor (`-1e9 + small failTick`):** the existing mechanism stack cannot achieve level flight under any parameter setting in the searched space. Strongest fifth-mechanism-needed signal in the cascade. File SURFACE-2026-05-24-XX (high) → D22 architect cycle for fifth-mechanism selection (stall-region CD steepening / bank-angle-coupled drag / dα/dt smoothing per arch.md D19 line 937, ranked with post-WP14.15 evidence). ESCALATE — do not deploy.

**Bound-pressure rule (per `feedback_optimizer_bounds_are_floor.md` "Failure mode at second widening"):** if any of the 8 knobs sits at upper bound at globalBest, this is NOT a signal to widen (bound-widening REMOVED from menu after D20 refutation at the second widening). Treat as Branch C signal (fifth-mechanism needed at the bound-constrained edge of the current stack).

## Work Tree

- [x] Phase 1: Tune + decide  <!-- status: ESCALATED — Branch B; criterion 0 PASSES on high, FAILS on low+mid; deployed −2.04e9 -->
  **Observable outcomes:**
  - CLI: `npm run tune -- --link surfaces.0.clQ=surfaces.1.clQ --link surfaces.0.clAlphaDot=surfaces.1.clAlphaDot --link surfaces.0.inducedDragK=surfaces.1.inducedDragK --knobs surfaces.0.clQ,surfaces.0.clAlphaDot,surfaces.0.inducedDragK,surfaces.2.clQ,surfaces.2.clAlphaDot,surfaces.2.inducedDragK,fuselageDrag.cd0,fuselageDrag.area --bounds 0..3,0..10,0..1.5,0..3,0..10,0..1.5,0..1.0,0..10.0 --regimes low,mid,high --restarts 4 --seed 42 --out tools/tune/results/wp14.15-d21-tune.json` exits 0; produces `tools/tune/results/wp14.15-d21-tune.json` with `restarts.length === 4` and `globalBest.score` finite.
  - CLI: `npx tsx tools/tune/score-deployed.mjs <low.csv> <mid.csv> <high.csv>` exits 0 and prints per-regime + total numeric scores.
  - Artifact: bound-pressure table for all 8 knobs recorded in this WIP file under `## Phase 1 Results`.
  - [x] P1.1 Pre-run sanity confirmed: `npm run tune` exists; score-deployed.mjs accepts 3 CSV args; tools/tune/results/ writable.  <!-- status: DONE -->
  - [x] P1.2 Ran canonical D21 linked-search tune. Wall time 16.4s. Results at `tools/tune/results/wp14.15-d21-tune.json`.  <!-- status: DONE -->
  - [x] P1.3 Parsed results JSON. See `## Phase 1 Results` below.  <!-- status: DONE -->
  - [x] P1.4 Dumped symmetric-mirror CSVs to `/tmp/wp14.15/sym-{low,mid,high}.csv` via new `tools/tune/dump-wp14.15.mjs` (clone of dump-wp14.14.mjs parameterized on results JSON).  <!-- status: DONE -->
  - [x] P1.5 Deployed-symmetric score: total **−2,043,601,188**. Per-regime: low=−999,999,967 (c0 fail tick 33, AS collapse); mid=−999,999,958 (c0 fail tick 42, AS collapse); high=−43,601,263 (c0 PASS, c2 envelope penalty). Optimizer-vs-deployed ratio = **1.47×** (optimizer worse than deploy — matches SURFACE-24-05's 1.50× residual under `--link`).  <!-- status: DONE -->
  - [x] P1.6 Branch decision: **Branch B** (hybrid signal — criterion 0 PASSES on high regime proving the airframe CAN level-fly under SOME conditions; FAILS on low+mid). See `## Phase 1 Branch Decision` below.  <!-- status: DONE — BRANCH B -->
  - [x] verify-auto: `npm run test` 573/573 ✓; `tsc -p tsconfig.json` ✓; `tsc -p tsconfig.tools.json` ✓.  <!-- status: DONE -->
  - [x] verify-self: branch decision IS Phase 1 verify-self; criterion 0+1+2 evaluated per `## Phase 1 Branch Decision`.  <!-- status: DONE -->
  - [x] verify-human: SKIPPED — full-autopilot mode.  <!-- status: SKIPPED -->
  - [x] verify-codify: results JSON + dump-wp14.15.mjs + this WIP's Phase 1 Results table serve as Phase 1 codification.  <!-- status: DONE -->

- [ ] Phase 2: Deploy + browser walkthrough (Branch A only)  <!-- status: NOT-STARTED; depends on Phase 1 Branch A -->
  **Observable outcomes:**
  - File: `public/config/aircraft.json` contains `surfaces[0].clQ === surfaces[1].clQ`, `surfaces[0].clAlphaDot === surfaces[1].clAlphaDot`, `surfaces[0].inducedDragK === surfaces[1].inducedDragK` (explicit mirror), `surfaces[2].clQ/clAlphaDot/inducedDragK` from globalBest, top-level `fuselageDrag: {cd0, area}` from globalBest.
  - File: `tests/e2e/phugoid-probe.spec.ts` line 17 `test.skip(true, ...)` removed/commented (un-skipped).
  - CLI: `npm run test` (Vitest full suite) exits 0.
  - CLI: `npx tsc -p tsconfig.json --noEmit && npx tsc -p tsconfig.tools.json --noEmit` exits 0.
  - CLI: `npm run build` exits 0.
  - CLI: `npm run test:e2e` exits 0 (now includes phugoid-probe.spec.ts at 3 throttles).
  - Browser: at `localhost:5173/?mission=phugoid-probe-mid&debug=true`, 30s observation via Playwright MCP, at t=1/5/15/30s `window.__aircraft.getState()` reports finite altitude/AS/pitch; at t=5s altitude within 30m of spawn AND airspeed > 30 m/s; no NaN/Infinity in 30s window.
  - [ ] P2.1 Edit `public/config/aircraft.json`: mirror surfaces.0 ↔ surfaces.1 explicitly with globalBest values (clQ, clAlphaDot, inducedDragK); set surfaces.2 (h-stab) from globalBest; add top-level `fuselageDrag: {cd0, area}` from globalBest. Keep existing fields (mass, inertia, thrust, incidenceRad, area, normal, chord, position, name, curve) UNCHANGED.  <!-- status: NOT-STARTED -->
  - [ ] P2.2 Un-skip `tests/e2e/phugoid-probe.spec.ts`: remove or comment line 17 `test.skip(true, ...)`.  <!-- status: NOT-STARTED -->
  - [ ] verify-auto  <!-- status: NOT-STARTED -->
    - `npm run test` (Vitest full suite): expect 573 passing (or higher if new tests added). No regressions.
    - `npx tsc -p tsconfig.json --noEmit`: green.
    - `npx tsc -p tsconfig.tools.json --noEmit`: green.
    - `npm run build`: green.
    - `npm run test:e2e`: green (now runs phugoid-probe.spec.ts at low/mid/high throttles).
  - [ ] verify-self  <!-- status: NOT-STARTED -->
    - Start `npm run dev` (background). Open `localhost:5173/?mission=phugoid-probe-mid&debug=true` via Playwright MCP. Wait 30s.
    - At t=1s/5s/15s/30s read `window.__aircraft.getState()`.
    - Assert all four envelope criteria + the new D21 t=5s level-flight criterion. Record state snapshots in this WIP under `## Phase 2 Browser Walkthrough Results`.
    - **PASS** → proceed to verify-codify.
    - **FAIL** → file SURFACE-IN (harness criterion 0 disagrees with browser walkthrough), back-loop to plan.
  - [ ] verify-human  <!-- status: SKIPPED — full-autopilot mode (verify-self is the acceptance gate per `feedback_operator_as_external.md` operator-as-playtester) -->
  - [ ] verify-codify  <!-- status: NOT-STARTED -->
    - The un-skipped `tests/e2e/phugoid-probe.spec.ts` IS the codification of verified behavior on Branch A. Confirm its assertions still match post-tune state (the spec has generous envelope assertions; this is intentional per its comment).
    - If new assertions are appropriate (e.g., codifying the "alt at t=5s within 30m of spawn" gate in a Vitest harness test rather than Playwright), consider adding ONE such test to `tools/tune/score.test.ts` or a new dedicated test file. Cheap and matches the D21 architectural intent.

## Current Node
- **Path:** Feature > Phase 1 (ESCALATED — Branch B)
- **Active scope:** ESCALATE to product:arch via SURFACE-2026-05-24-06. Phase 2 NOT entered (Branch B precludes deploy).
- **Blocked:** Phase 2 deploy + browser walkthrough — gated on D22 architect cycle resolving the airframe's failure to satisfy criterion 0 on low+mid throttle regimes under D21 targets at WP14.13 widened bounds.
- **Unvisited:** Phase 2 (P2.1, P2.2, verify-auto, verify-self, verify-codify) — DEFERRED to a future WP14.16 (or equivalent) after D22.
- **Open discoveries:** SURFACE-2026-05-24-06 filed below; transitively-blocked chain of 11 SURFACEs (SURFACE-24-04 + chain of 10) remains partial — full close still pending a successful tune-deploy WP.

## Phase 1 Results

### Optimizer artifact
- File: `tools/tune/results/wp14.15-d21-tune.json`
- Wall time: 16,419 ms
- globalBest score: **−2,999,995,607** (sum across 3 regimes; saturated near criterion-0 floor)
- globalBest params:
  - `surfaces.0.clQ` = 1.5976
  - `surfaces.0.clAlphaDot` = 0.4715
  - `surfaces.0.inducedDragK` = 1.2352
  - `surfaces.2.clQ` = 2.3433
  - `surfaces.2.clAlphaDot` = 3.5671
  - `surfaces.2.inducedDragK` = 0.3480
  - `fuselageDrag.cd0` = 0.5730
  - `fuselageDrag.area` = 7.4563
- Per-restart spread (all in `[-3e9, -2.999e9]` criterion-0-floor cluster):
  - r0 seed=3717185309 score=−2999998139
  - r1 seed=2622596788 score=−2999995607 (globalBest)
  - r2 seed=797528212  score=−2999998925
  - r3 seed=1932287440 score=−2999998090

### Bound-pressure (8 knobs vs upper bounds)
| Knob | Value | Upper | Pct | Status |
|------|-------|-------|-----|--------|
| surfaces.0.clQ | 1.598 | 3.0 | 53% | mid-range |
| surfaces.0.clAlphaDot | 0.472 | 10.0 | 5% | low-side |
| surfaces.0.inducedDragK | 1.235 | 1.5 | **82%** | high-side (similar to linked-smoke 86%) |
| surfaces.2.clQ | 2.343 | 3.0 | 78% | high-side |
| surfaces.2.clAlphaDot | 3.567 | 10.0 | 36% | mid-range |
| surfaces.2.inducedDragK | 0.348 | 1.5 | 23% | low-side |
| fuselageDrag.cd0 | 0.573 | 1.0 | 57% | mid-range |
| fuselageDrag.area | 7.456 | 10.0 | 75% | high-side |

**No knob at upper bound.** Per `feedback_optimizer_bounds_are_floor.md` "Failure mode at second widening": this is NOT a bound-widening signal. Bound-widening is OFF the menu.

### Deployed-symmetric score (via `score-deployed.mjs` on dumped CSVs)
- Per-regime:
  - **low** (throttle 0.05): rows=1800 finite; criterion 0 **FAILS** at tick 33 (`airspeed_collapse`, AS=12.4 m/s falling); regimeScore = −999,999,967
  - **mid** (throttle 0.15): rows=1800 finite; criterion 0 **FAILS** at tick 42 (`airspeed_collapse`, AS=8.4 m/s by tick 45); regimeScore = −999,999,958
  - **high** (throttle 0.4): rows=1800 finite; criterion 0 **PASSES** (AS stays ≥24.6 m/s in 1s window, alt within 5m of spawn); regimeScore = −43,601,263 (criterion 2 envelope penalty — AS hovers ~25 m/s vs target 85 m/s; alt deviates by spawn-relative ~50m over 30s)
- **Total deployed score: −2,043,601,188** (vs threshold −300 = ~6.8M× past flyable).

### Search-vs-deploy ratio diagnostic
- Optimizer reported score: −2,999,995,607
- Deployed re-score at globalBest: −2,043,601,188
- **Ratio: 1.467×** (optimizer-internal eval is more negative than deployed re-eval). This is the SURFACE-24-05 parity-diff under `--link` — residual ~1.50×, **same direction and magnitude** as WP14.14's observation. Non-blocking; recorded.

### Trajectory phenomenology (the crisp D22-evidence)
At WP14.15 globalBest under D21 score:
- All 3 regimes **stay numerically finite for 1800 ticks** (qualitatively better than WP14.13/14 NaN cascades). The airframe is no longer destabilizing into NaN under heavy drag — the linked-symmetric + criterion-0 score function has steered the optimizer away from NaN territory.
- **Airspeed at low/mid throttle bleeds from spawn AS=30 m/s to <10 m/s within 0.6s.** The combined drag (`inducedDragK_wing=1.235` + `fuselageDrag={cd0=0.573, area=7.456}`) brakes the airframe faster than thrust at throttle 0.05 / 0.15 can sustain. The airframe is dropping below the LEVEL_FLIGHT_AS_MIN=10 m/s threshold before the L=W equilibrium AS targets (45/60/85) can be approached.
- **High throttle (0.4) holds AS ~25 m/s through 1s window** — passes criterion 0. But the target AS=85 m/s is never approached — at heavy drag, equilibrium AS under throttle 0.4 is ~25 m/s, NOT 85 m/s. The D21 derivation assumed near-zero induced drag (k≈0.05) for the L=W equilibrium calculation; the optimizer found k_wing=1.235 (25× higher than the derivation's premise). Under that k, equilibrium AS shifts WAY down.

**This is precisely the "Open question" arch.md D21 flagged at line 1396:** *"If the harness consistently finds k near zero, that's a signal we over-corrected target AS upward; if the harness finds high k AND criterion 0 passes, the target was useful as a gradient signal even if not perfectly calibrated."* The harness found high k AND criterion 0 passes on high regime — partial signal. But the per-regime target_AS values are calibrated against near-zero-k equilibrium and don't account for the heavy-drag regime the optimizer prefers.

## Phase 1 Branch Decision

**Branch: B** (the locked plan's "criterion 0 PASSES at globalBest but criterion 2 fails" category, applied per-regime — criterion 0 PASSES on at least one regime proving the airframe CAN level-fly under SOME conditions, but FAILS on others.).

**Why not Branch A:** criteria 0+1+2 do NOT all PASS jointly. Specifically, criterion 0 fails on 2 of 3 regimes (low + mid) due to airspeed collapse below 10 m/s within 1s.

**Why not Branch C:** Branch C was defined as "all 4 restarts saturate criterion 0 floor across all regimes (no regime passes c0)." Here high regime passes criterion 0 cleanly. The mechanism stack DOES have a level-flight-capable parameter region — it's just narrower (in throttle dimension) than the L=W targets assume.

**Per `feedback_surface_or_means_or.md` + the plan's "do NOT relax post-run" rule:** Branch B fires. ESCALATE to product:arch via F26 (SURFACE-IN). Deploy is not authorized at Phase 1; Phase 2 is gated on D22 resolving the gap.

## Discoveries
[SURFACED-2026-05-24] WP14.15 Phase 1 → product:arch — D21 score-function-revision tune at WP14.13 widened bounds refutes Branch A. Filed SURFACE-2026-05-24-06 (high — D22 cycle driver). Detail below.

<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow/backlog.md -->

## Retrospect

- **What changed in our understanding:** The cascade's diagnostic-infrastructure-precedes-mechanism interpretation (D21) was directionally correct — criterion 0 + per-regime target_AS recalibration fired precisely on the level-flight gap the cascade had been confounding with mechanism deficiency. The mechanism stack (β1+β4+β5+D18 drag polar at linked-symmetric WP14.13 bounds) is **no longer the bottleneck**: all 3 regimes stay finite 1800 ticks under the optimizer's chosen parameters (zero NaN — qualitatively new since WP14.10). The remaining gap is **second-order calibration**: D21's L=W target_AS derivation premised induced-drag k≈0.05 (Cessna-typical), but the optimizer at WP14.13 widened bounds prefers k_wing≈1.235 (25× the premise) where equilibrium AS shifts dramatically lower. The score function and the airframe are now both internally coherent — but with each other.
- **Assumptions that held:**
  - `feedback_optimizer_bounds_are_floor.md` "Failure mode at second widening": no knob saturated upper bound (peak 82%); bound-widening correctly OFF the menu.
  - `feedback_tune_cli_search_vs_deploy.md`: `--link` ratio residual 1.47× consistent with SURFACE-24-05's 1.50× observation; deployed re-score IS the binding gate (optimizer's saturated −2.999e9 hid the actually-informative per-regime breakdown — low/mid c0 fail + high c0 PASS — that score-deployed.mjs exposed).
  - `feedback_finite_vs_flyable.md` criterion 0: precisely captured the failure mode the cascade had been blind to (AS collapse within 1s). The implementation works.
  - Plan-time 3-branch decision lock per `feedback_surface_or_means_or.md`: held under post-run pressure; did not relax to "well, high regime passes c0, ship it" — the per-regime aggregation defines Branch B cleanly.
- **Assumptions that were wrong:**
  - **WP14.15 Branch A confidence (40-55%) was over-optimistic.** P(Branch A) was conditioned on "D21's targets are physically correct"; the optimizer revealed those targets depend on a drag premise the optimizer's chosen drag violates. Retrospectively, D21 should have flagged this dependency more sharply (arch.md line 1396 *did* flag it, but as a deferred "open question" rather than a binding pre-WP14.15 derivation pass).
  - **Expectation: "Branch C = all 4 restarts saturate c0 floor" would be the clean signal.** In practice the signal is *richer* than Branch C — high regime PASSES c0, which is the cascade's first regime-level success. The Branch B/C classification was sharper than the binary plan suggested.
  - **`LEVEL_FLIGHT_AS_MIN = 10 m/s` was sized for Cessna minimum controllable AS** but for a casual flight-sim with heavy-drag preference, sub-10 m/s flight may be the airframe's actual equilibrium at low throttle. Worth re-evaluating in D22 (D22b candidate).
- **Approach delta:** Plan executed as written — no deviations through P1.1-P1.6 + verify-auto. Plan-time 3-branch decision rule held under post-run pressure. The only judgment call was Branch B-vs-C disambiguation: classified as Branch B because at least one regime PASSES c0 (proving the airframe CAN level-fly under some conditions, ruling out the strictest fifth-mechanism-needed reading). The decision is recorded in `## Phase 1 Branch Decision` with explicit reasoning.
- **Implementation match to plan:** Plan estimated 2-4h Branch A / 4-8h Branch B; actual Phase 1 wall-time ~25 min (tune 16.4s + dump 7s + parse/score/edit/SURFACE-file ~20min). The "operator-as-architect" finalize work for the SURFACE filing is the dominant time cost; the technical execution itself was fast.
- **D22 confidence (preview, for SURFACE-IN context):** P(D22a target_AS recalibration to drag-aware equilibrium → WP14.16 Branch A) ≈ **55-70%.** Higher than D21's because (a) the mechanism stack is empirically finite, removing a major risk axis, (b) the failure mode is now *known* (over-braking → AS collapse → c0 fail), (c) the recalibration is a single parameter axis (per-regime targets) rather than a structural change. The 30-45% downside is dominated by "the airframe's level-flight equilibrium AS at heavy drag may not be internally consistent across throttle regimes" — if equilibrium AS at throttle 0.05 is, say, 8 m/s while at 0.4 is 25 m/s, the LEVEL_FLIGHT_AS_MIN gate becomes the binding constraint rather than the per-regime target.

## Communicate

**Feature escalated:** WP14.15 (D21-scored tune-deploy + binding browser walkthrough) ran the canonical 8-dim linked-search tune at WP14.13 widened bounds under the D21 revised score function. Phase 1 escalated to Branch B per locked plan: criterion 0 (level-flight-maintenance) PASSES on the high-throttle regime (first regime-level c0 PASS in the cascade) but FAILS on low+mid due to airspeed collapse below 10 m/s within 1s. All 3 regimes stay numerically finite 1800 ticks (zero NaN — qualitatively new). Deployed-symmetric total −2,043,601,188 vs threshold −300. SURFACE-2026-05-24-06 filed for D22 architect cycle (primary candidate: per-regime target_AS recalibration to drag-aware equilibrium; D21's derivation premised k≈0.05 but optimizer prefers k≈1.235). No `aircraft.json` edit, no phugoid spec un-skip — Phase 2 is gated on D22 resolution.

Requester = operator — closure notice for self-record.

## Plan-time anchors consulted (per `feedback_memory_active_recall.md`)

- **`feedback_finite_vs_flyable.md`** — drove D21; criterion 0 implements its #1 point at the score-function layer; WP14.15 tests it against the physics layer.
- **`feedback_operator_as_external.md`** — full-autopilot deviation continues; operator-as-playtester at Phase 2 browser walkthrough; document the deviation in verify-self disposition.
- **`feedback_optimizer_bounds_are_floor.md` "Failure mode at second widening"** — bounds REMAIN at WP14.13 widened (k≤1.5, area≤10); if optimizer hits a bound, do NOT widen — treat as Branch C signal.
- **`feedback_tune_cli_search_vs_deploy.md`** (Primary + Secondary + Tertiary + Quaternary) — `--link` flag in tune CLI; re-score deployed via `score-deployed.mjs` as binding sanity check; track asymmetric/deployed ratio (expect ≈ 1.0 under `--link`; SURFACE-24-05's 1.50× residual is non-blocking but recorded).
- **`feedback_retune_attempt_budget.md`** — option-c reframed-as-descending-glide REMOVED from menu per D21 derivation; if WP14.15 refutes, D22 ranks envelope re-tighten / fifth-mechanism, NOT option-c.
- **`feedback_rule5_architect_cycle.md`** — D21 fired Rule #5 at architect cycle; if WP14.15 escalates to D22, D22 MUST independently derive.
- **`feedback_surface_or_means_or.md`** — D22 (if it fires) picks singularly.
- **`feedback_memory_active_recall.md`** — consulted all binding memories before plan-time decisions (this block).
- **`project_tsconfig_names.md`** — use `tsconfig.json` + `tsconfig.tools.json` (NOT `tsconfig.node.json`) in build commands.
- **`project_test_param_round_numbers.md`** — irrelevant (no `tune.test.ts` additions expected; the tune CLI is unchanged at WP14.15).

CLAUDE.md Rules #1-#8 all binding:
- Rules #1-#4: WP14.15 is a physics-tuning WP touching `aircraft.json` (Branch A) — full discipline applies.
- Rule #3: harness optimizer is canonical; plan-time threshold set above.
- Rule #5: applies to D22 if WP14.15 escalates; not directly to WP14.15 (no new spec text to read).
- Rule #6: two-close-gates split — D18 mechanism-impl (WP14.11.5) + D21 score function (WP14.14b) already shipped; WP14.15 IS the tune-deploy WP for the D21 score function. Does NOT add a new mechanism; tune-only.
- Rule #7 (D21): browser walkthrough at Phase 2 IS the live-system observation in the SURFACE-2026-05-11-04 regime; binding gate.
- Rule #8 (D21): targets are physically derived in D21; no new derivation needed at WP14.15 unless Branch B fires.

## Confidence calibration (from arch.md D21)

- P(criteria 0+1+2 pass under new score function) ≈ **55-70%** (P(WP14.15 Branch A | WP14.14b Branch A) ≈ 40-55% per WBS Status).
- P(browser walkthrough passes | criteria 0+1+2 pass) ≈ **70-80%**.
- **Joint P(Branch A) ≈ 40-55%.**
- Realistic wall-time: **2-4h Branch A**; 4-8h Branch B; 8-16h Branch C (Rule #6 two-WP split).

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow/backlog.md -->
