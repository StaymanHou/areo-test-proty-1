---
workflow: feature
state: ESCALATED-superseded-by-integrator-fix (2026-05-25)
superseded_by: WP14.19 (D24 implementation — arch.md Revision 2026-05-25)
superseded_at_commit: 46f9b42 (`fix-resetforces-bug` task close, 2026-05-24)
created: 2026-05-24
escalated: 2026-05-24 (Branch C — all 4 criteria FAIL; deployed -2.999e9 = full c0-floor; investigation spike commit `e77671c` found root cause = Rapier per-tick force accumulator never cleared; fix commit `46f9b42`)
entry: spec (complex feature — D23 cascade close gate)
drive_mode: full-autopilot
wbs_ref: WP14.18 (now ESCALATED-superseded; replaced by WP14.19)
arch_ref: arch.md Revision 2026-05-24 (night) — D23 (Revision 2026-05-25 — D24 supersedes this for cascade-close purposes)
closes_surfaces_on_branch_a:
  - SURFACE-2026-05-24-07
  - SURFACE-2026-05-24-06
  - SURFACE-2026-05-24-04
  - SURFACE-2026-05-24-03
  - SURFACE-2026-05-24-01
  - SURFACE-2026-05-23-01
  - SURFACE-2026-05-17-03
  - SURFACE-2026-05-17-01
  - SURFACE-2026-05-16-04
  - SURFACE-2026-05-16-01
  - SURFACE-2026-05-12-03
  - SURFACE-2026-05-12-01
  - SURFACE-2026-05-11-04
---

# Feature: WP14.18 — D23 cascade tune-deploy (Branch A close gate for 13 SURFACEs)

## Problem Statement

WP14.17 shipped the D23-γ-evolved per-regime score-function dispatch in `tools/tune/score.ts` (commit `c9c5733`) — tooling-only, no `aircraft.json` change. The mechanism stack (D14/D17/D16/D18/D19/D20/D21/D22 cascade) is structurally complete; production `aircraft.json` is at WP14.10 baseline (UNCHANGED through the entire cascade).

**Gap:** the D23 score function has never been driven by the optimizer. The hypothesis the architect cycle locked in: under the per-regime mode dispatch (low=controlled-descent, mid=slow-flight, high=level-cruise), the c0-floor attractor that has gamed every prior tune (WP14.14 / WP14.15 / WP14.16) is structurally undermined. The optimizer should now seek mode-appropriate trajectories rather than collapsing AS at low+mid to a 1-second-window artifact.

WP14.18 is the cascade close gate. Branch A — globalBest produces mode-appropriate trajectories at all 3 regimes + browser walkthrough passes the operator-as-playtester feel test — resolves **13 SURFACEs together** and unblocks Phase 2 mission content (WP15/WP16/WP17).

## User Stories

- **As the operator-as-architect,** I want WP14.18 to re-tune the same parameter space WP14.16 searched (no bound widening, no mechanism additions) but under the corrected score function, so that I can isolate whether the D23 reframe alone is sufficient to find a flyable globalBest.
- **As the operator-as-playtester,** I want to observe each throttle setting (low/mid/high) at `localhost:5173/?mission=phugoid-probe-mid&debug=true` for 30s and confirm the airframe behaves like a real aircraft in the expected mode — a Cessna at idle descends, at cruise levels off — so that the cascade closes on player-feel, not just numerical proxies.
- **As future-me on the next cascade,** I want the third bidirectional-bound-failure-mode observation persisted into `feedback_optimizer_bounds_are_floor.md` rule 2 so the lesson survives.

## Acceptance Criteria

**Branch A (the close path) — all 4 criteria PASS:**

1. **Criterion 0 — mode-specific c0 PASS at globalBest across all 3 regimes** (locked at plan-time per CLAUDE.md Rule #3):
   - Low (controlled-descent): AS finite + positive in first 1s, pitch within [-30°, +10°].
   - Mid (slow-flight-or-shallow-descent): AS within [25, 50] m/s in first 1s, pitch within ±20°.
   - High (level-cruise): D21's existing level-flight check — alt drop ≤ 20m, AS ≥ 10 m/s in first 1s.

2. **Criterion 1 — all 3 regimes finite 1800 ticks** at globalBest (no NaN, no Infinity). Should be a freebie given WP14.16 globalBest already satisfied this; mechanism stack confirmed sufficient.

3. **Criterion 2 — deployed-symmetric total score ≥ −300** under D23-γ-evolved envelopes via `tools/tune/score-deployed.mjs`. This is the harness-level "mode-appropriate behavior" gate.

4. **Criterion 3 — browser walkthrough binding gates per throttle** (operator-as-playtester gate per CLAUDE.md Rule #7 live-system observation + `feedback_operator_as_external.md`):
   - 30s observation at `localhost:5173/?mission=phugoid-probe-mid&debug=true` with each of the three `phugoid-probe-{low,mid,high}` missions selected via UI dropdown.
   - Per-mode numerical gates read via `window.__aircraft.getState()` at t={1, 5, 15, 30}s:
     - **Low:** sink rate in [1, 5] m/s at t=10s, AS finite, pitch within [-30°, +10°], no inverted attitude.
     - **Mid:** AS within [25, 50] at t=10s, sink rate in [-1, +3], pitch within ±20°.
     - **High:** altitude within 50m of spawn at t=10s, AS within ±25 of 85, pitch within ±15°.
   - **Casual-gamer-feel gate:** for each mode, operator answers yes to "does this look like an aircraft flying in the expected mode?" Failure on any single mode = Branch B.

**Branch B — criteria 1+2 PASS but browser walkthrough fails casual-gamer-feel:** file SURFACE-2026-05-25-XX with operator's specific feel observation, back-loop to plan, escalate to D24 architect cycle for per-mode envelope tuning OR per-mode mechanism additions. Do NOT proceed to Phase 2.

**Branch C — optimizer still gets stuck in c0-floor cluster OR `inducedDragK_wing` saturates ≥85% of 0.3 bound under the new score function:** file SURFACE-2026-05-25-XX, escalate to D24 for: (i) D23-δ fifth-mechanism longitudinal damper, (ii) per-mode envelope re-derivation, (iii) reframe of low-regime mode from "controlled-descent" to "engine-out-glide." **DO NOT widen the bound** — bidirectional bound-failure-mode rule binds.

**Bound-pressure sanity (binding regardless of branch):** record post-tune bound saturation on all 8 knobs. If `inducedDragK_wing` saturates ≥85% of 0.3 bound, that's the **fourth observation** of the loosen-constraint anti-pattern in this cascade — file SURFACE for D24, do NOT widen.

## Out of Scope

- **No `aircraft.json` change at the WP14.18 implementation phase before Branch A is confirmed.** Production `aircraft.json` mirror happens at the Branch A close commit; not before.
- **No bound changes.** D22-β narrowed bounds (same as WP14.16) are preserved verbatim. The tune command from WP14.16 is reused byte-for-byte.
- **No new mechanism additions.** D23 is score-function-only. The D23-δ fifth-mechanism longitudinal damper stays deferred to D24 contingency.
- **No new score-function additions.** WP14.17 already landed the D23-γ-evolved dispatch. WP14.18 consumes it; does not extend it.
- **No tune-CLI flag changes.** `--link` flag (commit `58badfe`) reused; no new flags.
- **No Vitest test additions for new physics behavior.** D23 is tooling-only; the 7 new Vitest cases from WP14.17 already exist. WP14.18 may add a Playwright regression anchor (un-skip `tests/e2e/phugoid-probe.spec.ts:17`) on Branch A, but not new Vitest.
- **No Phase 2 mission content (WP15/WP16/WP17) work.** Stays gated behind Branch A.

## Technical Constraints

- **Binding spec:** `docs/product/arch.md` Revision 2026-05-24 (night) D23 section + `docs/product/wbs.md` WP14.18 entry (lines 583–608). No deviations from the locked tune command, bounds, restarts, or seed.
- **Tune command (verbatim from WBS WP14.18):**
  ```
  npm run tune -- \
    --link surfaces.0.clQ=surfaces.1.clQ \
    --link surfaces.0.clAlphaDot=surfaces.1.clAlphaDot \
    --link surfaces.0.inducedDragK=surfaces.1.inducedDragK \
    --knobs surfaces.0.clQ,surfaces.0.clAlphaDot,surfaces.0.inducedDragK,surfaces.2.clQ,surfaces.2.clAlphaDot,surfaces.2.inducedDragK,fuselageDrag.cd0,fuselageDrag.area \
    --bounds 0..3,0..10,0..0.3,0..3,0..10,0..0.3,0..0.5,0..1.0 \
    --regimes low,mid,high --restarts 4 --seed 42 \
    --out tools/tune/results/wp14.18-d23-tune.json
  ```
- **Results JSON gitignored** per `.gitignore:40`. Dump script `tools/tune/dump-wp14.18.mjs` (cloned from `dump-wp14.16.mjs`) IS committed alongside `aircraft.json` per CLAUDE.md Rule #3 audit-trail intent.
- **CLAUDE.md Rules #1–#8 all binding.** Rule #3 (harness optimizer, not hand-tuning) — followed by definition. Rule #5 (plan-time physics derivation) — fires at plan: independently derive what mode-appropriate trajectories look like under D22-β drag at each throttle BEFORE running the tune. Rule #7 (live-system observation) — fires at the browser walkthrough. Rule #8 (score-function targets are physical quantities) — D23 refined this from "equilibrium AS" to "mode-appropriate behavior."
- **`tsconfig.json` + `tsconfig.tools.json`** for typecheck (per `project_tsconfig_names.md`). Not `tsconfig.node.json`.
- **`feedback_optimizer_bounds_are_floor.md` rule 2 memory update QUEUED for WP14.18 close** (per D23 arch revision generalizable-lesson section). Apply via `/session-store-learning` after finalize, BEFORE next session-pause. Three-observation threshold (D22 + WP14.16 saturation + D23-α-would-have-been) is met.

## Open Questions

- [ ] None blocking spec. All architecture decisions locked at D23 architect cycle (arch.md Revision 2026-05-24 night). All tooling pre-existing (tune CLI from WP14.6; score.ts D23 dispatch from WP14.17 commit `4386536`; `--link` flag from `tune-cli-link-flag` commit `58badfe`; score-deployed.mjs pre-existing). Working tree clean at WP14.17 finalize (commit `c9c5733`).
- [ ] **Confidence calibration (locked at plan-time, do NOT relax):** P(Branch A) ≈ 25–40% per arch.md. The cascade has consistently surfaced unexpected issues at each "we got it this time" moment; Branch B/C escalation paths are well-defined.

## Work Tree

- [ ] Phase 1: Re-tune at D22-β bounds under D23 score function + bound-pressure sanity  <!-- status: ESCALATED — Branch C; all 4 acceptance criteria FAIL; SURFACE-2026-05-24-08 filed; D24 architect cycle required -->
  **Observable outcomes:**
  - CLI: `npm run tune -- <verbatim WP14.16 command, --out tools/tune/results/wp14.18-d23-tune.json>` exits 0; results JSON written; `dump-wp14.18.mjs <results.json>` exits 0 and writes 3 CSVs (sym-low/mid/high) under `/tmp/wp14.18/` deterministic (md5-stable across re-runs).
  - CLI: `node tools/tune/score-deployed.mjs --json tools/tune/results/wp14.18-d23-tune.json` prints per-regime breakdown + total. Per-regime mode-specific c0 fields populated; bound-pressure column for all 8 knobs printed.
  - Console: optimizer's `globalBest.score` and per-restart spread printed; no `c0-floor` saturation across all 4 restarts (i.e., not all 4 restarts cluster within 1% of the c0-floor value).
  - [x] P1.1 Clone `tools/tune/dump-wp14.16.mjs` → `tools/tune/dump-wp14.18.mjs`
  - [x] P1.2 Rule #5 plan-time derivation memo (see `## Plan-time derivation`)
  - [x] P1.3 Run the canonical tune (best score -2.035e9; stdout at `/tmp/wp14.18/tune.log`)
  - [x] P1.4 Run `dump-wp14.18.mjs` (md5-stable; high regime NaN's at tick 1530)
  - [x] P1.5 Run `score-deployed.mjs` (deployed-symmetric total -2.999e9 = full c0-floor)
  - [x] P1.6 Bound-pressure audit (`inducedDragK_wing` saturates 92.4% — Branch C)
  - [x] P1.7 c0-floor cluster check (3 of 4 restarts at -3e9 c0-floor — Branch C)
  - [x] verify-auto  <!-- N/A — no new code paths beyond dump script; full Vitest preserved -->
  - [x] verify-self  <!-- Branch C: 4 of 4 acceptance criteria FAIL; bound-pressure 92.4%; c0-floor cluster 3/4 -->
  - [ ] verify-human  <!-- SKIPPED per Mode 4 full-autopilot -->
  - [ ] verify-codify  <!-- N/A — Branch C escalation, no new behavior to codify -->

- [ ] Phase 2: Mirror globalBest to aircraft.json + un-skip phugoid-probe e2e (Branch A only)  <!-- status: NOT-STARTED; depends on Phase 1 -->
  **Relevance check (before Phase 2):** TBD at Phase 2 entry — re-evaluate after Phase 1 results land. The decision branch (A vs B vs C) is the relevance gate; only Branch A proceeds to Phase 2 as written.

  **Observable outcomes:**
  - CLI: `git diff public/config/aircraft.json` shows updates to `surfaces.0`, `surfaces.1` (mirror of 0), `surfaces.2`, and top-level `fuselageDrag`. No other surfaces touched.
  - CLI: `npm run typecheck` exits 0 against both `tsconfig.json` and `tsconfig.tools.json` (per `project_tsconfig_names.md`).
  - CLI: `npm test` exits 0 with ≥583 passing (preserves WP14.17's 583/583 baseline; may add an un-skipped phugoid-probe regression anchor).
  - CLI: `npm run build` exits 0; produces `dist/` static files.
  - CLI: `npm run test:e2e` exits 0 including the now-un-skipped `phugoid-probe.spec.ts`.
  - File: `tests/e2e/phugoid-probe.spec.ts:17` no longer contains `test.skip(true, ...)`.
  - [ ] P2.1 Mirror globalBest → `public/config/aircraft.json`: write surfaces.0 from globalBest knobs (clQ, clAlphaDot, inducedDragK), copy to surfaces.1 (symmetric mirror), write surfaces.2 from globalBest, preserve all non-tuned fields (mass, thrust, areas, incidenceRad, etc.) AND top-level `fuselageDrag.{cd0, area}`. NO other field touched.  <!-- status: NOT-STARTED -->
  - [ ] P2.2 Un-skip `tests/e2e/phugoid-probe.spec.ts:17` — replace `test.skip(true, 'WP14.5 disposition...')` with a real test body that asserts trajectory finite over 30s + per-mode envelope gates from spec criterion 3. Add a header comment documenting the WP14.18 un-skip + cascade closure rationale.  <!-- status: NOT-STARTED -->
  - [ ] verify-auto  <!-- status: NOT-STARTED -->
  - [ ] verify-self  <!-- status: NOT-STARTED -->
  - [ ] verify-human  <!-- status: NOT-STARTED -->
  - [ ] verify-codify  <!-- status: NOT-STARTED -->

- [ ] Phase 3: Browser walkthrough — operator-as-playtester gate per throttle mode  <!-- status: NOT-STARTED; depends on Phase 2 -->
  **Relevance check (before Phase 3):** TBD at entry. Phase 3 only runs on Branch A (Phase 2 completed cleanly).

  **Observable outcomes:**
  - Browser: dev server up at `localhost:5173`. Playwright MCP navigates to `localhost:5173/?mission=phugoid-probe-low&debug=true`, runs for 30s, reads `window.__aircraft.getState()` at t={1,5,15,30}s. Repeats for mid and high.
  - Per-mode numerical assertions PASS (see spec criterion 3): low sink rate ∈ [1,5], mid AS ∈ [25,50], high alt within ±50 of spawn AS within ±25 of 85, all within mode pitch envelopes.
  - Console: no `NaN`, `Infinity`, or uncaught errors over 30s × 3 missions = 90s of in-browser observation.
  - Operator-as-playtester answers yes to "does this look like an aircraft flying in the expected mode?" for each of low/mid/high.
  - **Note (full-autopilot deviation):** the operator-as-playtester text-call is deferred to the operator at session-resume per `feedback_operator_as_external.md`; full-autopilot mode 4 SKIPs verify-human, but the numerical browser gates per criterion 3 run in verify-self via Playwright MCP. If verify-self numerical gates fail, that's Branch B/C without operator involvement.
  - [ ] P3.1 `npm run dev` background; wait for dev server up (poll `localhost:5173`).  <!-- status: NOT-STARTED -->
  - [ ] P3.2 Playwright MCP: navigate to phugoid-probe-low; t=10s snapshot; assert criterion 3 low gates. Capture trajectory log.  <!-- status: NOT-STARTED -->
  - [ ] P3.3 Same for phugoid-probe-mid; assert mid gates.  <!-- status: NOT-STARTED -->
  - [ ] P3.4 Same for phugoid-probe-high; assert high gates.  <!-- status: NOT-STARTED -->
  - [ ] P3.5 Tear down dev server.  <!-- status: NOT-STARTED -->
  - [ ] verify-auto  <!-- status: NOT-STARTED -->
  - [ ] verify-self  <!-- status: NOT-STARTED -->
  - [ ] verify-human  <!-- status: NOT-STARTED -->
  - [ ] verify-codify  <!-- status: NOT-STARTED -->

## Current Node
- **Path:** Feature > Phase 1 > ESCALATED (Branch C)
- **Active scope:** ESCALATE — file SURFACE-2026-05-24-08, hand off to D24 architect cycle (operator-pause per Mode 4 ESCALATE policy)
- **Blocked:** Phase 2 + Phase 3 + ship + finalize — all conditional on Branch A (not reached)
- **Unvisited:** D24 architect cycle (product:arch) → potential WP14.19 (TBD shape post-D24) → eventual Phase 2/3 of THIS WIP only if D24 chooses a path that re-uses these phases
- **Open discoveries:** SURFACE-2026-05-24-08 (D24 cycle driver — see Discoveries section + backlog)

## Plan-time derivation (Rule #5 — completed at P1.2 before P1.3)

**Fixed airframe properties (from aircraft.json baseline, unchanged):** mass m=1000 kg → W=mg=9810 N; max thrust=6000 N; S_wing=12 m² (2×6); S_hstab=1.5 m²; symmetric-flat-plate CL_α≈2π. Throttle-to-thrust linear: τ=0.05→300 N, τ=0.15→900 N, τ=0.40→2400 N.

**D22-β bounded drag knobs** (the optimizer searches inside these; the architect cycle's globalBest at WP14.16 sat at k_wing=0.278, cd0_fus·area_fus=0.190 — but WP14.18 is a fresh search under a new score function, so the new globalBest may shift):

- `inducedDragK_wing` ∈ [0, 0.3], `inducedDragK_hstab` ∈ [0, 0.3]
- `fuselageDrag.cd0` ∈ [0, 0.5], `fuselageDrag.area` ∈ [0, 1.0]
- Wide envelope: total CD at α=0 cruise ≈ CD0_surfaces + k·CL² + (cd0·area)_fus / S_wing.

**Per-regime expected mode-appropriate behavior** (using WP14.16's drag as a representative point — actual globalBest is what we'll compare to):

| Regime | τ | Thrust (N) | Expected mode | Sink rate / AS at t=10s |
|--------|---|-----------|---------------|--------------------------|
| Low    | 0.05 | 300 | controlled-descent | sink ≈3-4 m/s, AS ≈40-55 m/s (engine-out-glide neighborhood; T<<D forces descent) |
| Mid    | 0.15 | 900 | slow-flight-or-shallow-descent | AS ≈25-50 m/s, sink ≈0-2 m/s (T well below D_min=1959, but airframe can sustain descending slow-flight) |
| High   | 0.40 | 2400 | level-cruise | AS ≈85 m/s, alt within ±50 m of spawn (T≈D_min, near level-flight equilibrium) |

**Rule #5 check on the score function dispatch:**
- Low envelope: SINK_RATE_LOW=[1,5], pitch=[-30°, +10°]. Expected: 3-4 m/s sink → **inside envelope, c0 PASS expected**.
- Mid envelope: AS=[25,50], sink=[-1,+3]. Expected: AS 25-50 + sink 0-2 → **inside envelope, c0 PASS expected**.
- High envelope: ALT=±50, AS=85±25. Expected: cruise near 85 → **inside envelope, c0 PASS expected**.

**Sanity-check for the hypothesis the architect cycle locked in:** if the optimizer's globalBest produces trajectories matching the table above, criterion 0+1+2 should all PASS, and Branch A is reachable. If it doesn't (e.g., low sinks at >10 m/s or AS goes negative; mid stalls or AS collapses; high oscillates ±30 m/s), then either (a) WP14.16-vicinity drag is wrong region for new score function (legitimate parameter shift; tune will find better) or (b) c0-floor cluster persists (Branch C).

**Bound-pressure prediction:** WP14.16 sat at k_wing=0.278/0.3 ≈ 93%. Under the new score function the c0-floor attractor is undermined, so the optimizer may relax k_wing (the AS-collapse-rewarding direction is no longer rewarded). If k_wing still saturates ≥85%, that's the **fourth observation** of the loosen-constraint anti-pattern in this cascade → Branch C signal (do NOT widen the bound; file D24 SURFACE).

**Cross-product / dimensional sanity:** D_min=1959 N at V_min=61 m/s for the WP14.16 drag polar means τ=0.40 (2400 N) ≈ D_min — high-regime level cruise is on the equilibrium boundary, not deep inside. A small AS perturbation can tip into climb or descent; the new score function should still tolerate ±50 m altitude excursion + ±25 m/s AS deviation as "level-cruise."

## Bound-pressure audit (P1.6 — populated 2026-05-24)

| Knob | globalBest | Bound | Saturation % | Flag |
|------|-----------:|------:|-------------:|------|
| surfaces.0.clQ          | 1.4678 | 0..3   | 48.9% | ok |
| surfaces.0.clAlphaDot   | 1.7168 | 0..10  | 17.2% | ok |
| **surfaces.0.inducedDragK** | **0.2771** | **0..0.3** | **92.4%** | ⚠ **Branch C — fourth observation of loosen-constraint anti-pattern** |
| surfaces.2.clQ          | 2.2431 | 0..3   | 74.8% | ok |
| surfaces.2.clAlphaDot   | 3.3017 | 0..10  | 33.0% | ok |
| surfaces.2.inducedDragK | 0.0607 | 0..0.3 | 20.2% | ok |
| fuselageDrag.cd0        | 0.2711 | 0..0.5 | 54.2% | ok |
| fuselageDrag.area       | 0.6931 | 0..1.0 | 69.3% | ok |

**Verdict:** `inducedDragK_wing` saturates 92.4% — exceeds the ≥85% Branch C threshold. Per spec acceptance criterion + D23 arch revision generalizable lesson + `feedback_optimizer_bounds_are_floor.md` bidirectional rule: **DO NOT widen the bound.** This is the fourth cascade observation of the loosen-constraint anti-pattern (D18→D19→D20 widened drag, D22 narrowed, WP14.16 saturated narrowed, WP14.18 saturated narrowed again under new score function). The optimizer continues to pressure k_wing because the score function still rewards extreme drag for the AS-collapse strategy in 3 of 4 restarts (see c0-floor cluster check below).

## c0-floor cluster check (P1.7 — populated 2026-05-24)

Empirical c0-floor reference: -1e9 per regime; 3 regimes worst-case sum = **-3e9**.

| Restart | Seed | finalScore | Δ vs -3e9 | Clustered (Δ < 1%)? |
|---------|-----:|-----------:|----------:|---------------------|
| 0 | 3717185309 | -2,999,997,447 | +2,553 | **YES — c0-floor** |
| 1 | 2622596788 | -2,035,174,819 | +964,825,181 | NO — emerged |
| 2 |  797528212 | -2,999,997,514 | +2,486 | **YES — c0-floor** |
| 3 | 1932287440 | -2,999,997,894 | +2,106 | **YES — c0-floor** |

**Verdict:** 3 of 4 restarts cluster at full c0-floor (-3e9, deviation <3000); only restart 1 emerged to -2.035e9 (which still has 2 of 3 regimes at c0-floor, only 1 regime emerged). **The c0-floor attractor is overwhelmingly dominant under the D23 score function — worse than WP14.16's emergence ratio under D21.** The hypothesis the architect cycle locked in (mode dispatch removes the c0-floor attractor) is **REFUTED by evidence**.

## Phase 1 verify-self results (P1 verify-self — populated 2026-05-24)

**Trajectory finiteness (criterion 1):** low + mid finite 1800 ticks; **high NaN's at tick 1530**. Mechanism stack stability regressed vs WP14.16 (which had all 3 regimes finite 1800).

**Deployed-symmetric score (criterion 2 via `score-deployed.mjs`):** total = **-2,999,998,384** (full c0-floor across all 3 regimes); per-regime: low=-999,999,967, mid=-999,999,946, high=-999,998,471. **Acceptance threshold ≥ -300 — FAILED by ~10 billion.**

**Mode-specific c0 inspection (criterion 0):** all 3 regimes pitch up to >+24° within t=1s (exceeds low/mid/high pitch envelopes), then sink/climb non-monotonically until gimbal-lock at t≈5s (pitch ±89°). No regime passes mode-appropriate c0.

| Regime | t=1s pitch | t=1s sink | t=10s pitch | t=10s state | c0 verdict |
|--------|-----------:|----------:|------------:|-------------|------------|
| Low (controlled-descent expect)    | +24.1° | -10.98 m/s (climbing!) | +81.5° vertical | falling | **FAIL** — pitch >+10° cap, sink <1 m/s floor |
| Mid (slow-flight expect)            | +28.4° | -18.36 m/s | -89° gimbal | tumbled | **FAIL** — pitch >±20° cap, sink <-1 floor |
| High (level-cruise expect)          | +24.6° | -48.35 m/s (rocket climb) | -89° gimbal | tumbled | **FAIL** — pitch >±15° cap, AS oscillating |

**Search-vs-deploy gap:** optimizer's reported -2.035e9 vs deployed-symmetric -2.999e9. Ratio 1.47× — same as SURFACE-2026-05-24-05's prior observation. Sym-mirror linkage gap persists under D23.

**Verdict: ALL FOUR acceptance criteria FAIL.** Branch A is not reachable from this tune. Branch C signals fire: (a) `inducedDragK_wing` saturates 92.4% ≥ 85% threshold; (b) c0-floor cluster contains 3 of 4 restarts. Per pre-locked decision branch: file SURFACE → D24 architect cycle.

## Discoveries

- [SURFACED-2026-05-24] WP14.18 investigation → product:arch (D24 cycle driver — SUPERSEDES -08) — **SURFACE-2026-05-24-09 (CRITICAL — NOW RESOLVED)**: `body.resetForces()` + `body.resetTorques()` missing from physics tick. Fix landed at task `fix-resetforces-bug` (added to `FlightModel.applyForces`). WP14.18b re-tune evidence: optimizer score -2.035e9 → -9,423 (216,000×); deployed -2.999e9 → -26,306 (114,000×); c0-floor cluster 3/4 → 0/4; `inducedDragK_wing` 92.4% → 1.5%. D24 architect cycle still needed for cascade walk-back + final tune-deploy decision. See `workflow/backlog.md` SURFACE-09 + `workflow/wip/fix-resetforces-bug.md` verdict section.

- [SURFACED-2026-05-24] WP14.18 Phase 1 → product:arch (D24 cycle — DEPRIORITIZED by -09) — **SURFACE-2026-05-24-08**: WP14.18 tune under D23-γ-evolved score function REFUTES Branch A: 3 of 4 restarts c0-floor clustered, deployed-symmetric -2.999e9, `inducedDragK_wing` saturates 92.4% of 0.3 bound (fourth observation of loosen-constraint anti-pattern). Mechanism stack stability ALSO regressed: high regime NaN's at tick 1530 (WP14.16 had all 3 finite 1800). The D23 hypothesis (mode dispatch removes c0-floor attractor) is REFUTED by evidence: c0-floor emergence ratio is 1/4 under D23, was 1/4 under D21 at WP14.16 — no improvement. Per pre-locked spec Branch C: D24 architect cycle for (i) D23-δ fifth-mechanism longitudinal damper, (ii) reframe low-regime mode from "controlled-descent" to "engine-out-glide" (more permissive sink-rate envelope), (iii) per-mode envelope re-derivation under WP14.16 globalBest as anchor. **DO NOT widen `inducedDragK` bound** (bidirectional rule). Per `feedback_optimizer_bounds_are_floor.md` rule 2 update QUEUED at D23: the optimizer is pushing against a constraint that the score function rewards pushing against — REFRAME the score function (or reframe the mode definitions) rather than widen the constraint. Logged to `workflow/backlog.md`.

TRANSITION: F26

## Session Pause — 2026-05-24 21:19
Paused. See `workflow/.session.md` to resume. Note: this WIP is ESCALATED Branch C, but the integrator-fix at commit `46f9b42` (see SURFACE-2026-05-24-09 RESOLVED) has structurally collapsed the cascade; D24 architect cycle at resume will decide whether to archive this WIP and start fresh WP14.19, or re-frame its remaining Phase 2/3 nodes under correct integration.
