# Backlog

Surface-notes from workflow runs. Consumed and resolved by higher-level workflows (arch revisions, new WPs, etc.).

## Open

### SURFACE-2026-05-24-04 — D20 further-bound-widening refutes: linked-search tune at 2× WP14.13 widened bounds OVERSHOOTS the stable region — all 4 restarts saturate at NaN-floor, deployed score regresses to −2.003e9
- **Source:** feature:build (WP14.14 Phase 1, 2026-05-24)
- **Target level:** product:arch (D21 candidate selection; primary candidate per WP14.14 evidence: **option-c "accept current state"** per `feedback_retune_attempt_budget.md` — ship the linked-smoke globalBest at WP14.13 widened bounds (deployed −6.7M) as a Phase 2 starting point with relaxed envelope, defer flyable-feel to Phase 3 playtest; secondary: **D19d score-envelope re-calibration** to formalize "deployable at relaxed envelope"; tertiary: fifth-mechanism candidates per arch.md D19 line 937 (bank-angle-coupled drag, stall-region CD steepening, dα/dt smoothing); quaternary: revert to WP14.13 widened bounds for any further drag tuning attempts — the 2× further widening is REFUTED, not in the menu)
- **Type:** arch-gap / hypothesis-refutation / cascade-attempt-3-exhausted
- **Priority:** high (D20 cascade driver; gates WP15/WP16/WP17 Phase 2 mission content; transitively blocks SURFACE-24-03, -24-01, -23-01, and the chain of 7)
- **Summary:** WP14.14 ran the canonical D20 linked-search tune (`--link surfaces.0={clQ,clAlphaDot,inducedDragK}=surfaces.1.*` + 8-dim knobs + bounds `0..3,0..10,0..3.0,0..4,0..10,0..3.0,0..1.0,0..20.0` + 4 restarts + seed 42) per arch.md D20 binding command. Wall time 17.3s. Optimizer globalBest: `surfaces.0: clQ=1.930, clAlphaDot=0.000, inducedDragK=2.436 / surfaces.2: clQ=3.470, clAlphaDot=0.923, inducedDragK=2.289 / fuselageDrag: cd0=0.443, area=15.484`. All 4 restarts saturated finalScore=−2,999,996,805 (NaN-floor, 0-of-4 sub-floor — worse than WP14.13's 2-of-4 and WP14.12's 1-of-4 patterns). **Deployed-symmetric score** (CSV-dump at globalBest with explicit surfaces.1 mirror): per-regime `low=−999,998,878 (NaN @ t=1123)`, `mid=−3,068,907 (full 1800 finite — clean partial-success!)`, `high=−999,998,289 (NaN @ t=1712)`; total **−2,003,066,074**. **REGRESSION vs the linked smoke at WP14.13 bounds:** deployed score −6,705,355 (linked-smoke) → −2,003,066,074 (WP14.14) = ~300× WORSE; the further-widened bounds pushed the optimizer past the stable region.
- **Diagnostic interpretation:**
  1. **Bound-widening hypothesis refutes at the 2nd widening cycle.** Cascade: WP14.12 bounds (k≤0.5, area≤3.0) → asym deployed −92.6M; WP14.13 bounds (k≤1.5, area≤10.0) → asym refuted but linked-smoke at SAME bounds → −6.7M (cascade best); WP14.14 bounds (k≤3.0, area≤20.0) → linked refuted at −2.003e9. **Drag bounds have a sweet spot near WP14.13's widened values**, not "the higher the better." Per `feedback_optimizer_bounds_are_floor.md` rule 2 the optimizer pushed `k_wing` to 1.290 at WP14.13 widened bounds (86% of 1.5); the inference "trust optimizer, widen further" turned out to be wrong — the underlying physics has both lower and upper unflyable bounds for drag (too little = energy excess, too much = numerical instability via CL² coupling at high CL). **Action implication for the memory anchor:** the "rule 2" guidance is sound for the first widening (texture of "is the bound a floor?") but breaks down when the second widening doesn't continue to improve. The cascade has now empirically located the optimum near `k_wing ≈ 1.3, area ≈ 8` — the rule 2 update should note "after 2 successive bound-widenings without improvement, the bound is NOT a floor; treat the prior-cycle optimum as the empirical optimum."
  2. **β5 (clAlphaDot_wing) collapsed to exactly 0.** Linked-smoke had 0.439; WP14.12 had 0.801; WP14.14 has 0.000 (AT FLOOR). β5_hstab also dropped to 9% (linked-smoke 36%). Under heavy drag (k_wing 2.44, area 15.5) the optimizer zeroes out β5 because drag does the dissipation work β5 was contributing. Physical, coherent. Not a defect but evidence that the *minimal flyable parameter set* may not include β5 at all once drag is properly tuned — actionable for D19d (envelope re-cal can also re-rank which mechanisms are load-bearing).
  3. **Mid regime stays clean through 1800 ticks at the heavy-drag globalBest.** Score −3.07M on mid is mostly envelope-deviation penalties, not NaN. The mid regime is the cleanest of the three — heavier drag improved level-cruise dynamics but destabilized the low/high regimes (which probe more extreme α and V conditions). Suggests the envelope tightness on AS=200 m/s in the low regime is the dominant penalty, NOT the underlying mechanism stack — supporting D19d as the natural Branch-B candidate.
- **Bound-pressure at globalBest (for the next architect cycle's evidence base):**
  - surfaces.0.clQ (wing): 1.930/3.0 (64%) — mid-range
  - surfaces.0.clAlphaDot (wing): 0.000/10.0 (**0% — AT FLOOR**)
  - surfaces.0.inducedDragK (wing): 2.436/3.0 (81%) — high-side
  - surfaces.2.clQ (h-stab): 3.470/4.0 (**87% — AT BOUND**)
  - surfaces.2.clAlphaDot (h-stab): 0.923/10.0 (9%) — low-side
  - surfaces.2.inducedDragK (h-stab): 2.289/3.0 (76%) — high-side
  - fuselageDrag.cd0: 0.443/1.0 (44%) — mid-range
  - fuselageDrag.area: 15.484/20.0 (77%) — high-side
- **Evidence:**
  - Optimizer artifact: `tools/tune/results/wp14.14-linked-widened-tune.json` — 4 restarts, 8 params, 55 total convergence iters (39% more iters than WP14.13's 37; the simplex worked harder to contract around the NaN-floor cluster).
  - Symmetric-mirror harness CSVs at globalBest: `/tmp/wp14.14/sym-{low,mid,high}.csv` (deterministic — md5-stable across re-runs of `tools/tune/dump-wp14.14.mjs`).
  - Deployed-score breakdown: per-regime + total above.
  - Dump script: `tools/tune/dump-wp14.14.mjs` (one-shot WP-scoped — could be extracted to a generic dump-at-globalBest utility in a future tooling WP if useful, but not blocking).
- **Recommended next architect cycle (operator-as-architect call, ranked — independently derive per `feedback_rule5_architect_cycle.md`):**
  1. **Option-c "accept current state."** Per `feedback_retune_attempt_budget.md` attempt-3 logic, the D14/D17/D18/D19/D20 cascade has now exhausted the systematic-tuning budget for the drag-bound axis. The linked-smoke globalBest at WP14.13 bounds (deployed −6.7M) is the empirical optimum. Ship that as Phase 2 starting point with relaxed envelope; document the deviation per `feedback_operator_as_external.md` (operator-as-playtester); defer flyable-feel to Phase 3 WP23 playtest. **Why high-leverage:** unblocks WP15/16/17 Phase 2 mission content TODAY without further mechanism work; the unflyability gap (22,000× past −300 threshold) is dominated by AS envelope mismatch which can be addressed at the score-function layer, not the physics layer. **Estimated scope:** S — `aircraft.json` edit at linked-smoke globalBest + un-skip phugoid spec + browser walkthrough with the explicit "operator-as-playtester accept" disposition documented + finalize.
  2. **D19d: score-envelope re-calibration** (formal). Promoted to secondary candidate at WP14.14. WP14.14 evidence: mid regime is clean (−3.07M score, no NaN); the envelope penalties dominate the deployed total. Re-calibrating envelopes (e.g. AS target adjusted per-regime: 30 m/s for mid, 80 m/s for low descent, 50 m/s for high climb; alt deviation envelope widened to 200-300m for phugoid-natural amplitude) could legitimately move the threshold from −300 to a value compatible with the current mechanism stack. NOT a "masking of unflyability" if the new envelopes are physically-justified (real Cessnas DO have 20-30% AS variation in unpowered phugoids). Tooling-only change. **Estimated scope:** S-M (`tools/tune/score.ts` envelope re-derivation + Vitest update + re-tune deployment with new envelopes).
  3. **Fifth-mechanism candidate (defer to D22+).** Bank-angle-coupled drag / stall-region CD steepening / dα/dt smoothing per arch.md D19 line 937. Now that drag bound-widening is refuted, these become more attractive — but per `feedback_surface_or_means_or.md` (singular not stacked) + `feedback_retune_attempt_budget.md` (option-c is option-c), defer until option-c + D19d have been tried.
  4. **D19b: Ixx-anchored inertia revision** — still rejected per D19/D20 architect cycles (causally weak; current Ixx Cessna-class).
  5. **Further drag-bound modification** — NOT in the menu. The 2× widening past WP14.13 bounds is refuted. If drag tuning is revisited in a later cycle, the bounds must be NARROWED toward the linked-smoke globalBest (k_wing≈1.29, area≈7.9), not widened further. Per `feedback_optimizer_bounds_are_floor.md` proposed clarification: after 2 widenings without improvement, treat prior-cycle optimum as the empirical optimum.
- **Relationship to other SURFACEs:**
  - PARTIALLY supersedes SURFACE-2026-05-24-03 (D20 cascade is the post-tooling-fix attempt). SURFACE-24-03 status remains partial; the next architect cycle's D21 decision is what closes-by-implementation.
  - SURFACE-2026-05-24-05 (parity-diff under `--link`) is a sibling discovery from this WP — separately filed below.
  - Transitively blocks the same chain SURFACE-24-03 blocks: -24-01, -23-01, -17-03, -17-01, -16-04, -16-01, -12-03, -12-01, -11-04. All wait on D21 close.
- **Memory anchors that fired this WP:**
  - `feedback_optimizer_bounds_are_floor.md` — drove the D20 architect cycle's bound-widening rationale (which was wrong at the second widening; the rule's proposed clarification is included in the diagnostic interpretation above).
  - `feedback_tune_cli_search_vs_deploy.md` Quaternary use — drove the artifact-optima lens that the D20 architect cycle applied; the lens was sound but the architect's parameter-space inference (further-widen) was wrong.
  - `feedback_surface_or_means_or.md` — D20 picked further-widening singularly; refutation now passes the singular choice to D21.
  - `feedback_finite_vs_flyable.md` — criterion 1 FAIL + criterion 2 FAIL (both saturated this time, like WP14.13 but more so). Reinforces "both criteria must be checked."
  - `feedback_retune_attempt_budget.md` — attempt-3 logic now fires; option-c is the legitimate primary candidate at D21.
  - `feedback_rule5_architect_cycle.md` — applies recursively: D21 architect cycle MUST independently derive choice; the SURFACE-24-04 recommendations are suggestions, not bindings.
  - `feedback_operator_as_external.md` — full-autopilot deviation continues; if D21 chooses option-c the operator-as-playtester gate is at WP23.
- **Status:** partial — D21 architect cycle (arch.md Revision 2026-05-24 evening) chose **score-function revision** (criterion 0 level-flight-maintenance probe + per-regime target_AS recalibration to physically-derived L=W equilibrium values) over the SURFACE's pre-inspection primary candidate (option-c). The post-WP14.14-finalize in-browser inspection at session-end revealed that even the cascade's empirical best CANNOT sustain 1 second of level flight at the phugoid-probe-mid throttle 0.15 entry conditions (pitch −73°, AS 16.5 m/s, nose-first dive at t≈1s) — independent plan-time derivation per CLAUDE.md Rule #5 showed the score function has been measuring deviation from target_AS values ~2× lower than the airframe's physical equilibrium AS for the entire D14/D17/D18/D19/D20 cascade. D21 supersedes the SURFACE's option-c primary candidate (option-c would still ship under the wrong score function). Implementation work split: WP14.14b (tooling-only score.ts edit + Vitest) and WP14.15 (re-tune at WP14.13 widened bounds under new score function + browser walkthrough). **Full close pending WP14.15 Branch A**, at which point the chain of 10 transitively-blocked SURFACEs full-closes together (11 total: -24-04 plus chain).
- **Blocks:** WP15, WP16, WP17 (Phase 2 mission content); full close of SURFACE-2026-05-24-03, SURFACE-2026-05-24-01, SURFACE-2026-05-23-01, SURFACE-2026-05-17-03, SURFACE-2026-05-17-01, SURFACE-2026-05-16-04, SURFACE-2026-05-16-01, SURFACE-2026-05-12-03, SURFACE-2026-05-12-01, SURFACE-2026-05-11-04

### SURFACE-2026-05-24-05 — Search-vs-deploy parity-diff under `--link`: optimizer-internal evaluation produces score 1.50× more negative than `score-deployed.mjs` re-score at bit-identical params
- **Source:** feature:build (WP14.14 Phase 1, 2026-05-24)
- **Target level:** product:wbs (tooling fix or methodology investigation; not arch-layer because the underlying harness is deterministic per-call — md5-stable CSVs at re-runs — and the divergence is between two presumably-equivalent evaluation paths)
- **Type:** tooling-bug / methodology-gap / parity-diff
- **Priority:** medium (diagnostic concern, not blocking; the `--link` flag's "search-airframe = deploy-airframe" claim is slightly less load-bearing than the D20 architect cycle assumed; future tune WPs need to re-validate the ratio is 1.0 OR derive deployment scores via `score-deployed.mjs` explicitly)
- **Summary:** At WP14.14 globalBest knobs (deterministic per harness invocation — `md5sum` stable across re-runs), the optimizer's reported `score=−2,999,996,805` vs the `score-deployed.mjs` total at the dumped CSVs `−2,003,066,074`. **Ratio = 1.497×.** Under `--link surfaces.0.*=surfaces.1.*` the optimizer's evaluation airframe should be bit-identical to the deployment airframe at the same params, and the score functions are identical (`score-deployed.mjs` imports `score.ts` directly), so the ratio should be exactly 1.0. The 1.50× deviation indicates one of: (a) the optimizer evaluates at a *different* simplex vertex than the one stored as `finalParams[bestIdx]` (off-by-one in optimizer.ts:310–311), (b) the optimizer's internal `runHarness` invocations share some state (Rapier world singleton? RNG sequence?) that the fresh dump-script's `runHarness` does not, or (c) the `--link` expansion in `buildObjective` produces a slightly different `params` array than the explicit `surfaces.1.*` overrides in the dump script (e.g. ordering or precision-of-string-conversion in `params.push(${k}=${v})`).
- **Evidence:**
  - Optimizer-reported score at globalBest: −2,999,996,805 (from `tools/tune/results/wp14.14-linked-widened-tune.json` → `.score`)
  - Dump-script CSVs at same globalBest: deterministic (md5 stable across 2 re-runs); per-regime firstNaN ticks: low=1123, mid=none, high=1712
  - Deployed score at CSVs: −2,003,066,074 (from `npx tsx tools/tune/score-deployed.mjs ...`)
  - Ratio computation: 2,999,996,805 / 2,003,066,074 = 1.497
- **Hypothesis ranking:**
  1. **Most likely:** `buildObjective` constructs the `params` array differently from `dump-wp14.14.mjs`. Both invoke `runHarness({ ..., params })`, but `buildObjective` passes the optimizer's normalized simplex-vertex floats, while the dump script passes JS Number.toString() of the stored params from JSON. If the storage→load roundtrip loses precision (e.g. `2.4355146341348983` → ... → `2.4355146341348983` survives `JSON.parse`, but the harness may parse `${k}=${v}` strings that hit `parseFloat` differently than the original Float64). Worth checking via a unit-test that the params array matches bit-for-bit.
  2. **Secondary:** Off-by-one in `optimizer.ts:310-311` — `simplex[bestIdx]` and `scores[bestIdx]` index into different generations. Read the optimizer history at the final restart: does the trace's last `bestScore` match `scores[bestIdx]` against `simplex[bestIdx]`?
  3. **Tertiary:** Harness-state-leakage. Check `createPhysicsWorld()` for any module-scoped state; check `runHarness` for any cached state that survives across calls.
- **Suggested action:**
  1. Write a Vitest case that calls the optimizer with `--restarts 1 --seed 42` and small bounds, then re-evaluates `runHarness` at the returned `finalParams[bestIdx]`. Assert the returned `score` matches the re-evaluation within 1e-6 relative.
  2. If the test fails: ranked-hypothesis investigation per above.
  3. If the test passes: the parity-diff is something else (config-file caching? read of `public/config/aircraft.json` differing between optimizer process and dump-script process?). Worth checking that `runHarness` reads the same `aircraft.json` content in both contexts.
- **Why medium priority:** the ratio of 1.50× is not 1.0 but is also not the 10,000× of pre-tooling-fix asymmetric search. The optimizer's score is still a usable proxy for branch decisions (Branch B was clear at either −2.003e9 or −2.999e9 — both saturate the threshold). The methodology-fix at `tune-cli-link-flag` is mostly load-bearing; the residual 1.50× is a methodology gap that needs further investigation but doesn't invalidate the cascade's evidence base.
- **Memory anchor that fired this WP:** `feedback_tune_cli_search_vs_deploy.md` (all four uses) — the rule "always re-score the deployed config via `score-deployed.mjs`" was load-bearing at WP14.14 P1.4. Without it, WP14.14 would have reported −2.999e9 as the deployed score and the architect-cycle interpretation would have missed the partial-success in the mid regime (visible only via per-regime breakdown of the deployed CSVs, not the optimizer's aggregate score). The rule's *practical value* survives the parity-diff bug; the rule's *theoretical foundation* (`--link` makes search = deploy) is the part that needs the fix.
- **Status:** open
- **Blocks:** nothing (diagnostic concern; future tune-pass WPs should keep using `score-deployed.mjs` as the binding gate while this parity-diff is investigated)

### SURFACE-2026-05-24-03 — D19 widened-bounds drag-polar tune refuted: deployed-symmetric airframe NaN at all 3 regimes; search-vs-deploy ratio INVERTS (0.67×) for first time in cascade — methodology mismatch (asymmetric search vs symmetric deploy) is now a binding gap
- **Source:** feature:build (WP14.13 Phase 2, 2026-05-24)
- **Target level:** product:arch (fifth mechanism layer OR methodology revision — primary candidate per WP14.13 evidence: tooling-fix to enforce mirror-symmetric search via `--link surfaces.0=surfaces.1` in tune CLI; secondary: D19d score-envelope re-calibration; tertiary: D19b inertia revision correctly re-anchored to Ixx; quaternary: new fifth-mechanism candidates per arch.md D19 line 937 — bank-angle-coupled drag, stall-region CD steepening, finite-difference dα/dt smoothing)
- **Type:** arch-gap / methodology-gap / search-vs-deploy-mismatch-now-binding
- **Priority:** high (consolidates and supersedes SURFACE-2026-05-24-01 as the actionable arch-revision driver post-D19; the WP14.13 evidence shifts the diagnostic from "which mechanism layer to add" to "how to tune the existing mechanism stack without the asymmetric/symmetric mismatch corrupting the optimizer's signal")
- **Summary:** WP14.13 ran the canonical 8-dim joint tune with D19 widened bounds (`inducedDragK_{wing,hstab}` upper 0.5→1.5, `fuselageDrag.area` upper 3.0→10.0; all other bounds unchanged from WP14.12 per arch.md Revision 2026-05-24). Optimizer globalBest (restart 0, seed 3717185309): `surfaces.0: clQ=1.627, clAlphaDot=3.498, inducedDragK=1.246 / surfaces.2: clQ=2.019, clAlphaDot=2.412, inducedDragK=0.889 / fuselageDrag: cd0=0.340, area=6.911`. Asymmetric search finalScore: −2.004e9 (WORSE than WP14.12's −1.057e9, despite widened bounds). 2-of-4 restarts found sub-NaN-floor regions (mild improvement vs WP14.11/WP14.12's 1-of-4). **All 3 D18 drag knobs increased in absolute magnitude vs WP14.12** (k_wing 0.436→1.246, k_hstab 0.125→0.889, area 2.410→6.911; cd0 traded down 0.617→0.340). **Deployed-symmetric score: −2,999,998,103** — saturated at NaN-penalty floor; criterion 1 FAILED (firstNaN: low=1014, mid=454, high=429 — all 3 NaN'd within 7-17 sec sim time); criterion 2 FAILED (penalty floor). **REGRESSION vs WP14.12 on criterion 1.** WP14.12 deployed-symmetric had all 3 regimes 1800-finite; WP14.13 deployed-symmetric has all 3 regimes NaN'd within 17 sec.
- **Search-vs-deploy ratio INVERSION (the key new diagnostic):** WP14.11=10,000× (asym << deploy, asym much worse), WP14.12=11.4× (asym worse than deploy by a small factor), **WP14.13=0.67× (asym BETTER than deploy for first time in cascade)**. Per `feedback_tune_cli_search_vs_deploy.md` Secondary use: the ratio is a diagnostic signal about which mechanism layer is closest to flyable. This inversion direction is *new* and points at a different gap than "more dissipation needed." The optimizer's asymmetric airframe (wing-right held at baseline `clQ=3, inducedDragK=0`) exploits wing lopsidedness to dodge symmetric instabilities — when *both* wings get heavy `(clQ=1.627, clAlphaDot=3.498, inducedDragK=1.246)`, the joint mechanism stack destabilizes within seconds. This is no longer a calibration nuisance; it is a binding methodological gap that corrupts the optimizer's score signal.
- **Evidence:**
  - Optimizer artifact: `tools/tune/results/wp14.13-widened-tune.json` — 4 restarts, 8 params, 37 convergence iters (early simplex contraction; far fewer iters than WP14.12's ~62), finalScore=−2.004e9 (asymmetric)
  - Per-restart spread: r0=−2.00e9 (globalBest), r1=−2.04e9 (also sub-floor — 2-of-4 sub-floor restarts, mild improvement vs WP14.11/WP14.12's 1-of-4), r2=−3.00e9 (NaN-floor), r3=−3.00e9 (NaN-floor)
  - Symmetric-mirror harness CSVs at globalBest: `/tmp/wp14.13/sym-{low,mid,high}.csv`, each NaN'd within 17 sec sim
  - Deployed-score log: `/tmp/wp14.13/deployed-score.log` — `Per-regime: low=−999,998,986; mid=−999,999,546; high=−999,999,571. Total (weighted sum, default envelopes): −2,999,998,103.00`
  - Knob position vs widened bounds: `inducedDragK_wing=1.246/1.5` (83% — still pushing high after widening!); `inducedDragK_hstab=0.889/1.5` (59% — moved up from WP14.12's 25%); `fuselageDrag.area=6.911/10.0` (69% — moved up from WP14.12's 80% at 3.0 bound)
- **Recommended next architect cycle (operator-as-architect call, ranked):**
  1. **Tooling-fix: enforce mirror-symmetric search in the optimizer** (NEW PRIMARY — promoted from "tooling fix candidate" in arch.md Revision 2026-05-23 line 877). Add `--link surfaces.0=surfaces.1` (or equivalent auto-mirror) to the tune CLI so the optimizer's search-airframe IS the deployed-symmetric airframe. Until this is done, the optimizer's score is exploring an unrealistic asymmetric airframe and producing parameter recommendations that don't survive deployment. This is the *root cause* of the WP14.13 NaN regression — the optimizer found a globalBest that is fine in asymmetric search but destabilizes when mirrored. **Why high-leverage:** removes the load-bearing-by-coincidence "asymmetric airframe finite" property the WP14.11/WP14.12/WP14.13 results depended on; deployed-score becomes the *primary* score; next tuning WP has reliable signal. **Estimated scope:** S — ~50 LOC in `tools/tune/tune.ts` (auto-set `surfaces.1.X = simplex_vertex(surfaces.0.X)` when `--link` is present) plus a Vitest case.
  2. **D19d: score-envelope re-calibration** (SECONDARY per arch.md D19 candidate table rank 2). WP14.13 evidence shows alt envelopes are mostly fine (where finite, peak alt-dev was reasonable). The AS=200 envelope drives most of the penalty. Real aircraft do exceed 200 m/s in dives. Worth revisiting *after* the tooling fix unblocks reliable search.
  3. **D19b: inertia revision** (TERTIARY per arch.md D19 candidate table rank 3, *correctly re-anchored to Ixx not Iyy* — see SURFACE-24-02). Causal mechanism remains weak for the airspeed-overshoot symptom (pitch period vs energy peak), but the WP14.13 evidence of "joint mechanism stack destabilizes" might involve pitch-axis dynamics — re-evaluate after the tooling fix.
  4. **New fifth-mechanism candidate per arch.md D19 line 937:** bank-angle-coupled drag, stall-region CD steepening, or finite-difference dα/dt smoothing. Defer to a future architect cycle if the tooling fix + envelope re-calibration are insufficient.
- **Why the tooling-fix is now an arch-layer concern, not a tooling-layer concern:** through WP14.11/WP14.12 the search-vs-deploy mismatch was a *calibration* gap — the optimizer's score was a noisy proxy for deployed score (10,000× off), but the deployed score was still informative on its own. WP14.13 shows the mismatch can now flip *signs* — at heavy dissipation, the asymmetric airframe is *more* finite than the symmetric one. This breaks the optimizer's gradient: the simplex contracts toward regions that are bad for deployment. Until this is fixed, no further tuning WP can produce reliable signal under the current methodology. This is now blocking; arch-level decision required on whether to fix the tooling or to abandon the harness-optimizer methodology for some other approach.
- **Relationship to other SURFACEs:**
  - PARTIALLY supersedes SURFACE-2026-05-24-01 — D19 widened-bounds path is now refuted at WP14.13; the inertia-revision candidate becomes one of several alternatives but is no longer the *primary* recommendation (the WP14.13 evidence shifts the primary to the tooling-fix). SURFACE-2026-05-24-01 status: open → resolved-by-cascade-but-partial.
  - SURFACE-2026-05-23-01 was already resolved-by-cascade-but-partial; extends another cycle.
  - SURFACE-2026-05-24-02 (axis-naming errata) still open; resolves on whichever future arch revision corrects the prose.
  - SURFACE-2026-05-17-03, -17-01, -16-04, -16-01, -12-03, -12-01, -11-04 stay transitively blocked-by chain extending to -24-03.
- **Memory anchors that fired this WP:**
  - `feedback_tune_cli_search_vs_deploy.md` Secondary use — search-vs-deploy ratio narrowing/widening as diagnostic signal. WP14.13 produced the FIRST inversion in the cascade. Strong signal.
  - `feedback_optimizer_bounds_are_floor.md` — widened bounds at WP14.13; optimizer chose `k_wing=1.246` still at 83% of widened upper (vs 87% of pre-widened upper). The optimizer wants even *more* drag in the asymmetric search-space — but this signal is now corrupted by the asymmetric/symmetric mismatch and should not be acted on until the tooling fix lands.
  - `feedback_finite_vs_flyable.md` — criterion 1 FAIL + criterion 2 FAIL (saturated) — both criteria failed this time, not just criterion 2.
  - `feedback_retune_attempt_budget.md` — 1 optimizer run = systematic budget exhausted; escalating to next architect cycle rather than running another tune at different bounds.
  - `feedback_surface_or_means_or.md` — D19 was the singular chosen path; refuted; next architect cycle picks ONE of (tooling-fix, D19d, D19b, fifth-mechanism) singularly. Not stacking.
  - `feedback_operator_as_external.md` — full-autopilot deviation continues; SURFACE-IN documents the deviation for Phase 3 re-validation; browser walkthrough was SKIPPED on Branch B per plan (Phase 4 is Branch-A-only).
- **Status:** partial — TOOLING-FIX (primary candidate) landed at task `tune-cli-link-flag` close (2026-05-24). `--link <src>=<dst>` flag added to tune CLI; symmetric-mirror search verified end-to-end (search-vs-deploy ratio now exactly 1.0×, matches independent score-deployed.mjs re-score bit-for-bit). Deployed-symmetric score at the linked-search globalBest is **−6.7M** — ~14× better than WP14.12, ~447× better than WP14.13 — but still ~22,000× past flyable threshold (−300). The METHODOLOGY GAP is resolved; the FLYABILITY GAP remains. Next step: architect cycle re-evaluates remaining candidates (D19d score envelope, D19b inertia revision re-anchored to Ixx, or new fifth-mechanism) at the next tune-deploy WP. Flyability close awaits that cycle.
- **Blocks:** WP15, WP16, WP17 (Phase 2 mission content); full close of SURFACE-2026-05-24-01, SURFACE-2026-05-23-01, SURFACE-2026-05-17-03, SURFACE-2026-05-17-01, SURFACE-2026-05-16-04, SURFACE-2026-05-16-01, SURFACE-2026-05-12-03, SURFACE-2026-05-12-01, SURFACE-2026-05-11-04


### SURFACE-2026-05-24-02 — Axis-naming error in arch.md Revision 2026-05-23 line 800 + propagation through SURFACE-23-01 and SURFACE-24-01 prose ("Iyy" used where "Ixx" is the pitch-axis inertia in our Y-up convention)
- **Source:** product:arch (D19 architect cycle, 2026-05-24 — Rule #5 plan-time derivation catch)
- **Target level:** docs (arch.md prose correction + backlog SURFACE-23-01/24-01 status notes; non-binding for any active WP)
- **Type:** docs-errata / sign-convention-misnaming
- **Priority:** low (non-blocking for D19 cascade; the D19 revision explicitly notes the error and re-anchors to Ixx; cleanup is a future-revision doc fix)
- **Summary:** arch.md Revision 2026-05-23 line 800 reads "Inertia tensor (SURFACE rank 2). Symptomatic, not causal. Iyy=3000 vs Cessna-class ≈1346 is ~2.2× heavy." This mis-names the pitch-axis inertia: in this project's right-handed Y-up convention, pitch is rotation about body X (confirmed by `src/aircraft/stability.test.ts:94` and `src/aircraft/physics-core/flightmodel.test.ts:214`), so the pitch-axis inertia is `inertia.x = Ixx`, not `inertia.y = Iyy`. Cessna 172's pitch-axis inertia in NASA-aero convention (Z-down) is Iyy ≈ 1346 kg·m²; mapped into our Y-up convention that becomes Ixx ≈ 1346. Current `aircraft.json` has `Ixx=1500` (already Cessna-class — only 1.11× heavy) and `Iyy=3000` (yaw inertia in our convention, 1.64× a Cessna's yaw inertia). The error propagated into SURFACE-2026-05-24-01's "halve Iyy 3000→1500" primary candidate prose, which the D19 architect cycle (2026-05-24) re-derived per CLAUDE.md Rule #5 and chose NOT to adopt (the inertia candidate is mis-anchored AND has weak causal mechanism for the airspeed-overshoot symptom; D19 picks bound-widening as the highest-leverage single option instead).
- **Suggested action:**
  1. Correct arch.md Revision 2026-05-23 line 800: replace "Iyy=3000 vs Cessna-class ≈1346 is ~2.2× heavy. Halving Iyy speeds the phugoid period by `√2 ≈ 1.41×`" with "In our right-handed Y-up convention, pitch is about body X, so pitch-axis inertia is `Ixx=1500` (not `Iyy`). Current Ixx is already Cessna-class (Cessna 172 pitch inertia ≈ 1346 kg·m² in NASA convention, mapping to our Ixx). `Iyy=3000` is yaw inertia in our Y-up convention, which doesn't directly couple to the airspeed-overshoot symptom."
  2. Update SURFACE-2026-05-24-01's "Recommended next architect cycle" prose to note: candidate #1 (inertia-tensor revision) was re-derived at the D19 architect cycle and (a) is mis-anchored on Iyy vs Ixx, (b) has weak causal mechanism for the airspeed-overshoot symptom; D19 chose bound-widening as the highest-leverage option instead.
- **Why low priority:** The arch.md D19 revision (Revision 2026-05-24) explicitly documents the error and re-anchors. No active WP depends on the corrected prose. Cleanup is opportunistic.
- **Memory anchor that fired this WP:** CLAUDE.md Rule #5 (plan-time physics derivation precedes spec-text reading) — this is the first time Rule #5 has fired at the *architect-cycle* stage, catching a SURFACE-IN author's primary-candidate recommendation. Prior firings caught spec errata at *implementation* stage (β-AoA sign convention, D15 layer ambiguity, D17 cross-product order). Lesson candidate (NOT yet persisted to memory pending a second observation per `feedback_memory_active_recall.md` discipline): the SURFACE-IN author's "Recommended next architect cycle" section is a *suggestion*, not a *binding*; the architect-cycle side must independently derive its choice. Defer memory write until a second firing confirms the pattern.
- **Status:** open
- **Blocks:** nothing (non-blocking doc fix)

### SURFACE-2026-05-24-01 — D18 drag-polar revision insufficient: 8-dim joint tune at expanded parameter space still produces unflyable trajectories — D19 (inertia-tensor revision) is next candidate
- **Source:** feature:build (WP14.12 Phase 2, 2026-05-24)
- **Target level:** product:arch (fourth mechanism layer — primary candidate: inertia-tensor revision halving Iyy from 3000 → ~1500 per SURFACE-2026-05-23-01 second-ranked candidate; secondary candidates if D19 also escalates: Theodorsen/unsteady aero, score-function envelope re-calibration)
- **Type:** arch-gap / mechanism-design / two-cycle-D18-D19-cascade
- **Priority:** high (consolidates and partially-supersedes SURFACE-2026-05-23-01 as the actionable arch-revision driver post-D18; gates WP15/WP16/WP17 mission content; Phase 2 mission content stays paused)
- **Summary:** WP14.12 ran the canonical 8-dim joint tune over `(clQ, clAlphaDot, inducedDragK) × wings+h-stab + (fuselageDrag.cd0, fuselageDrag.area)` at `[0..3]×[0..10]×[0..0.5]×[0..3]×[0..10]×[0..0.5]×[0..1.0]×[0..3.0]` (4 restarts, seed 42, 1800-tick fixtures). Optimizer's globalBest (restart 1, seed 2622596788): `surfaces.0: clQ=1.286, clAlphaDot=0.801, inducedDragK=0.436 / surfaces.2: clQ=2.134, clAlphaDot=3.665, inducedDragK=0.125 / fuselageDrag: cd0=0.617, area=2.410`. Asymmetric search score: −1.057e9. **Deployed-symmetric score (per `feedback_tune_cli_search_vs_deploy.md`, mirroring surfaces.1=surfaces.0): −92,574,536.** Criterion 1 (all 3 regimes finite at 1800 ticks): PASS. Criterion 2 (deployed total ≥ −300): FAIL by ratio ~309,000×. Per-regime envelope: throttle-low peakAS 313 m/s (envelope 200, over), throttle-mid peakAS 142 m/s (under), throttle-high peakAS 220 m/s (over). vs WP14.11 narrow-bounds best (−96.1M, low 373 / mid 103 / high 230): marginal improvement (~3%), not the orders-of-magnitude required. **D18 (drag-polar revision alone) is refuted as a sufficient third mechanism layer.** Per arch.md D18 closes-by-implementation prose + `feedback_surface_or_means_or.md` (singular not stacked), escalate to D19 — inertia-tensor revision was the second-ranked candidate in SURFACE-2026-05-23-01 architect cycle (recommended next step #2): Iyy=3000 with mass=1000 and h-stab arm=3m gives pitch oscillation frequency too slow for the airframe; Cessna 172 (similar weight class) has Iyy ≈ 1346 kg·m²; ours is 2.2× higher.
- **Evidence:**
  - Optimizer artifact: `tools/tune/results/wp14.12-joint-tune.json` — 4 restarts, 8 params, 62 convergence iters, finalScore=-1.057e9 (asymmetric)
  - Per-restart spread: r0=−3.00e9 (NaN-floor), r1=−1.057e9 (globalBest), r2=−2.06e9, r3=−3.00e9 (NaN-floor). Only 1 of 4 restarts found sub-NaN-floor region — mirrors WP14.11 + WP14.5-retry patterns.
  - Symmetric-mirror harness CSVs at globalBest: `/tmp/wp14.12/sym-{low,mid,high}.csv`, each 1800 finite rows
  - Deployed-score log: `/tmp/wp14.12/deployed-score.log` — `Total (weighted sum, default envelopes): -92574536.18`
  - Plan-time energy-balance derivation matched WP14.11.5 hand-probe observation (V_t ≈ 91 m/s at D18 = `k=0.3, k=0.5, cd0=0.5, area=2.0`) — drag is now textbook-shaped; remaining gap is in the inertia/moment domain, not the dissipation domain.
- **Recommended next architect cycle (operator-as-architect call):**
  1. **D19 — Inertia-tensor revision** (primary candidate): halve Iyy from 3000 → 1500 per SURFACE-23-01 #2 candidate. Possibly also reduce Ixx + Izz proportionally. This is a JSON-only change (no schema extension; existing `inertia: Vec3` field). One mechanism-impl WP + one tune-deploy WP per CLAUDE.md Rule #6 two-close-gates split, OR a single tune-WP since no schema extension. Confidence ~70% based on the airspeed-overshoot pattern (throttle-low peakAS=313 m/s = phugoid period too long = airframe takes too long to pitch back = consistent with high Iyy).
  2. **Secondary candidate (if D19 escalates):** Theodorsen unsteady aero (lift-lag from α̇). This is SURFACE-23-01 #3, deferred because complexity is much higher than D18 or D19.
  3. **Tertiary candidate:** score-function envelope re-calibration. SURFACE-23-01 #5 — rejected at architect cycle 2026-05-23 because it would mask unflyability; re-evaluate only if D19 ships and the resulting airframe has "real plane" 70-100m altitude excursion still penalty-heavy.
- **Search-vs-deploy ratio observation:** WP14.12 ratio was −1.057e9 / −92.6M = ~11.4×, smaller than WP14.11's ~10,000×. This is consistent with D18 making the deployed-symmetric airframe more flyable than the asymmetric search-airframe (which has wing-right at baseline clQ=3 = β4 runaway-prone). The narrower ratio means the optimizer's search-space score is becoming a more reliable proxy for deployed score — a useful signal for D19 search budget calibration.
- **Why this matters at the arch layer, not the WP layer:** WP14.12 was sized S in the WBS on the assumption that D18 + (clQ, clAlphaDot) tuning would suffice. The actual finding is that the drag dissipation has been correctly textbook-shaped but the airframe's pitch-axis dynamics remain too slow to keep airspeed bounded. This is an arch-layer question (which mechanism to add next).
- **Relationship to other SURFACEs:** PARTIALLY supersedes SURFACE-2026-05-23-01 — that SURFACE's D18 candidate is now refuted at WP14.12; the D19 candidate (its #2 recommendation) becomes the actionable next step. SURFACE-2026-05-23-01 status: open → resolved-by-cascade-but-partial (D18 was the chosen path but is insufficient; D19 is the natural follow-up). SURFACE-2026-05-17-03, -17-01, -16-04, -16-01, -12-03, -12-01, -11-04 stay blocked-by-this-SURFACE (transitive chain extends one cycle).
- **Memory anchors that fired this WP:**
  - `feedback_retune_attempt_budget.md` — 1 optimizer run + 1 deployed-score check = the systematic budget. Escalating to D19 candidate rather than widening bounds.
  - `feedback_tune_cli_search_vs_deploy.md` — exactly what its title says; 11.4× ratio observed at this WP, deployed-score gate was the criterion-2 binder.
  - `feedback_finite_vs_flyable.md` — criterion 1 PASS + criterion 2 FAIL was the canonical case this rule anticipates.
  - `feedback_surface_or_means_or.md` — D18 OR D19 OR D20 was the SURFACE-23-01 architect cycle; chose D18 singularly; D18 refuted; now D19 singularly. Not stacking D18+D19.
  - `feedback_operator_as_external.md` — full-autopilot deviation continues; SURFACE-IN documents the deviation for Phase 3 re-validation.
- **Status:** resolved-by-cascade-but-partial — D19 widened-bounds path landed at WP14.13 (2026-05-24); criterion 1 FAILED (deployed-symmetric NaN at all 3 regimes) and criterion 2 FAILED (saturated −2.999e9). The chosen path (bound-widening) closed; the flyability gap remains; the WP14.13 evidence shifted the diagnostic from "which mechanism layer to add" to "the asymmetric/symmetric methodology mismatch is now binding." **Next architect cycle:** see SURFACE-2026-05-24-03 (filed by WP14.13 escalation) — the new primary candidate is the tooling-fix (enforce mirror-symmetric search), with D19d (score envelope) / D19b (inertia, re-anchored to Ixx) / new fifth-mechanism candidates as alternatives.
- **Blocks:** WP15, WP16, WP17 (Phase 2 mission content); full close of SURFACE-2026-05-23-01, SURFACE-2026-05-17-03, SURFACE-2026-05-17-01, SURFACE-2026-05-16-04, SURFACE-2026-05-16-01, SURFACE-2026-05-12-03, SURFACE-2026-05-12-01, SURFACE-2026-05-11-04 (all transitively blocked-by SURFACE-2026-05-24-03 now)


### SURFACE-2026-05-23-01 — Third-mechanism-layer needed: D17+D16 achieve numerical finiteness but airframe energy balance produces unflyable trajectories at every searched (clQ, clAlphaDot) point
- **Source:** feature:build (WP14.11 Phase 2, 2026-05-23)
- **Target level:** product:arch (third mechanism layer — candidate revisions: drag-coefficient model, inertia-tensor revision, Theodorsen/unsteady aero, or WP6.5 ω×r retirement now that D17 is in place)
- **Type:** arch-gap / mechanism-design / convergence-of-prior-SURFACEs
- **Priority:** high (consolidates and supersedes SURFACE-2026-05-16-04 as the actionable arch-revision driver post-D17+D16; gates WP15/WP16 mission content for any non-trivial flight envelope; Phase 2 mission content stays paused)
- **Summary:** WP14.11 ran the canonical WBS joint-tune command at `[0..15]^4` (4 restarts, seed 42) and then a narrowed-bounds re-run at `[0..3]×[0..10]×[0..3]×[0..10]` per SURFACE-2026-05-17-03's recommendation. Hand-probed an additional symmetric point `clQ=1.5, clAlphaDot=5` (all 4 surfaces uniform). **Finite-trajectory region exists** under D17+D16 — multiple points produce 1800 finite ticks across all 3 throttle regimes. This is a real improvement over WP14.5-retry (which had zero finite points across the entire searched box). **But no point produces flyable trajectories.** Deployed-config (symmetric mirror) score at the narrow-bounds optimizer best (clQ=1.93, clAD=4.68 wings; clQ=2.71, clAD=7.15 h-stab): **−96.1M**. Hand-probe (clQ=1.5, clAD=5 uniform): **−105.3M**. Threshold criterion #2 was `total score ≥ −300`. Failure ratio: ~320,000×. Cause: airspeed peaks 373 m/s at throttle-low, 230 m/s at throttle-high; far beyond phugoid-probe.spec.ts's 200 m/s envelope. Altitude excursion 70-100m only modestly over the 50m envelope. The β4 D17 + β5 D16 textbook mechanisms are doing what they were designed to do (damping); the airframe's underlying mass/inertia/aero curve/thrust setup produces large phugoid mode oscillation that *no amount* of (clQ, clAlphaDot) tuning can resolve at the searched bounds.
- **Search-vs-deploy mismatch discovered during the run:** the WBS canonical command tunes `surfaces.0` (wing-left) + `surfaces.2` (h-stab) only — `surfaces.1` (wing-right) keeps `aircraft.json` baseline `clQ=3` during optimizer evaluation. We deploy symmetric (surfaces.1 mirrored to surfaces.0). This causes the optimizer's score to reflect an asymmetric airframe that is fundamentally unflyable, producing systematically worse scores than the symmetric airframe actually produces. Confirmed by re-running the harness at the narrow-bounds global best in both modes: asymmetric (wing-right=baseline) throttle-high NaN at tick 503; symmetric (wing-right=optimizer-best) all 1800 finite. **The current `score(...)` value the optimizer reports is not the deployable-airframe score.** Tooling fix candidate: add `--mirror surfaces.0=surfaces.1` to the tune CLI, or change WP14.11's canonical command to tune 6 knobs (surfaces 0/1/2) with surfaces.0 == surfaces.1 enforced via a `--link` mechanism. Both are out-of-scope for WP14.11 itself.
- **Evidence:**
  - Canonical run: `tools/tune/results/wp14.11-joint-tune.json` — bounds `[0..15]^4`, best score −2.001e9 (1-regime-finite at restart 1, 3 restarts at NaN-floor)
  - Narrow run: `tools/tune/results/wp14.11-joint-tune-narrow.json` — bounds `[0..3]×[0..10]×[0..3]×[0..10]`, best score −1.604e9 (asymmetric eval; 1 restart at 1-regime-finite, others worse)
  - Symmetric-mirror harness CSVs at the narrow-bounds best: `/tmp/wp14.11/p2-narrow-{throttle-low,throttle-mid,throttle-high}.csv` — all 3 regimes 1800 finite ticks, peak AS 373/103/230 m/s
  - Symmetric hand-probe at clQ=1.5, clAD=5 uniform: `/tmp/wp14.11/p2-mixed-{throttle-low,throttle-mid,throttle-high}.csv` — all 3 regimes 1800 finite ticks, similar score profile
  - Deployed-config score script: `tools/tune/score-deployed.mjs` (scratch artifact this WP; either move to tools/tune/ as a permanent utility OR delete at finalize)
- **Per-regime score breakdown at the narrow-bounds best (symmetric mirror):**
  | Regime | rows | first NaN | maxAltDev (m) | AS range (m/s) | regimeScore |
  |---|---|---|---|---|---|
  | throttle-low | 1800 | NONE | 98.5 | 0.2 → 373.6 | −55.1M |
  | throttle-mid | 1800 | NONE | 68.8 | 0.4 → 103.2 | −9.6M |
  | throttle-high | 1800 | NONE | 83.2 | 1.6 → 230.3 | −31.4M |
- **Recommended next steps (operator-as-architect call):**
  1. **Investigate energy balance** — at throttle=0.05 the airframe accelerates from 25 m/s spawn to 373 m/s peak; that's ~14× kinetic energy gain with very low thrust input. Either the drag model is dramatically underestimating CD at low/moderate α, or gravity-PE → KE coupling in dives is uncountered by CD. First test: probe CD at α=0 in symmetric-flat-plate at various flow speeds; compare to a textbook value (CD_0 ≈ 0.02 for clean airfoils; flat-plate post-stall CD ≈ 1.0 at α≈90°). The current curve may have a CD_0 that's effectively zero, allowing free-fall acceleration.
  2. **Inertia tensor sanity check** — Iyy=3000 with mass=1000 and h-stab arm=3m gives a pitch oscillation frequency that may be too slow for the airframe's natural feel. Cessna 172 (similar weight class) has Iyy ≈ 1346 kg·m²; ours is 2.2× higher. Possible quick win: drop Iyy to 1500 and re-run.
  3. **Theodorsen / unsteady aero** — the quasi-steady `cl += clAlphaDot · α̇ · c̄/(2V)` assumes the lift response to α̇ is instantaneous. The Wagner indicial response model (Theodorsen function in frequency domain) shows lift lags α̇ by ~2 chord-lengths of travel. At V=200 m/s and c̄=1m, that's a 0.01s lag — not huge, but at large α̇ during stall-recovery it may matter. This is the most complex of the three candidates; consider only if drag-CD + inertia don't fix it.
  4. **WP6.5 ω×r retirement** — the WP6.5 linear ω×r mechanism still contributes to the rotation-induced airflow in the linear term of `computeAeroForce`. D17 added the *damping correction* on top. It's possible the linear ω×r itself is adding non-trivial moment that the damping needs to counteract; retiring it (folding all of it into the D17 clQ branch) might reduce the joint-search dimensionality. Verify by reading aerosurface.ts step-2/step-3 and noting whether ω×r contribution can be cleanly subtracted.
  5. **Score function envelope re-calibration** — the envelopes (ALT_ENVELOPE=50m, AS_ENVELOPE=30m, PITCH_RATE_LIMIT=360°/s) were set in WP14.8 for a hypothetical-flyable airframe. If post-revision the airframe still has 70-100m altitude excursion (real planes do), the envelope is right and we need to fix the airframe. If the airframe is fixed but envelopes are still penalty-heavy, the envelopes need relaxation. This decision should follow steps 1–4, not precede.
- **Why this matters at the arch layer, not the WP layer:** WP14.11 was sized XS-S in the WBS on the assumption that D17+D16 mechanism-side fixes were sufficient and only a joint tune was needed. The actual finding is that the *aerosurface model + airframe constants* together produce unflyable dynamics that no (clQ, clAlphaDot) can resolve. This is an arch-layer question (which mechanism to add next, or which constant to revise) — the answer is not a tune-pass at any WP.
- **Relationship to other SURFACEs:** Consolidates and largely supersedes SURFACE-2026-05-16-04 (joint NaN-poisoning) — which is now superseded as the actionable arch driver (β4+β5 are no longer the bottleneck; airframe physics elsewhere is). SURFACE-2026-05-17-03 (narrow stable region) is partially closed — narrowing was the right pre-tune call, but the narrowed search still doesn't yield a flyable point. SURFACE-2026-05-17-01 (D15 reframe) full close blocked. SURFACE-2026-05-12-03 + SURFACE-2026-05-11-04 + SURFACE-2026-05-12-01 all blocked-by this SURFACE.
- **Memory anchors that fired this WP:**
  - `feedback_retune_attempt_budget.md` — 2 optimizer runs + 3 hand-probes = bounded budget; escalating per the rule's intent rather than continuing to widen the search.
  - `feedback_asymmetric_fix_no_op.md` — the harness-search vs deploy-symmetric mismatch is exactly the kind of asymmetric-fix problem this memory anchor flags; surfaced as the tooling fix candidate.
  - `feedback_verify_self_envelope.md` — embedded in the multi-regime score function; this WP's evidence shows the envelope is doing its job (catching unflyable finite trajectories that a "no NaN" pass would have missed).
  - `feedback_operator_as_external.md` — full-autopilot deviation held; SURFACE-IN documents the deviation for Phase 3 re-validation.
- **Status:** resolved-by-cascade-but-partial — D18 implementation landed at WP14.11.5 (2026-05-23 commit `a93c277`); 8-dim joint tune ran at WP14.12 (2026-05-24 commit TBD on this WP's finalize). Criterion 1 (all 3 regimes finite at 1800 ticks) PASS at globalBest; criterion 2 (deployed total ≥ −300) FAIL at globalBest (−92.6M vs threshold −300, ~309,000× gap). D18 refuted as sufficient third mechanism layer; the chosen path closed, but the underlying flyability gap remains. **Next architect cycle:** D19 (inertia-tensor revision) per SURFACE-2026-05-24-01 (filed by WP14.12 escalation). The chain of blocked-by SURFACEs extends one cycle.
- **Blocks:** WP15, WP16, WP17 (Phase 2 mission content); full close of SURFACE-2026-05-16-04, SURFACE-2026-05-17-03, SURFACE-2026-05-17-01, SURFACE-2026-05-12-03, SURFACE-2026-05-11-04, SURFACE-2026-05-12-01 (transitively blocked-by SURFACE-2026-05-24-01 now)

### SURFACE-2026-05-17-03 — D17 empirical stable clQ region is narrower than textbook 1–10 range (WP14.11 bounds revision + possible mechanism-interaction investigation)
- **Source:** feature:verify-self (WP14.9b Phase 1, 2026-05-17)
- **Target level:** product:wbs (WP14.11 bounds revision, no arch change)
- **Type:** tuning-bounds-revision + potential mechanism-interaction investigation
- **Priority:** high (gates WP14.11 cross-threshold finding; if WP14.11 fails to find a stable point in the narrowed bounds, escalates to product:arch as a third-mechanism-layer concern)
- **Summary:** WP14.9b verify-self triple-gate revealed the D17 stable clQ region under current aircraft.json (mass=1000, inertia=(1500,3000,1500)+ surface geometries) is **narrower than arch.md's "textbook 1–10" range claim** (Etkin & Reid Table 5.4). Empirical sweep at uniform clQ on all surfaces (throttle-high, 1800 ticks): clQ=0 finite ✓, clQ=1 finite ✓, clQ=2 NaN@1022, clQ=5 NaN@747, clQ=8 NaN@483, clQ=12 NaN@687. Higher clQ generally less stable (monotonic for 0→8); clQ=12 outlier may be due to wings-at-12 providing enough roll damping to delay a secondary instability mode. **Implementation is sound** — control clQ=0 finite (Rule #4 PASS), mechanism observably active at clQ=1 (different trajectory from clQ=0 in both throttle-low and throttle-high regimes per Rule #1 probe), default-parity preserved (520/520 Vitest under aircraft.json baseline). The narrowness is a tuning question.
- **Evidence:** `/tmp/wp14.9b/triple-{baseline,clQ12,clQ0,clQ1,clQ2,clQ5,clQ8}.csv` (gitignored; reproducible via `npm run --silent harness -- --fixture throttle-high --ticks 1800 --params "surfaces.0.clQ=N,surfaces.1.clQ=N,surfaces.2.clQ=N,surfaces.3.clQ=N"`). Also `/tmp/wp14.9b/rule1-{throttle-low,throttle-high}-clQ{0,1}.csv` for the Rule #1 sign-direction observation.
- **Suggested action (WP14.11):**
  1. Tighten optimizer bounds from `[0..15]×[0..15]` to `[0..2]×[0..2]` per surface as the primary search range. The empirical stable region is `[0..1.5]` for uniform clQ; per-surface heterogeneity (h-stab can take more damping than wings) may widen the joint space.
  2. Score function should heavily weight finiteness — at the narrowed bounds, the optimizer is searching a small space and shouldn't waste budget on NaN regions.
  3. If WP14.11 cannot find a cross-threshold point in `[0..2]` per surface, escalate to product:arch — possibly a third-mechanism-layer concern (Theodorsen function for unsteady aero, separate moment-of-inertia handling, or interaction between β4 D17 form and the unfixed β5 raw-rate form). Note: WP14.10 (β5 D16 non-dim form) might widen the stable region by fixing β5's overshoot — worth re-probing the clQ region AFTER WP14.10 lands but BEFORE WP14.11's optimizer run.
- **Relationship to other SURFACEs:** This is a sub-finding of the broader D17 cascade. Does NOT block WP14.9b close (implementation sound per WBS WP14.9b close gate). DOES gate WP14.11 — WP14.11's bounds should be revised before optimizer run.
- **Status:** partial — bounds revision applied in WP14.11 (narrow-bounds re-run at `[0..3]×[0..10]^4` per surface). Recommendation was correct (the narrowed search confirmed the narrowness of the β4 stable region holds under D16 — see Phase 1 sweep in archived WIP). Now superseded by SURFACE-2026-05-23-01 as the actionable arch driver. Full close awaits resolution of SURFACE-2026-05-23-01.

### SURFACE-2026-05-17-02 — arch.md D17 dampAxis cross-product order is sign-inverted vs textbook damping convention
- **Source:** feature:build (WP14.9b Phase 1, 2026-05-17)
- **Target level:** product:arch (D17 errata — single-character fix to literal spec text)
- **Type:** arch-errata / sign-convention
- **Priority:** medium (cosmetic — implementation already proceeds with the corrected sign per CLAUDE.md Rule #1 live-derivation; arch.md prose just needs to match what the code does)
- **Summary:** arch.md D17 (Revision 2026-05-17) and the WP14.9b WBS entry both specify `dampAxis = (position × normal).normalized()`. By right-hand rule on the canonical h-stab (`position=(0,0,3), normal=(0,1,0)`), this gives `(−1, 0, 0)` = anti-pitch axis. The textbook β4 damping convention requires `dot(angular_velocity, dampAxis) · clQ > 0` for damping in the moment direction (positive pitch rate × positive clQ → positive ΔCL → upward force at aft surface → nose-down moment → damps the pitch). With the literal arch.md order, `dot((1,0,0), (−1,0,0)) = −1`, so ΔCL is negative — that's ANTI-damping (amplifies the +pitch rate). The corrected order is `(normal × position)`, giving `(+1,0,0)` = +pitch axis = correct damping sign.
- **Verified across all 3 surface types under the corrected order:** h-stab → +X (pitch damping); wing-right → −Z (anti-roll damping when right wing goes down on +roll); wing-left → +Z (opposite, correct); v-stab → primarily −Y (yaw-damping direction).
- **Evidence:** WP14.9b Vitest run after attempt-1 implementation with literal arch.md sign: `positive clQ amplifies rotation-induced airflow → larger damping force on rotating body` failed at line 868 (dampedY < undampedY because ΔCL was reducing total CL, not adding to it). After flipping the cross-product order in code, behavior aligns with the test's expectation.
- **Suggested action:** arch.md D17 prose update (single-character fix: `(position × normal)` → `(normal × position)` in lines ~697-700 of `docs/product/arch.md` AND in the Risk 3 v-stab derivation example). WP14.9b WBS entry has the same literal text — update both. The dampAxis field-doc in `aerosurface.ts` already records the deviation + reasoning. No code change needed beyond what WP14.9b already shipped.
- **Status:** open
- **Relationship to other SURFACEs:** This is a sub-finding of the broader D17 cascade — it does NOT block WP14.9b close (the code is correct; only the arch prose needs updating). Closes when the arch.md errata commit lands.

### SURFACE-2026-05-17-01 — D15 Form A "moment-amplification-ratio" implementation does not eliminate the SURFACE-2026-05-16-01 instability — Form A must be implemented at the ω-correction level, not the airflow-amplification level
- **Source:** feature:verify-self (WP14.9 Phase 1, 2026-05-17)
- **Target level:** product:arch (D15 implementation-shape clarification — possibly D15 revision, or scoping of WP14.9 from S to M/L)
- **Type:** arch-clarification / scope-correction (the spec's prose is mathematically sound; the implementation shape it suggests is ambiguous and the "simple" reading fails empirically)
- **Priority:** high (gates WP14.9 close; gates WP14.10/WP14.11 cascade; downstream gates WP15/WP16 mission content)
- **Summary:** WP14.9 attempted to implement D15 Form A as a single-line refactor of the airflow-amplification factor at `src/aircraft/physics-core/aerosurface.ts:449`: replace `(1 + clQ · vScale)` with `(1 + clQ · vScale) / (1 + clQ · max(0, vScale − 1))`. The new factor is bit-identical to pre-D15 at `vScale ≤ 1` (low-V calibration preserved) and asymptotically bounded above V_REF (factor → 1 as `vScale → ∞`). The hypothesis was that the divisor IS the implicit-Euler correction `(1 + dt · k_excess / I)` for excess damping above the WP6.5 baseline, with `k/I` absorbed into the dimensionless form. **The harness empirically refutes this:** at throttle-high (1800-tick parity fixture), the baseline (`clQ=3,8`) NaN's at tick 416 — within 1 tick of the pre-D15 SURFACE-2026-05-16-01 onset at tick 417. Non-default `clQ=12` lasts longer (NaN at tick 817) but still fails. With `clQ=0` (β4 disabled entirely) the trajectory stays finite all 1800 ticks — confirming β4 IS the sole instability driver and D15's "moment-amplification-ratio" implementation does not actually fix it.
- **Diagnostic interpretation:** the moment-amplification-ratio implementation makes the damping factor DECREASE with V (from 9 at v=V_REF to ~1 asymptotically). At high V the lift-moment-from-`ω×r` scales as V², so the un-damped destabilizing moment GROWS quadratically while the damping factor shrinks toward 1. Result: above V_REF, the new code provides LESS net damping than the un-amplified case, making the system effectively undamped at exactly the regime where damping is most needed. The arch.md D15 spec is correct that the implicit-Euler form keeps the discrete-time pole inside the unit circle, but achieving this requires applying the correction at the **ω-correction level** (post-force, before Rapier's step, with explicit access to the body's inertia tensor), NOT at the airflow-amplification level inside `computeAeroForce`.
- **Diagnostic evidence (artifacts attached):**
  - Baseline (clQ=3,8) trajectory: `/tmp/wp14.9-verify-baseline.csv` (gitignored; reproducible via `npm run --silent harness -- --fixture throttle-high --ticks 1800 --out <path>`). First NaN at tick 416. 1384 NaN rows of 1800.
  - Non-default (clQ=12) trajectory: `/tmp/wp14.9-verify-clq12.csv`. First NaN at tick 817. 983 NaN rows of 1800.
  - clQ=0 control trajectory: `/tmp/wp14.9-verify-clq0.csv`. Zero NaN rows. 1800 finite ticks. Confirms β4 isolation.
  - WIP retrospect: `workflow/wip/wp14.9-beta4-implicit-euler.md` → `## Resolution` (records the "moment-amplification-ratio" hypothesis and Why-It-Worked-In-Theory).
- **Why this matters at the arch layer, not the WP layer:**
  - The arch.md D15 spec text describes the math `ω_{n+1} = (ω_n + dt · M_un_damped / I) / (1 + dt · k / I)` correctly but says "Apply this correction to ω before Rapier's own step" — which means the implementation site is at the FlightModel layer (where AircraftBody is accessible) or higher, NOT inside the pure-math `computeAeroForce` (which has no inertia access by design).
  - WP14.9 was sized S in the WBS on the assumption that the fix was a hot-path one-liner. The actual fix requires (a) caching `bodyInertia` per surface (or threading at compute-time), (b) deriving each surface's per-axis damping target, (c) extracting the would-be damping moment from `clQ` as a separate quantity (decoupled from the airflow-amplification step which currently couples force + moment via the CL curve nonlinearity), (d) computing the implicit correction at the AircraftBody-step level with explicit `M/I` and `k/I` terms. This is M-L sized, not S.
  - Possible alternative interpretations of D15 not yet refuted: (a) ω-pre-correction by direct angular-velocity scaling (write the corrected ω back to Rapier before step), (b) moment-input adjustment where Rapier is fed a pre-corrected torque, (c) Form B (the "magnitude clamp" arch.md called fallback) — actually this is now the empirically next-most-promising option since the moment-amplification-ratio form was refuted in attempt 1.
- **Recommended next action (operator-as-architect call):**
  - **Option 1 (preferred — clarify and re-implement):** arch revision that pins D15 implementation shape: ω-correction at FlightModel level with per-surface inertia caching. Re-scope WP14.9 from S to M-L, or fold the ω-correction infrastructure into a new WP14.9a (infra) + WP14.9b (Form A implementation on the new infra).
  - **Option 2 (fallback — re-evaluate Form B):** Form A is empirically refuted in the simplest interpretation; Form B (damping-moment magnitude clamp) becomes the next-cheapest test. arch.md D15 said Form B was rejected because "it loses asymptotic correctness — at large clQ it caps to 'no damping reversal' rather than 'smooth exponential decay'" — but smooth exponential decay is exactly what attempt 1 *also* failed to deliver, so re-evaluation is warranted.
  - **Option 3 (full reframe):** the diagnostic ("β4 explicit-Euler instability above V_REF") may itself be incomplete — examine whether the V-scaling rationale `(1 + clQ · max(v, V_REF) / V_REF)` is the *right* shape at all. Possibly the V-scaling itself is what should be revised, not the explicit-vs-implicit dimension.
- **Code state at SURFACE filing:** WP14.9's attempt-1 code change has been committed locally to the working tree (not pushed). The `aerosurface.ts:449` factor uses the moment-amplification-ratio form; the `chordLength` cache is in place (useful for WP14.10 regardless of the D15 outcome); the `aerosurface.test.ts` regression anchor at line 916 has been rewritten to assert `yHigh < yRef` (the bounded-asymptote contract of attempt-1). **If the operator picks Option 1 or Option 3, this code change should be reverted; if Option 2, the test rewrite stays and the implementation pivots.**
- **Status:** D17 reframe shipped at WP14.9b (commit `0df9a07`); β4 mechanism layer no longer suspect. Tuning side of the original SURFACE superseded by SURFACE-2026-05-23-01 (third-mechanism-layer concern). Origin record stays as historical reference for the D15→D17 reframe story.
- **Blocks:** (none active — D17 + WP14.9b shipped; downstream tuning concerns moved to SURFACE-2026-05-23-01)
- **Supporting evidence for Option 3:**
  - **The V-scaling formula was never an arch decision.** arch.md has no D-section for it. It was introduced empirically in WP6.6 under SURFACE-2026-05-11-03 to avoid a `1/V` singularity. Replacing it doesn't invalidate any arch decision — it closes a gap that was never closed.
  - **The textbook form is well-known.** Etkin & Reid *Dynamics of Flight* §5.10–5.12 and standard pitch-rate damping derivations give `cl_q · (q · c̄ / 2V)` — a *dimensionless* angular-rate term. arch.md's D16 already cites this for β5 (AoA-rate damping); the β4 (pitch-rate) form is structurally identical, just with `ω_along_pitch_axis` substituted for `dα/dt`.
  - **Dimensional analysis refutes the current form.** The current `(1 + clQ · vScale)` amplifies the rotation contribution to airflow, which then enters the lift force `0.5·ρ·V²·A·CL`. Net damping scaling vs V: `(linear in vScale) × (V² in dynamic pressure) = V³`. The textbook form scales as `(1/V) × V² = V` — linear, not cubic. The current cubic shape is *why* the explicit-Euler stability boundary is exceeded above V_REF: the destabilizing-moment-from-`incidenceRad` grows as V², while the damping was being given a cubic growth rate that overshoots into instability.

### SURFACE-2026-05-16-04 — β5 + β4 joint parameter space is empirically NaN-poisoned at dt=1/60 — both mechanisms need arch revision (Option A on each)
- **Source:** feature:build (WP14.5-retry Phase 1+2B, 2026-05-16)
- **Target level:** product:arch (mechanism revision — likely D15 or two separate D-decisions, one per coefficient)
- **Type:** arch-gap / mechanism-design-bug / convergence-of-prior-SURFACEs
- **Priority:** high (this is the *consolidated* escalation that supersedes SURFACE-2026-05-12-03 + SURFACE-2026-05-16-01 as the actionable arch-revision driver; gates WP15 takeoff/landing and WP16 combat for any non-trivial flight envelope)
- **Summary:** WP14.5-retry ran the D14 harness-optimizer (`npm run tune`) over a 4D box covering (clQ, clAlphaDot) jointly for wing-left and h-stab, at the canonical command `npm run tune -- --knobs surfaces.0.clQ,surfaces.0.clAlphaDot,surfaces.2.clQ,surfaces.2.clAlphaDot --bounds 0..20,-10..20,0..20,-10..20 --regimes low,mid,high --restarts 4 --seed 42`. **All 4 random restarts converged to the NaN floor at score = -2,999,999,985 (= 3 × -1e9 + ~15) — i.e., every regime NaN's at tick ~5 across the entire box.** Supplementary hand-picked probes at 4 more (clQ, clAlphaDot) points (baseline, zero-clQ, tiny-clAD, low-clQ) confirmed: no point in the searched space produces all-3-regimes-finite trajectories. The current `aircraft.json` baseline (clQ=3,8; clAlphaDot=0) is itself NaN in high-throttle at tick 417 — exactly reproducing SURFACE-2026-05-16-01's diagnostic — and is the *least bad* sampled point.
- **Diagnostic evidence (artifacts attached):**
  - Optimizer results JSON: `tools/tune/results/wp14.5-retry.json` (gitignored; reproducible via the canonical command above with `--seed 42`)
  - Per-regime CSV trajectories at the optimizer global best: `tools/tune/results/wp14.5-retry-traj-best-{low,mid,high}.csv` (all 3 NaN at tick 5)
  - Per-regime CSV trajectories at the current baseline: `tools/tune/results/wp14.5-retry-traj-baseline-{low,mid,high}.csv` (low/mid finite over 1800 ticks; high NaN at tick 417 — matches SURFACE-2026-05-16-01's tick-417 observation precisely)
  - 4-point hand-picked probe table recorded in `workflow/wip/wp14.5-retry-joint-tune.md` → `## Phase 1 outcome`
  - Analysis script: `tools/tune/probe-baseline.mjs` (gitignored under tools/tune/results/'s sibling; will move/delete in finalize)
  - Trajectory-dump script: `tools/tune/dump-trajectories.mjs` (likewise)
- **Regression-Hessian analysis (degeneracy IS the evidence):**
  - The optimizer's quadratic-regression step (per arch.md §D14.6) returned `null` for `gradient`, `hessian`, and `conditionNumber` on all 4 restarts.
  - Per `tools/tune/optimizer.ts`, this happens when the simplex's score samples have insufficient variance for a stable quadratic fit. At the NaN-floor regime, every sample scores `-1e9 + nanTick` with `nanTick ∈ {tiny range}`, so the regression solver finds no informative quadratic shape.
  - **Diagnostic interpretation:** the entire parameter space sampled (4D box [0..20]×[-10..20]×[0..20]×[-10..20]) is uniformly NaN-poisoned at dt=1/60. There is no gradient direction the optimizer could climb. This refutes Option D (parameter-search-based fix) of SURFACE-2026-05-16-01 decisively — the issue is fundamental to the mechanism shape, not a corner of the parameter space we hadn't tried.
- **Per-regime score breakdown at sampled points:**
  | Configuration | low | mid | high | total |
  |---|---|---|---|---|
  | Optimizer global-best (clQ=12.02, clAD=3.45, clQ_h=17.05, clAD_h=10.09) | -1e9+5 | -1e9+5 | -1e9+5 | -2,999,999,985 |
  | Baseline (clQ=3,8; clAD=0,0) — current aircraft.json | -11,316 (finite) | -68,804 (finite) | -1e9+417 (NaN @ tick 417) | -1,000,079,703 |
  | Zero (clQ=0,0; clAD=0,0) | -10,146,201 (finite) | -1e9+1387 | -108,047,202 (finite) | -1,118,192,015 |
  | Tiny clAD (clQ=3,8; clAD=0.1,0.1) | -1e9+199 | -1e9+137 | -1e9+85 | -2,999,999,579 |
  | Lower clQ (clQ=1,1; clAD=0,0) | -1e9+344 | -1e9+290 | -1e9+302 | -2,999,999,064 |
- **Recommended mechanism revisions (both required; not OR — these are independent issues per `feedback_surface_or_means_or.md`):**
  - **β4 mechanism revision (resolves SURFACE-2026-05-16-01):** Adopt **Option A — implicit-Euler form for the damping term**. The empirical pattern (high-throttle NaN at tick 417 driven by stiff-damper sign-flips above V_REF) is precisely what implicit-Euler is designed to fix. Option B (magnitude clamp) and C (vScale clamp) are half-measures and are not supported by the data — the optimizer searched the relevant clamp range and found no stable point. Option D (lower clQ) is **REFUTED** — lower clQ collapses WP6.5's ω×r baseline stability.
  - **β5 mechanism revision (resolves SURFACE-2026-05-12-03):** Adopt **Option A — non-dimensional form `cl += clAlphaDot · (dα/dt) · c̄ / (2V)`** with `max(V, V_REF)` floor (analogous to WP6.6's β4 V-scaling fix). The tiny-clAD probe (0.1 magnitude) NaN's within 85-199 ticks across all regimes — confirming the raw `dα/dt` mechanism is dimensionally wrong at physics-tick scale. Option B (magnitude clamp) might mitigate but doesn't address the dimensional issue. Option C (sign flip) is **REFUTED** — restart 3's clAD_h=-9.88 produces identical NaN-floor as positive-sign restarts.
  - **Suggested WBS unfolding:** Two arch-decision candidates (D15 β4-implicit-Euler, D16 β5-non-dim form), each with a schema-landing WP + a tuning-via-harness WP. Per the **physics-mechanism discipline** in CLAUDE.md (rules #1, #2, #3): both schema-landing WPs must verify-self at a non-default coefficient (NOT just default-zero parity) before close; both tuning WPs must run through `npm run tune` (no hand-guessing); both must observe live before any sign-convention unit test is written.
- **Verification approach when fixed:** `tests/e2e/phugoid-probe.spec.ts` (currently `test.skip(true, 'WP14.5 disposition — β5 mechanism needs arch revision; see SURFACE-2026-05-12-03')` at line 18) un-skipped + all 3 throttle bands pass. Additionally, re-running `npm run tune` with the canonical command above should produce a results JSON with `.score > -300` and a non-null `.regression.gradient` (informative quadratic shape).
- **Why we did not catch this at WP10.5 / WP14.6 / WP14.7:** WP10.5 closed β5 schema-landing on 256/256 default-zero parity tests (Rule #2 of physics-mechanism discipline was not yet codified — this WP's findings are why that rule now exists). WP14.6/14.7 closed harness-parity on low/mid throttle fixtures + the throttle-high fixture but as "parity of divergence" (browser and node-stub diverge identically), not as "parity of stability." This SURFACE is the WP14.5-retry-empirical-search outcome — the systematic search the harness exists to enable. The cascade did its job: WP14.6 + WP14.7 + WP14.8 produced the tool that allowed WP14.5-retry to refute Option D in 7.24 seconds of wall-clock.
- **Memory anchors that fired this WP:**
  - `feedback_retune_attempt_budget.md` — 4 optimizer restarts + 4 hand-probes = bounded budget; escalating per the rule's intent (the optimizer IS the systematic budget at this scale).
  - `feedback_surface_or_means_or.md` — search space was joint, not stacked options; held throughout.
  - `feedback_asymmetric_fix_no_op.md` — fired diagnostically: the empirical evidence shows the current mechanisms are NOT no-ops in the working regime (baseline NaN's in high throttle), so any revision must satisfy this property explicitly.
  - `feedback_verify_self_envelope.md` — embodied INSIDE the score function (multi-regime weighted sum across low/mid/high); this WP's evidence shows the lesson worked — the envelope-aware score caught what the prior single-regime mid-throttle tests missed.
  - `feedback_operator_as_external.md` — full-autopilot deviation held; verify-human SKIPPED across Phase 1; this SURFACE is the durable record of the deviation's Phase 1 outcome for future re-validation.
  - `feedback_memory_active_recall.md` — re-greped at each phase-build entry.
- **Status:** superseded — SURFACE-2026-05-23-01 is now the actionable arch driver post-WP14.11. β4 + β5 mechanism halves are confirmed working as designed (numerical finiteness achieved at multiple (clQ, clAlphaDot) points); the unflyable-trajectory problem is no longer in the β4/β5 mechanism layer. This SURFACE stays as historical record of the joint-NaN-poisoning observation that drove the D17+D16 cascade.

### SURFACE-2026-05-16-03 — arch.md §D14.4 NaN-penalty formula has wrong sign vs stated intent
- **Source:** feature:build (WP14.8 Phase 1, 2026-05-16)
- **Target level:** product:arch (doc-typo level — one character)
- **Type:** doc-bug / arch-text-typo
- **Priority:** low (does not affect any code; intent is unambiguous and was followed)
- **Summary:** arch.md §D14.4 literally writes the NaN penalty as `-1e9 - tick_of_first_NaN` while the surrounding text says "Higher is better" AND "the optimizer can move *toward* later-NaN regions" (prefer-failing-later). Under higher-is-better, `-1e9 - tick` makes EARLIER NaN score higher (better), which is the opposite of the stated intent. The correct formula honoring the intent is `-1e9 + tick_of_first_NaN`. WP14.8 Phase 1's `tools/tune/score.ts` implements the intent-correct formula; the file header documents the discrepancy.
- **Suggested action:** One-character edit to arch.md §D14.4 — change `-1e9 - tick_of_first_NaN` to `-1e9 + tick_of_first_NaN`. No code change needed (score.ts already correct).
- **Verification approach:** N/A — text edit.
- **Status:** pending

### SURFACE-2026-05-16-02 — Wall-clock perf assertion in `flightmodel.test.ts:368` is load-flaky
- **Source:** feature:verify-codify (WP14.7 Phases 1/2/3, 2026-05-16)
- **Target level:** task (small test-refactor)
- **Type:** test-design / flake
- **Priority:** low (does not affect ship readiness; full-suite reliably passes within 1-3 runs; pre-existing — not introduced by any WP14.7 change)
- **Summary:** The Phase 1 codify cycle, Phase 2 codify cycle, and Phase 3 verify-self cycle all observed at least one failure of `src/aircraft/physics-core/flightmodel.test.ts:368` (`expect(elapsed).toBeLessThan(50)` after 1000 `applyForces` calls). Failure values were ~50.8ms, ~52.0ms — 1.6%-4% over a 50ms wall-clock threshold. The same test passes 19/19 in isolation consistently; the flake only manifests under full-suite parallelism. Triaged each occurrence as load-induced under the codify discipline; no code or test modification was made (triage hard rule). No occurrence affected ship — every full-suite run that flaked was followed by a clean re-run.
- **Update 2026-05-16 (WP14.8 Phase 3 codify):** Worst-case sustained-flake run observed — 3 consecutive full-suite failures at 86.68ms / 79+ms (vs the prior 50-75ms range), then a 4th full-suite run came back 516/516 green. Isolation run of the same test consistently passes in ~38ms (well under the 50ms threshold). The widened tail confirms the flake is system-load-induced — running multiple `npm run test` invocations in close succession during a single agent session can stack CPU contention. **Action recommendation reaffirmed:** option (a) relative-baseline rework or option (b) move to a perf-only invocation. Priority remains low but the heavier-tail observation suggests the fix should land sooner than later if codify cycles continue to take 4+ retries.
- **Suggested action:** rework the perf assertion. Two reasonable shapes:
  - **(a) Relative baseline:** measure a known-cheap reference op (e.g. 1000 empty `for` iterations) at the start of the test, scale the threshold proportionally. Removes the absolute wall-clock dependency.
  - **(b) Move to a perf-only test invocation:** tag the test with a Vitest tag like `@perf` and exclude from `npm run test`; run only via `npm run test:perf`. Keeps the regression signal for explicit perf-monitoring runs without flaking CI.
- **Rationale for low priority:** the flake is observed about 1 in 3 full-suite runs and is always resolved by a single re-run. It does not block any feature. The perf-proxy intent (catch a regression that would 10× the per-tick cost) is still useful but a 50ms threshold at 1000 calls = 50 μs/call is too tight a margin for wall-clock measurement on a loaded CPU.
- **Status:** pending

### SURFACE-2026-05-16-01 — β4 (`clQ`) explicit-Euler instability above V_REF — h-stab drives runaway in ~7s at 0.4 throttle
- **Source:** feature:verify-auto (WP14.7 Phase 1, 2026-05-16)
- **Target level:** product:arch (mechanism revision — analogous to D14 surfacing β5; this surfaces β4 stability hole)
- **Type:** arch-gap / numerical-stability-bug / mechanism-design-bug
- **Priority:** high (blocks the new throttle-high parity fixture from asserting full-trajectory stability; same blocking shape as SURFACE-2026-05-12-03 for sustained-throttle Phase 2 missions; gates WP15 takeoff/landing and WP16 combat above V_REF)
- **Summary:** WP14.6 shipped the β4 fix in `aerosurface.ts:445-450` per the prior arch revision: `(ω × r)` is amplified by `(1 + clQ · max(v, V_REF) / V_REF)`, with the `max(v, V_REF)` floor preserving WP6.5's low-V β4 calibration bit-for-bit. The arch claim was that the floor "matches the V² growth of the destabilizing pitch moment from `incidenceRad`, keeping damping ratio constant." That claim is dimensionally correct in continuous time, but **falsifies at dt=1/60 once v exceeds V_REF=30**: the discrete-time damping stiffness exceeds what explicit Euler can integrate stably. WP14.7's new `throttle-high` parity fixture (0.4 throttle, 30s) is the first fixture that takes body |v| above V_REF for a sustained window and surfaced this immediately.
- **Diagnostic data (captured via /tmp/diagnose-blowup.ts, 2026-05-16):**
  - **Ticks 0–407:** low-throttle phugoid oscillation, bounded. Body |v| < V_REF=30 throughout. β4's `vScale` is floored at 1; amplification = `(1 + 8·1) = 9` constant on h-stab.
  - **Tick 408:** body |v| crosses 30 m/s. h-stab sees |v|=59 m/s (vs body 11 — ω×r contribution dominates), force = 222,000 N.
  - **Tick 411:** body |v|=50, h-stab |v|=93, **force = 950,000 N**. h-stab force is now ~80× the wing force.
  - **Tick 412–414:** angvel sign-flips and grows: -2.4 → -25 → +38 → -262 rad/s. Classic stiff-damper signature: damper reverses ω each tick with more energy than ω had. Force: 21k → 2.6M → 10.9M N.
  - **Tick 415:** aircraft flips 180° pitch+roll+yaw in one tick. h-stab force = 1.27 × 10⁹ N.
  - **Tick 416:** |v| = 21,396 m/s. Force = 5.6 × 10¹⁷ N.
  - **Tick 417:** NaN/Infinity. All four surfaces, all components.
- **Surfaces involved:** h-stab is the primary driver (clQ=8, largest coefficient in `aircraft.json`). Wings (clQ=3) also contribute but are stable in isolation at this regime. v-stab (clQ=0) is unaffected.
- **Root cause:** numerical stiffness. `clQ=8` means the discrete-time effective damping coefficient on pitch rate, multiplied by lever arm² and dynamic pressure, exceeds `2/dt` once v > V_REF — putting the explicit-Euler-style update past its stability boundary. The "constant damping ratio" arch claim assumed continuous-time integration; at dt=1/60 the discrete-time system has a Nyquist constraint on coefficient magnitude that the formula does not respect.
- **Why we did not catch this earlier:** WP6.5's β4 calibration regime is `V < V_REF` (descending-glide attractor, where the floor keeps the formula at constant `(1 + clQ)`). WP10.5's β5 schema-landing parity test was 30s at mid throttle, also `V < V_REF` throughout. WP14.6's first parity fixture was mid throttle (same regime). No fixture exercised sustained `V > V_REF` until WP14.7 Phase 1.4 added throttle-high to close the `feedback_verify_self_envelope.md` gap. This is the lesson firing exactly as intended — envelope-widening surfaced a real bug, not a fake one.
- **Compounds with SURFACE-2026-05-12-03 (β5):** β5 was the original phugoid-damping mechanism that drove the D14 harness cascade. The harness was intended to search the β5 parameter space; we now have a *second* parameter (clQ) that also needs joint-search, and any β5 tuning is meaningless if β4 is unstable at the same operating point. The harness/optimizer cascade's search space is now (clQ, clAlphaDot) jointly, per surface — not clAlphaDot alone.
- **Suggested action (arch-revision candidate, parallel to D14's β5 treatment):**
  - **Option A — implicit-Euler form for the damping term:** apply the rotational amplification implicitly, so the discrete-time pole stays inside the unit circle regardless of clQ. This is the correct fix and matches how real flight-sim codes handle stiff aerodynamic damping. Requires its own WP.
  - **Option B — magnitude clamp on the amplified ω×r contribution:** clamp `|angCross_amplified| ≤ K · |angCross_unamplified|` for some K (say 5). Crude but preserves WP6.5 calibration when amplification is small and caps the runaway. Half-measure.
  - **Option C — clamp `vScale` itself:** cap `vScale` at some maximum (e.g. 3, equivalent to capping at v=3·V_REF=90 m/s). Smallest delta. Doesn't fix the underlying numerical-stiffness problem but raises the divergence threshold meaningfully.
  - **Option D — use the harness/optimizer (post-WP14.8) to search for a stable clQ at dt=1/60.** Lower clQ might be sufficient; the calibration came from real-aircraft data but our integrator's stability bounds matter more than the real damping derivative.
- **Verification approach when fixed:** WP14.7's `throttle-high` parity fixture is the canonical regression check. Full 1800-tick trajectory must stay finite, with `|browser - synthetic| < 1e-6` at every row.
- **Why we accept it for WP14.7 Phase 1 close:** The mechanism is pre-existing (shipped at the prior β4 revision, predating WP14.7). WP14.7's job is to build the harness, not to fix β4 — but the throttle-high fixture *will not* satisfy the original "full-trajectory parity" goal until β4 is fixed. The honest disposition: keep throttle-high in `PARITY_FIXTURES`, redefine the parity-diff assertion to "parity of divergence" (browser and synthetic must diverge at the same tick with the same NaN/Infinity pattern), document the SURFACE here, and move on. Phase 1 ships with a known mechanism gap surfaced and waiting for the harness to be able to *search* for a fix.
- **Memory anchors that fired:** `feedback_verify_self_envelope.md` (the new throttle-high fixture closed an envelope gap and surfaced this immediately — exactly its purpose); `feedback_asymmetric_fix_no_op.md` (the previous β4 fix violated this in the high-V regime — the formula was a no-op below V_REF but became newly destabilizing above it); the operator's own framing during disposition discussion ("aren't we in search of a flight mode that can be flyable?") forced a re-read of the diagnostic instead of green-washing the test.
- **Update 2026-05-16 (later):** **WP14.8 shipped** (commit `f2fcc36`). D14 cascade fully landed. `npm run tune` is live; the optimizer can now search jointly over (clQ, clAlphaDot) per surface — exactly the joint-search this SURFACE forced into WP14.8's scope. Resolution path now reads: WP14.5-retry runs the joint-space optimizer; if it finds a stable parameter point, this β4 issue is resolved by parameter selection (Option D in the original suggested-action). If no stable point exists across regimes, WP14.5-retry escalates to Options A/B/C with regression-gradient evidence from the optimizer's results JSON.
- **Update 2026-05-16 (final):** **WP14.5-retry escalated → SURFACE-2026-05-16-04.** Option D refuted decisively: the optimizer's 4 random restarts plus 4 hand-picked probes (8 distinct (clQ, clAlphaDot) points) all produce NaN-floor outcomes across regimes. The baseline aircraft.json NaN's in high-throttle at exactly tick 417 (matching this SURFACE's diagnostic). The optimizer's regression Hessian came back null (degenerate) — there is no gradient direction in the searched 4D box. Per evidence, the recommended fix is **Option A (implicit-Euler form for the damping term)**, not parameter selection. See SURFACE-2026-05-16-04 for the consolidated escalation with full empirical evidence + per-regime CSV trajectories.
- **Update 2026-05-17:** **WP14.9 attempt-1 escalated → SURFACE-2026-05-17-01.** Option A's "moment-amplification-ratio" interpretation refuted at verify-self (baseline NaN at tick 416, vs pre-D15 tick 417 — within 1 tick). Operator selected Option 3 (full reframe of V-scaling shape, not implicit-vs-explicit). The actionable driver is now -17-01, which targets a textbook non-dimensional pitch-rate-damping form `cl_q · ω · c̄ / (2V)` parallel to D16's β5 form. The cubic V³ damping growth of the current `(1 + clQ · vScale)` form is now understood as the root cause (textbook gives linear V).
- **Status:** β4 mechanism side closed by WP14.9b (D17 impl); tuning side superseded by SURFACE-2026-05-23-01. Origin record stays as historical reference (the SURFACE-2026-05-16-01 high-throttle β4 runaway IS now resolved by D17 — `clQ=0` runs finite, `clQ=1` runs finite under aircraft.json baseline; the remaining unflyability is no longer about β4).

### SURFACE-2026-05-12-03 — β5 (`clAlphaDot`) mechanism diverges at any tuning value — needs arch revision (likely D14)
- **Source:** task:act (WP14.5 tuning attempts 1-3, 2026-05-12)
- **Target level:** product:arch (analogous to D13 → D14 mechanism revision; or WP10.5-style schema fix WP)
- **Type:** arch-gap / mechanism-design-bug
- **Priority:** high (blocks WP14.5 tuning side; blocks any sustained-throttle Phase 2 mission per SURFACE-2026-05-12-01; gates WP15 takeoff/landing and WP16 combat for non-trivial flight envelopes)
- **Summary:** WP14.5 attempted 3 tuning passes against the β5 mechanism at `src/aircraft/aerosurface.ts:475-483` (`cl += clAlphaDot · dα/dt` — raw rate, no V-normalization, no magnitude clamp). All three diverged catastrophically (alt to ±10³ to ±10¹² m, AS to 10⁵ to 10¹⁵ m/s within ~6s) at every tuning value tried:
  - **Attempt 1 (wings=+5, h-stab=+10):** SURFACE-2026-05-12-01's suggested starting range. ALL three throttle bands (0.05, 0.15, 0.4) diverged within 1-2s — not just the high-throttle target. The "no-op in working regime" goal from `feedback_asymmetric_fix_no_op.md` was violated catastrophically.
  - **Attempt 2 (wings=+1, h-stab=+2):** 5× smaller. Even worse — low/mid diverged in <2s; high diverged to 6.6e12 m alt, 8.4e14 m/s AS (vs pre-tune 6e10 m, 9e12 m/s — i.e. moving "with the wind" made divergence worse).
  - **Attempt 3 (wings=-1, h-stab=-2):** Tested the sign-convention hypothesis (sign-flipped tuning). Still diverges, but with reversed direction (alt → -26533 instead of +10⁹). Both signs blow up; the magnitude is the issue, not the sign alone.
- **Root cause (hypothesized):**
  1. The β5 mechanism uses **raw `dα/dt`** in rad/s without non-dimensionalization. At physics-tick scale (dt=1/60s), even 1° of AoA change per tick produces dα/dt ≈ 1.05 rad/s. Multiplied by clAlphaDot=1, that's a CL augmentation of 1.05 — comparable to the entire steady-state CL. Startup transients amplify this further (the aircraft settles into glide attitude in the first few ticks, producing large dα/dt).
  2. The mechanism creates a **positive-feedback loop** at certain sign/magnitude combinations: more lift → more pitch up → more α → more dα/dt → more lift.
  3. **Sign convention is documented but not physically validated.** The β5 unit test at `aerosurface.test.ts:1135` literally asserts that positive clAlphaDot + rising α produces additional lift — but additional lift on a rising α is **destabilizing**, not damping (analogous to the SURFACE-2026-05-10-01 AoA sign-convention bug lesson). The arch.md D13 description claims "positive clAlphaDot dampens" but does not derive this from physics.
- **Suggested action (arch-revision candidate D14):**
  - **Option A — non-dimensional form:** change the mechanism to `cl += clAlphaDot · (dα/dt) · c̄ / (2V)`, where c̄ is a reference chord length and V is body airspeed. This matches the standard aerodynamic form of cl_α̇ damping coefficients (Etkin/Reid). Adds a floor `max(V, V_REF)` analogous to WP6.6's β4 fix to avoid 1/V singularity at low V.
  - **Option B — magnitude clamp:** clamp `dα/dt` to [-DALPHA_MAX, DALPHA_MAX] (e.g., ±0.5 rad/s) before applying. Crude but kills the startup transient pathology.
  - **Option C — physics-side fix:** revisit the sign convention. If the test at `aerosurface.test.ts:1135` is wrong about which sign damps, flip the sign in `aerosurface.ts:482` and update the assertion. Then re-test tuning with positive values.
- **Verification approach when fixed:** the ≥30s Playwright probe at `tests/e2e/phugoid-probe.spec.ts` is already in place (3 test missions in `public/missions/`); just remove the `test.skip(...)` line at the top of the file. The probe exercises throttle ∈ {0.05, 0.15, 0.4}. The probe also serves as the "regression: descending-glide must still work" check (low throttle = the WP14 waypoint-patrol regime).
- **Why we accept it for WP14.5 close:** Per `feedback_retune_attempt_budget.md`, three attempts refuted decisively. The mechanism is unsuitable for direct tuning. Continuing to tune would be wasted budget — the real fix is an arch-level revision of the mechanism (D13 → D14, analogous to how WP6.6 added V-scaling to β4 after WP6.5). WP14.5 closes as "tuning refuted; surfaced arch revision." The pre-WP14.5 baseline (clAlphaDot=0 everywhere) is unchanged — current behavior preserved.
- **Memory anchors used during this task:** `feedback_retune_attempt_budget.md` (3-attempt cap held; option-c accepted); `feedback_surface_or_means_or.md` (single-knob changes per attempt — magnitude shrink, then sign flip); `feedback_asymmetric_fix_no_op.md` (revealed by violation: the mechanism is *not* a no-op in the working regime even at small coefficients, which is itself the diagnostic); `feedback_verify_self_envelope.md` (probed all three throttle bands — caught that Attempt 1 broke low-throttle too, not just high).
- **Update 2026-05-12 (afternoon):** Operator routed to `/product-arch` → **D14** landed (physics tuning harness + Nelder-Mead optimizer). Cascade WPs in WBS: WP14.6 (physics-core extraction + parity test) → WP14.7 (Node harness) → WP14.8 (optimizer) → rescoped WP14.5 (β5 tuning via harness). **WP14.6 shipped this afternoon** (`fb54c65` + `cf6254a`): `src/aircraft/physics-core/` is framework-agnostic Node-runnable; `tests/parity-diff.test.ts` asserts bit-identical browser↔Node-stub trajectories at `|Δ|<1e-6`. Cascade is unblocked but this SURFACE remains open — resolution requires WP14.7 + WP14.8 to ship, then rescoped WP14.5 to either tune β5 successfully (best case) or escalate to mechanism revision Options A/B/C with regression-gradient evidence (still open case).
- **Update 2026-05-16:** **WP14.7 shipped** (commit `8bca32c`). Node harness CLI is live (`npm run harness`, `npm run harness:parity`); harness-vs-browser parity holds at `|Δ|<1e-6` on finite rows. **Search-space scope expanded:** the throttle-high parity fixture surfaced SURFACE-2026-05-16-01 (β4 explicit-Euler instability above V_REF), which means the WP14.8 optimizer must search **(clQ, clAlphaDot) jointly per surface**, not clAlphaDot alone. Resolution path for this SURFACE now reads: WP14.8 lands → WP14.5-retry searches the joint space → tunes either both coefficients in tandem, or escalates to mechanism revision Options A/B/C with regression-gradient evidence from the harness/optimizer's empirical results.
- **Update 2026-05-16 (later):** **WP14.8 shipped** (commit `f2fcc36`). D14 cascade fully landed. `npm run tune` is live; optimizer + score function + CLI ready for the joint (clQ, clAlphaDot) search. WP14.5-retry is now genuinely unblocked — it can call the optimizer over the joint space and either commit results or escalate with regression-gradient evidence per the original plan.
- **Update 2026-05-16 (final):** **WP14.5-retry escalated → SURFACE-2026-05-16-04.** The joint optimizer + supplementary hand-picked probes (8 distinct (clQ, clAlphaDot) points across the 4D box) all NaN. The tiny-clAD=0.1 probe at the WP6.5-stable clQ baseline confirms the raw `dα/dt` mechanism diverges immediately even at small magnitudes (NaN at ticks 85-199 across regimes) — exactly this SURFACE's "raw dα/dt is dimensionally wrong" hypothesis. Option C (sign flip) refuted again — restart 3's clAD_h=-9.88 produces identical NaN-floor as positive-sign restarts. Recommended fix per evidence: **Option A (non-dimensional form `cl += clAlphaDot · (dα/dt) · c̄ / (2V)` with `max(V, V_REF)` floor)**. See SURFACE-2026-05-16-04 for the consolidated escalation.
- **Update 2026-05-23:** **D16 arch + WP14.10 impl shipped (commit `27324aa`).** Option A landed exactly as recommended: `cl += clAlphaDot · dα/dt · chordLength / (2·max(V, V_REF))` reusing the chordLength cache + shared BETA4_V_REF=30 from WP14.9. Verify-self triple gate PASS at clAlphaDot=5,5,8,0 (textbook 1–10 range) across throttle-low + throttle-mid (600 ticks all-finite); Rule #4 control (explicit clAlphaDot=0) bit-identical to baseline at throttle-mid; Rule #1 two-regime live observation confirms mechanism damps descent in predicted direction. Mechanism-revision side of this SURFACE is closed; tuning side remains for WP14.11.
- **Status:** mechanism side closed (D16 + WP14.10); tuning side superseded by SURFACE-2026-05-23-01. Origin record stays as historical reference; no further action on this SURFACE.

### SURFACE-2026-05-12-02 — Test-only probe missions are listed on player-facing mission-select
- **Source:** task:act (WP14.5 T1, 2026-05-12)
- **Target level:** feature:plan (small UX cleanup) or task
- **Type:** test-fixture / UX-papercut
- **Priority:** low
- **Summary:** The three `phugoid-probe-{low,mid,high}` JSON files (test fixtures for WP14.5's ≥30s probe) had to be added to `public/missions/index.json` because `src/main.ts:353` gates `?mission=<id>` deep-link auto-start on manifest membership (`missionManifest.some(m => m.id === requestedMissionId)`). They now appear on the mission-select screen alongside Free Flight and Waypoint Patrol.
- **Context:** Two clean fixes possible — (a) make the deep-link permissive: drop the manifest check, try to load the JSON directly, show error screen on fetch failure (the load already has the error path); (b) add a `hidden?: boolean` field to manifest entries and filter on render. Option (a) is smaller and also enables direct-URL access to any mission JSON that exists, which is useful for dev/test. Option (b) preserves manifest-as-allow-list discipline. Don't pick until someone asks.
- **Suggested action:** Pick (a) or (b) after WP14.5 is closed; tiny task workflow.
- **Status:** pending

### SURFACE-2026-05-12-01 — Phase 2 waypoint missions need non-zero `clAlphaDot` tuning to support sustained non-zero-throttle flight
- **Source:** feature:build (WP14 Phase 1 verify-self, 2026-05-12)
- **Target level:** product:wbs (likely a new WP14.5 — `clAlphaDot` tuning pass — analogous to WP10.5's schema-extension WP)
- **Type:** tuning / arch-tuning-side
- **Priority:** medium (gates "interesting" waypoint patrols at WP14, gates takeoff/landing at WP15, gates combat-circling at WP16)
- **Summary:** WP14 plan set spawn `throttle: 0.4` to mitigate the descending-glide attractor (per SURFACE-2026-05-11-02's suggested action) so waypoints at 600m distance could be reached. Verify-self at `/?mission=waypoint-patrol` produced NaN altitude/airspeed within ~3s of mission start — exactly the SURFACE-2026-05-11-04-documented phugoid divergence under non-zero throttle forcing. The two open SURFACE items (-11-02 descending-glide and -11-04 phugoid) are dual: you cannot mitigate -11-02 (higher throttle) without triggering -11-04 (phugoid divergence) at this airframe. The architectural close was already adopted (D13 — β5 `clAlphaDot` schema) and shipped at WP10.5; the schema currently ships with default 0. **The tuning side of D13 — setting non-zero `clAlphaDot` on wings/h-stab in `aircraft.json` — now needs to land.**
- **Context:** This is the predicted-and-anticipated next step in arch.md Rev 2026-05-12 — D13's text literally says "tuning per Phase 2 mission as needed" and WBS WP14 description says "First Phase 2 candidate for non-zero `clAlphaDot` tuning if the descending-glide attractor proves unplayable for sustained waypoint runs (per D13 — verify against ≥30s probe)." Confirmation: WP14 IS that mission.
- **WP14 disposition for THIS feature:** mission-scope reduced to a glide-reachable short patrol (2 waypoints within ~250m at descending altitude, spawn throttle=0, 30s timeout). This ships WP14 today as the minimum-viable waypoint mission. The "real" patrols (longer, climbing, sustained-throttle) wait on the tuning pass below.
- **Suggested action:** New WP14.5 (XS-S) `clAlphaDot` tuning pass — set non-zero `clAlphaDot` on wings (try ~5-10 starting) and h-stab (try ~10-15) in `aircraft.json`; verify against a ≥30s Playwright probe at non-zero throttle values 0.05, 0.15, 0.4 (per arch D13 verification requirement). If tuning works, follow up with WP14.6 ambitious-patrol mission OR amend `waypoint-patrol.json` to the original 4-waypoint loop spec.
- **Verification approach:** ≥30s Playwright probe at throttle values 0.05, 0.15, 0.4 — assert no NaN, bounded altitude/airspeed/pitch oscillation. Same probe planned for WP17 phase-2 verification per arch.md.
- **Why we accept it for WP14 ship:** The mission framework + HUD + waypoint mechanics + objective ordering ALL get end-to-end coverage with the reduced-scope patrol. The arch limitation (phugoid undamped at non-zero throttle) is honestly documented. Without this surface, we'd either ship a NaN'ing mission or stall WP14 indefinitely.
- **Memory anchors used:** `feedback_asymmetric_fix_no_op.md` (mission-local change is no-op for free-flight); `feedback_surface_or_means_or.md` (try ONE option — picked mission-geometry, not shared-config tuning); `feedback_retune_attempt_budget.md` (do not burn this WP's budget on shared-config tuning — escalate as separate WP).
- **Update 2026-05-12 (WP14.5 task):** **Tuning side cannot close via parameter sweep — escalated to SURFACE-2026-05-12-03 (mechanism revision needed).** WP14.5 attempted 3 tuning passes (+5/+10, +1/+2, -1/-2 on wings/h-stab); all diverged catastrophically. The β5 mechanism at `aerosurface.ts:475-483` uses raw `dα/dt` (rad/s) with no V-normalization or magnitude clamp, producing CL augmentations comparable to steady-state CL at typical startup transients. WP14.5 closed via option-c (revert config to pre-tune defaults; skip the new phugoid-probe spec). This SURFACE remains open as the *mission-side* anchor; SURFACE-2026-05-12-03 is the *mechanism-side* anchor and is now the actual blocker.
- **Update 2026-05-23:** **Mechanism-side blocker resolved.** WP14.10 (commit `27324aa`) landed D16's non-dim β5 form; β5 is now physically tunable in the textbook 1–10 range (vs the pre-D16 raw-rate form which NaN'd at 0.1). This SURFACE is no longer blocked-by-mechanism; it is now blocked-by-tuning (WP14.11 joint optimizer run).
- **Status:** pending — blocked by SURFACE-2026-05-23-01 (WP14.11 closed ESCALATED; third mechanism layer needed before this can resolve)

### SURFACE-2026-05-11-04 — Phugoid (long-period) mode is undamped at Phase 1 airframe
- **Source:** feature:build (WP7 Phase E retune attempts 1 + 2, 2026-05-11)
- **Target level:** product:arch (likely Phase 2 — not Phase 1 blocking)
- **Type:** tech-debt / arch-gap
- **Priority:** low–medium (NOT gating for Phase 1 ship; gates "level cruise feel" if/when that becomes a Phase 2 goal)
- **Summary:** WP7 Phase E tried two single-knob retunes (mass=700+throttle=0.15; mass=1000+throttle=0.05) against the WP6.6-baseline. Both were stable at short observation windows (≤7s) but **divergent at long windows (≥14s)**. Diagnosis: the phugoid mode (long-period airspeed↔altitude exchange, ~10–14s at this airframe) is fundamentally undamped. WP6.6 added airspeed-scaling to the *short-period* β4 damping (pitch-rate `q`) but did NOT add anything to dampen the long-period mode. Any non-zero baseline throttle injects energy that the airframe cannot dissipate per cycle, so the phugoid grows until tumble/NaN. The only stable attractor is throttle=0 (the descending glide). Phase E shipped option (c) — the WP6.5 baseline (descending glide) — and deferred the "feels like level flight" question to Phase F's casual-player feel-check.
- **Context:** This was anticipated as a possible failure mode in arch.md's "Fallback path" (which sanctioned β4-pitch-rate-damping as a hedge against case (3) "tuning is just hard"). The fallback path mentions option (3) automated parameter search but does NOT mention a long-period damping mechanism. A new arch decision would be required.
- **Suggested action (Phase 2 candidate):** Add `cl_alpha_dot` (alpha-rate damping) as a per-surface coefficient — phugoid is driven by AoA tracking lag at constant thrust, so damping `α̇` directly should attenuate the long-period mode. Could be analogous to WP6.6's V-scaling fix (a single field added to AeroSurfaceConfig, simple math in computeAeroForce). Alternative: simulate atmospheric drag growth more aggressively (currently the model has only cdMax=1.2 at ±π/2). Both are arch-level extensions; not in scope for Phase 1.
- **Verification approach:** any future fix must verify against a ≥30-second Playwright probe (3+ phugoid periods) with various baseline throttle values (0.05, 0.15, 0.4) — single-period observation hides the divergence.
- **Why we accept it for Phase 1:** the descending-glide attractor IS playable (matches "takeoff/landing" mission type from vision.md). Phase F's casual-player test will judge whether it's acceptably playable. If not, escalate. Memory `feedback_verify_self_envelope.md` (just persisted) is the lesson here.
- **Update 2026-05-11 (WP9 verify-self):** the phugoid is **divergent under non-zero forcing**, not merely undamped. Under sustained full throttle the airspeed amplitude grows unbounded (3↔113 m/s oscillation crossing to Infinity then NaN at t≈8s). Previously characterized as "bounded oscillation"; that was only true for the zero-forcing case (the descending-glide attractor, which IS marginally stable). Any baseline throttle injects energy faster than the natural damping can dissipate. Phase 2 phugoid-damping work should target the divergent regime, not merely tighten the bounded regime.
- **Update 2026-05-12 (WP10 + WP10.5):** **architectural side resolved.** arch.md Revision 2026-05-12 D13 adopts β5 (per-surface `clAlphaDot` AoA-rate damping) — the exact "Suggested action" above. WP10.5 (commit 7b2018d) shipped the schema with default 0 (parity with Phase 1 baseline preserved — Vitest 256/256, Playwright 1/1 green). The mechanism is now available to the per-mission tuning that Phase 2 will do. **Tuning side remains open:** non-zero `clAlphaDot` values must be set on wings/h-stab to actually dampen the phugoid; that tuning lives in whichever Phase 2 mission first surfaces a level-cruise need (WP14 waypoint or WP16 combat). WP17 Phase 2 verification has a ≥30s phugoid probe at non-zero throttle (per arch.md D13) that will confirm closure.
- **Update 2026-05-23 (WP14.10):** **D16 + WP14.10 shipped — the β5 mechanism is now physically tunable.** WP10.5's schema landed with the raw-rate form which subsequently turned out to be dimensionally broken (SURFACE-2026-05-12-03); WP14.10 (commit `27324aa`) replaces it with the textbook non-dim form `cl += clAlphaDot · dα/dt · c̄ / (2·max(V, V_REF))`. β5 is now tunable in the textbook 1–10 range — the tuning side of THIS SURFACE is unblocked. WP14.11 (joint clQ/clAlphaDot optimizer) is the natural close mechanism: a cross-threshold joint tune produces a flight-mode that holds level cruise at non-zero throttle, un-skipping `tests/e2e/phugoid-probe.spec.ts`.
- **Status:** partial — architectural + mechanism sides resolved (β4 D17 + β5 D16); tuning side superseded by SURFACE-2026-05-23-01 (phugoid mode requires third-mechanism-layer revision, not further β4/β5 tuning).

### SURFACE-2026-05-11-02 — β1+β4 stable state is a descending glide, not level cruise (parameter-tuning gap)
- **Source:** feature:build (WP6.5 Phase 3 verify-self, 2026-05-11)
- **Target level:** product:wbs (WP7 Phase E retune — already paused and queued)
- **Type:** parameter-tuning / feel
- **Priority:** medium (load-bearing for WP9 verification: "developer takes off, flies around, crashes" needs an aircraft that can hold airspeed)
- **Summary:** With wings incidenceRad=+2°, h-stab incidenceRad=-1°, wings clQ=3, h-stab clQ=8, the airframe is dynamically stable (max|pRate|=149°/s, no tumble). But airspeed bleeds 30→2 m/s and altitude trends 50→33m within the 6s observation window. The system is in a low-energy descending glide because at mass=1000 kg, spawn airspeed v=30 m/s, and zero throttle, lift is only ~14.8% of weight. Force balance for level flight requires v≈90 m/s OR baseline throttle ≈ 0.4 OR reduced mass.
- **Context:** WP6.5 closed the *architectural* gap (no level-trim equilibrium / dynamic instability). The remaining "feels like flight" tuning is exactly WP7 Phase E's job. WP7 was already paused awaiting WP6.5; it now resumes with a clean stable baseline to tune against.
- **Suggested action:** At WP7 Phase E entry: experiment with (a) baseline `throttle = 0.4` at spawn (cheapest — `Controls` class might need a constructor option), (b) `mass = 500–700 kg` (changes ground feel), (c) `area = 9–10 m²` per wing (changes visual feel of wing size). Iterate via lil-gui live; export preset to `aircraft.json` when it feels right. The strong physical priors (incidence 0–4°, clQ 0–16, lift/weight ratio ~1 at cruise speed) make this a bounded search — likely 1–2 lil-gui sessions.
- **Status:** WP7 Phase E disposition 2026-05-11 — the "level cruise" goal is **not closable within Phase 1 scope** due to SURFACE-2026-05-11-04 (phugoid undamped). Phase E shipped option (c) (accept descending glide). This entry stays open as a candidate for Phase 2 if the casual-player feel-check (Phase F AC #7) rejects the descending glide as unplayable.

### SURFACE-2026-04-19-01 — Bundle size: Rapier WASM dominates build
- **Source:** feature:build (WP1 verify-auto)
- **Target level:** product:arch or feature (Phase 3 polish)
- **Type:** perf / tech-debt
- **Summary:** The first production build clocked in at ~2.7 MB unminified / ~978 KB gzipped — above Vite's default 500 KB warning threshold. Dominated by Rapier WASM which is currently bundled inline.
- **Context:** Relates to R1 in research.md (WASM load UX). At 978 KB gzipped, first-load on a mid-range connection is meaningful. WP18 (onboarding) already plans to preload WASM in parallel with splash — this is the mitigation. No action needed before Phase 3.
- **Suggested action:** Leave as-is for Phase 1/2. At Phase 3 WP18/WP21, measure real load time and consider: (a) code-splitting Rapier via dynamic import, (b) loading WASM from `@dimforge/rapier3d-compat`'s external `.wasm` file instead of inlining.
- **Priority:** low (tracked, not urgent)
- **Status:** pending

## Resolved

### SURFACE-2026-05-09-01 — End-to-end browser test infrastructure not configured
- **Source:** feature:verify-codify (WP6 Phase 4)
- **Resolution:** Resolved by WP9.6 (2026-05-11). Adopted `@playwright/test` as a devDependency; minimal config at `playwright.config.ts` (Chromium-only, webServer auto-starts `npm run dev`, retries=0, workers=1, list reporter). Single load-bearing smoke test at `tests/e2e/casual-flight.spec.ts` — loads `/?debug=true`, waits for `window.__aircraft` to be defined, simulates 5s, asserts via `__aircraft.getState()` that `position.{x,y,z}` and `airspeed` are finite, `airspeed > 0` (aircraft moving), aircraft moved from spawn within loose bounds, and no `NaN`/`Infinity` in console or `pageerror` events. Doubles as the regression anchor for SURFACE-2026-05-11-05 (collider fix). New npm script `npm run test:e2e`. Vitest's exclude updated (`vitest.config.ts` created with `tests/e2e/**` excluded) to prevent Vitest/Playwright glob collision. CLAUDE.md "Testing" section updated.
  - **Cross-browser deferred to WP21** (Phase 3 polish) per the original re-target plan.
  - **Suite is intentionally tiny** (one test, one assertion cluster) to avoid the "Playwright tests are flaky" anti-pattern.
- **Status:** resolved 2026-05-11

### SURFACE-2026-05-11-05 — Aircraft has no collider; tunnels through terrain and NaN's the simulation under any non-trivial input
- **Source:** feature:build (WP9 Phase 3 operator-as-tester probe, 2026-05-11)
- **Resolution:** Resolved-with-test by WP9.5 (2026-05-11). One-line addition in `src/aircraft/rigidbody.ts` constructor: `world.createCollider(ColliderDesc.cuboid(0.5, 0.3, 3.0).setDensity(0), this.body)` — matching the fuselage placeholder geometry (`BoxGeometry(1, 0.6, 6)`). `setDensity(0)` keeps the existing `setAdditionalMassProperties` configuration authoritative (otherwise the collider's auto-computed mass would stack on top).
  - **Test coverage:** two new tests in `src/aircraft/rigidbody.test.ts` — (1) structural anchor "attaches at least one collider to the body so it can interact with terrain" (`numColliders() > 0`), (2) behavioral integration "aircraft body collides with a static ground plane (does not tunnel through)" (creates a Rapier world with a static ground collider, drops the aircraft from y=3 with vy=-10, steps 60 ticks, asserts final y > 0). Total 246/246 tests green; tsc clean.
  - **Verification (verify-self):** targeted teleport-to-ground probe via Playwright-MCP — body to y=3 with vy=-10, observed impact at t=0.3 (alt=0.28m, vy reversed to +0.30), then bounded bouncing motion in 1.5–6.4m range. `anyNaN=false`, `collisionDetected=true`. Long-horizon no-input 30s also clean.
  - **Lesson captured (verify-self method):** the original WP9 Phase 3 regression-anchor probe (the gentle casual-input session) was over-broad. It exercised BOTH the tunneling pathology this WP fixes AND the SURFACE-2026-05-11-04 phugoid-divergent-under-forcing pathology that's explicitly out-of-scope. Running it post-fix produced a misleading FAIL signal because the now-stable aircraft climbs to ~110m where it hits the unrelated divergent mode. The targeted teleport probe isolates the collider's contract directly. **General lesson candidate for `/session-store-learning`:** when a regression-anchor exercises multiple defect zones, isolate each zone with a targeted probe; broad probes mask success on one fix when a different defect lights up.
  - **Codified regression anchor:** WP9.6 (2026-05-11) added `tests/e2e/casual-flight.spec.ts` — a `npm run test:e2e` smoke that watches `window.__aircraft.getState()` for 5s and fails on NaN/Infinity. This is the durable regression-anchor for the collider fix; the targeted teleport probe served as the one-shot verify-self proof.
- **Status:** resolved 2026-05-11

### SURFACE-2026-05-09-05 — Phase 4 verify-self required WP7 trim to fully validate (resolved by disposition)
- **Source:** feature:build (WP8 Phase 4 verify-self back-loop, 2026-05-09)
- **Resolution:** Closed-by-implementation 2026-05-11 at WP7 finalize. The original surface expected a WP7-committed-tuned-preset (level cruise) to enable sustained-frame observation of WP8's deferred outcomes (`horizon-tilt-after-roll`, `tower-parallax-on-approach`). WP7's actual disposition was option (c) — ship the WP6.5 baseline (descending glide) unchanged — because the phugoid is undamped (SURFACE-2026-05-11-04) and no single-knob tune produces a usable long-horizon cruise. The descending-glide trajectory IS observable for 6+ seconds before significant altitude loss, which proved sufficient for WP8's Phase F verify-self outcomes during the WP7 verify-self subagent run (multiple successful long-horizon Playwright probes documented in the WP7 archive). The two-way dependency the surface noted is now moot. If WP9 finds that the descending-glide trajectory IS still too short for some cross-browser observation, the right path is to use the `?debug=true` paused state (the debug GUI has a "Pause physics" toggle) for any sustained-frame visual check, not a tuning fix.
- **Status:** resolved 2026-05-11

### SURFACE-2026-05-09-03 — `window.__aircraft` debug telemetry hook
- **Source:** feature:build (WP7 Phase E tuning session, 2026-05-09)
- **Resolution:** Implemented in `src/main.ts` inside the existing `if (debug)` block during the Phase F back-loop diagnosis (2026-05-10). Adds: a `Telemetry` lil-gui folder with read-only displays for altitude/airspeed/vertical speed/pitch/roll/yaw + their rates; a `window.__aircraft` global exposing `{ body, flightModel, getState() }`; a 100 ms `[tel f=N]` `console.log` line carrying the full kinematic state. Gated on `?debug=true`. Used heavily and successfully as the verify-self mechanism for WP6.5 and WP6.6 — the back-loop diagnosis tooling became the project's primary aero-physics observability infrastructure. No tests written (debug-only helper). Surface closed retroactively during WP6.6 task-close (2026-05-11) on the observation that it had been silently providing service for two work-packages.
- **Status:** resolved 2026-05-11

### SURFACE-2026-05-11-03 — β1+β4 stability margin is not robust to airspeed (damping ratio collapses as V grows)
- **Source:** feature:build (WP7 Phase E retune, 2026-05-11)
- **Resolution:** Resolved-with-test by WP6.6 (2026-05-11). One-line change in `src/aircraft/aerosurface.ts` `computeAeroForce`: replaced the airspeed-independent `(1 + clQ)` β4 amplification on `(ω × r)` with `(1 + clQ · max(v, V_REF) / V_REF)` where `v = |bodyState.linvel|` and `V_REF = 30 m/s`. The `max(v, V_REF)` floor preserves WP6.5's low-V β4 calibration bit-for-bit (the formula reduces to `(1 + clQ)` for all v ≤ V_REF — exactly matching pre-fix); above V_REF the amplification grows linearly with v so the damping moment scales as V², matching the V² growth of the destabilizing pitch moment from `incidenceRad`. No 1/V singularity. Schema unchanged (`clQ` keeps its meaning).
  - **Test coverage:** two new regression anchors in `src/aircraft/aerosurface.test.ts` — one asserting the high-V growth branch (`yHigh > yRef` at v=60 vs v=30), one asserting the low-V floor doesn't blow up (forces at v=5,10,20,30 are all finite and bounded). Existing β4 default-zero-parity and sign-convention tests preserved unchanged. Total 244/244 tests green, tsc clean.
  - **Verification:** two Playwright-MCP verify-self trajectories at `?debug=true`:
    - Trajectory A (low-V regression, spawn linvel z=-30): output bit-identical to the pre-fix WP6.5 baseline (max|pRate| ≤ 110°/s, bounded ±30° pitch). The floor branch does its job — no behavior change in the WP6.5 regime.
    - Trajectory B (high-V probe, spawn linvel z=-90, 3·V_REF): max|pRate| = 390°/s (single transient at f=64 during near-stall recovery, surrounding frames ≪ 360°/s), airspeed bounded < 70 m/s, no NaN, no gimbal flips. **Dramatic improvement vs the pre-fix high-V failure modes:** the previous Run A (mass=700, thrust=8000, throttle=0.4) collapsed to NaN at f=54 (airspeed 845 m/s); the previous Run B (WP6.5 baseline + throttle=0.4) produced max|pRate|=1766°/s with ±90° pitch flips. The post-fix Trajectory B has max|pRate| ~4.5× lower than pre-fix Run B, ~3e8× lower than pre-fix Run A, and is bounded throughout.
  - **Residual:** ±50° pitch oscillation at high-V is parameter-tuning territory (precisely WP7 Phase E's job). The architectural goal — "make β4 damping work across the V envelope so tuning can take over" — is met.
  - **Lessons captured:** the initial implementation (no floor: `1 + clQ · v / V_REF`) regressed low-V by shrinking amplification below the WP6.5-calibrated `(1 + clQ)` baseline. When a fix targets an asymmetric problem (here: only high-V was broken), write the formula to be a no-op in the unaffected regime, not a redistribution across both. Caught by Trajectory A retest before commit.
- **Status:** resolved 2026-05-11

### SURFACE-2026-05-10-02 — Phase 1 airframe has no level-trim equilibrium (architectural) + SURFACE-2026-05-11-01 — β1 alone is dynamically unstable
- **Source:** feature:build (AoA sign-convention fix Phase 2 verify-self, 2026-05-10; deepened in static-margin geometry fix attempt, 2026-05-10; β1-alone divergence finding 2026-05-11)
- **Resolution:** Resolved-with-test by WP6.5 (2026-05-11). Two-phase implementation:
  - **β1 (`incidenceRad`)** per arch.md Revision 2026-05-11 / D10 — per-surface mount angle gives the airframe a level-trim equilibrium. Wings +2°, h-stab −1°, v-stab 0.
  - **β4 (`clQ`)** per arch.md "Fallback path" hedge — per-surface pitch-rate damping amplifies the natural ω×r damping mechanism by `(1 + clQ)`. Wings clQ=3, h-stab clQ=8, v-stab clQ=0. No 1/V singularity (a key correction over the prior abandoned attempt's standard `cl_q · c̄ / (2V)` form, which NaN'd at low airspeed).
  - **Verification:** live telemetry 6s window at `http://localhost:5174/?debug=true` showed max|pRate|=149.10°/s (target <360, pass by 2.4×), no gimbal flips, no JS errors. β1 alone produced 8401°/s divergence; β1+β4 brings it to 149 — full stability achieved.
  - **Test coverage:** 7 unit tests in `src/aircraft/aerosurface.test.ts` (default-zero parity for both incidence and clQ, positive-incidence positive-lift, surface-property invariance, sign-convention regression anchors, amplification ratio) + 6 unit tests in `src/aircraft/config.test.ts` (absent / explicit numeric / non-finite-throws for both fields) + 2 integration-boundary tests in `src/aircraft/flightmodel.test.ts` (asserts `incidenceRad` and `clQ` thread through `parseAircraftConfig → FlightModel.surfaces` and produce real-physics behavior). Total 242/242 tests green, tsc clean.
  - **Caveat (deferred to WP7 Phase E retune):** the verified-stable state is a descending glide, not level flight — airspeed bleeds from 30 to ~2 m/s and altitude trends down 50→33m within bounds. The cause is parametric (mass=1000 too high for spawn airspeed v=30 to produce lift=weight without thrust; lift~mg balance only at v≈90 m/s). The architectural goal of WP6.5 ("spawn airborne, no tumble, bounded pitch rate") is fully achieved; making the aircraft hold a useful cruise state is a parameter-tuning concern for WP7.
- **Lessons captured in archived plan:**
  - The β1 static-margin path was empirically refuted in the prior abandoned attempt before β1's actual mechanism was understood — confirmation that "structural property of the schema" (not parameters) was the real gap.
  - The dynamic-instability finding (200× discrepancy between my linearized analytical model and observed angular acceleration) is a useful frame for any future tuning work: linear-stability analysis underestimates the real divergence rate because stall regime + descent-induced AoA coupling are first-order, not perturbative.
  - The agent's first-try sign error on the incidence rotation (P1.6 catch — `-incidenceRad` not `+incidenceRad` per the canonical span axis) demonstrates that **physical-sign tests** ("positive incidence → positive lift") are the only reliable convention anchor. Pure-math identity tests would have passed.
  - Operator instinct to stop-and-escalate after Phase 2 failure (rather than have the agent unilaterally pick A/B/C) preserved the option to choose between path (A) damping or (C) automated tuning-search; (A) succeeded, (C) deferred.
- **Status:** resolved 2026-05-11

### SURFACE-2026-05-10-01 — AoA sign-convention bug in `computeAngleOfAttack` causes divergent pitch instability
- **Source:** feature:build (WP7 Phase F → Phase E back-loop → code investigation, 2026-05-10)
- **Resolution:** Resolved-with-test by commit `2bd5119` (`fix(aero): correct AoA sign convention`). Phase 1 flipped the sign of `perp` in `src/aircraft/aerosurface.ts` `computeAngleOfAttack` and updated `CONVENTIONS.md`. Phase 2 flipped the four routing-table sign multipliers in `flightmodel.ts` (aileron L/R, elevator, rudder) so `+control` still produces the documented body motion under the corrected physics, and corrected 13 test setups whose physics embedded the same sign error. Phase 3 added `src/aircraft/stability.test.ts` with two regression-anchor tests that would have failed under the buggy convention (rest-state |angvel.x| < 0.7 rad/s after 10 steps, was 1.31; perturbation Mx < −100 N·m restoring, was +1561 amplifying). Final 227/227 tests green. **Note:** SURFACE-2026-05-08-01 (resolved 2026-05-08) had documented the *chord-direction* convention but baked the sign-flip in question into the test fixtures it produced — a reminder that conventions need an independent physical check, not just internal consistency. Lesson captured in archived plan retrospect.
- **Status:** resolved 2026-05-10

### SURFACE-2026-05-09-04 — Three.js CubeTexture data-upload contract is non-obvious
- **Source:** feature:build (WP8 Phase 4 verify-self back-loop)
- **Resolution:** Resolved-with-test in WP8 Phase 4 back-loop. Codified by `skybox.test.ts: cube texture face entries are DataTexture instances` and `scene-composition.test.ts: skybox has the data-texture upload-path contract intact`. Lesson for future procedural-cubemap WPs (likely WP20 visual polish): pass `DataTexture` instances to `new CubeTexture(...)`, not the raw `.image` records. Three's `uploadCubeTexture` (three.module.js:12411) inspects `image[0].isDataTexture` to choose the upload branch; the wrong branch throws `texSubImage2D` and corrupts WebGL state.
- **Status:** resolved 2026-05-09

### SURFACE-2026-05-09-02 — No-horizon viewport blocks visual confirmation of attitude
- **Source:** feature:build (WP7 Phase E tuning session)
- **Resolution:** Resolved by WP8 (2026-05-09). Viewport now renders gradient skybox + textured ground + runway + tower; horizon line plainly visible. Verified at the WP8 Phase 4 boot screenshot. Unblocks WP7 Phase F (external casual-player feel-check).
- **Status:** resolved 2026-05-09

### SURFACE-2026-04-19-02 — Destructive-scaffold near-miss
- **Source:** feature:build (WP1 Phase 1)
- **Resolution:** Recovery complete via conversation transcript at the time. Lesson captured in user's auto-memory (`feedback_pre_scaffold_checklist.md`, `feedback_read_cli_flags.md`) and global CLAUDE.md "Pre-risky-action checklist" — `git init` baseline + read flag docs before running scaffolders / template generators / `--overwrite` CLIs.
- **Status:** resolved 2026-04-19

### SURFACE-2026-05-08-01 — AoA sign convention "chord = into the wind"
- **Source:** feature:build (WP4 Phase 2)
- **Resolution:** Documented in `CONVENTIONS.md` §Coordinates during WP4 finalize. Convention is now: `chord` points leading-edge-into-wind; for a forward-flying plane chord = (0,0,−1); positive AoA = wind on underside → positive lift. Six Phase 1 tests rewritten to physical setups during the discovery fix.
- **Status:** resolved 2026-05-08
