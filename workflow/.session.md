---
paused: 2026-06-13
last_workflow: feature
last_wip: workflow/archive/wp22-deploy.md
next_action: feature-workflow — WP24 aircraft selection UI
drive_mode: autopilot
---

# Session Pause — 2026-06-13

## What just closed

**WP22 deploy + share** — Phase 3 deploy milestone closed. Live at https://staymanhou.github.io/areo-test-proty-1/.
- Ship commit: `f8d804b`
- Close commit: `6bc0c00` (LOCAL ONLY — operator chose to defer push)
- Archived WIP: `workflow/archive/wp22-deploy.md`
- Code-quality review: 0 CRITICAL, 3 MAJOR + 4 MINOR auto-backlogged as `SURFACE-2026-06-13-QUALITY-*`

## What got dropped at this pause

**WP23 playtesting — DROPPED from v1** (operator directive, 2026-06-13). Operator handles playtesting outside the workflow system. Same precedent as WP21 (Chromium-only). Critical path is now empty.

## Operator-queued next

**WP24 — Aircraft selection UI** (S–M, Phase 3). Promote the deep-link-only jet (WP14.21) + per-mission `config?` plumbing (WP14.20) into a player-facing choice on mission-select.

### Pre-session-entry decisions for WP24 (operator should confirm at /feature-spec)

1. **Vision-constraint negotiation:** `roadmap.md:62` lists "Multiple aircraft selection" as v1 out-of-scope. The exclusion was written when no second airframe existed; cost calculus has changed since WP14.21 jet shipped. Operator decision required at spec entry: (a) promote to v1 (current direction), or (b) keep deferred to v1.x.
2. **Per-mission constraint vs free choice:** does each mission constrain its airframe, or can the player freely pick across all four missions? Default position in WBS entry: free across all four, per-mission override only if a mission becomes unplayable in a given airframe.
3. **Aerobatic seed disposition:** `aircraft-aerobatic.json` (from `scripted-input-harness` ship, 2026-06-06) — third selectable option, or stay test-fixture? See SURFACE-2026-06-06-06 (Phase B feel-tune deferred).
4. **UI naming:** class-name ("Trainer" / "Jet" / "Aerobatic") vs airframe-name ("Cessna" / "MiG-15" / "Pitts").
5. **UI location:** global toggle on mission-select, or per-mission tile choice?

### Recommended entry point

Run `/feature-spec` (NOT `/feature-plan`) — WP24 fails the small/simple criteria:
- It introduces a new player-facing UI surface (mission-select airframe picker)
- It requires architectural-level decisions (the 5 above)
- It potentially negotiates a v1 scope exclusion

## Other queued (non-critical)

- **WP25** — In-game control manual (read-only). XS–S. Reads from `DEFAULT_KEY_MAP`.
- **WP26** — Audio mix pass: lower engine-loop baseline gain + master volume slider. S. Surfaced at WP22 verify-human ("engine sound too loud").

## Open quality-debt SURFACEs from WP22

All low-or-medium priority, bundle into a single task workflow at operator discretion:
- `SURFACE-2026-06-13-QUALITY-vite-base-hardcoded` (medium) — env-override `base` + comment
- `SURFACE-2026-06-13-QUALITY-ci-no-test-gate` (medium) — add `tsc --noEmit` (or full test suite) to deploy.yml
- `SURFACE-2026-06-13-QUALITY-unnecessary-404-fallback` (medium) — delete the SPA-fallback step or comment-explain
- 4 MINOR (low): base-url-trailing-slash-contract, wip-duplicate-research-heading, deploy-lockfile-not-asserted, surface01-stale-status (already resolved)

## Push state

`origin/main` at `6bc0c00`. Pushed at session-pause per the newly persisted project rule `feedback_ship_pushes_to_main_default.md` (ship commits push to main by default; this project deploys via GitHub Pages from `main`). The push triggered a docs-only redeploy — identical `dist/` artifact.

## Resume

Run `/session-resume` to restore this context, then `/session-start "WP24 aircraft selection UI"` (or directly `/feature-spec WP24 aircraft selection UI`).
